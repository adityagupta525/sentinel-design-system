# Contradiction list

> **Margin note for readers of Sentinel_v2_Spec §4.3.** Its "one approved addition" is **superseded — do not re-add either token.** `data/track` was added and then deleted the same day in favour of one opaque `--color-track` (#edebe7); an alpha track rendered differently on a white card than on the cream canvas. `data/de-emphasis` already shipped as `--color-data-deemph` (#a39a91). Pass 1 added no tokens.
>
> **Margin note on the pattern plates.** The plates were drawn in a standalone file before the token audit, so their hex values and half-pixel type sizes are approximations of intent, not specifications. Where a plate and a token disagree, **the token wins** — `#9a938d` is `--color-data-deemph`, and 12.5 / 11.5px snap to the real scale (13 / 12).

Places where the code disagrees with itself. Flagged, not resolved — each is a decision.

| # | Kind | Where | What disagrees |
|---|---|---|---|
| 1 | Hardcoded hex where a token exists | `ui.tsx` Composer | Border colours `#b69377` / `#e1deda` set inline instead of `bronze` / `line`; MoneyComposer uses `ring-line` — two composers, two border mechanisms, and MoneyComposer has **no focus state** |
| 2 | Hardcoded hex where a token exists | `ui.tsx` HeroNumberCard meters | `#b69377` / `#ebd4c3` inline instead of `alloc-equity` / `alloc-cash` |
| 3 | Hardcoded hex where a token exists | `ui.tsx` ProgressRail | Gradient `#715035 → #b69377 → #ebd4c3` inline; also a **three-stop gradient** on a magnitude rail contradicts "one bronze hue carries magnitude" |
| 4 | Hardcoded hex where a token exists | `Chat.tsx` SHARMA_ALLOC, `Portfolio.tsx` ALLOC | Allocation segment colours passed as `#b69377 #d9bb9e #ebd4c3` rather than the `alloc-*` tokens they duplicate |
| 5 | Hardcoded hex where a token exists | `icons.tsx`, ChipGlyph, CanvasHeader, ScrollToBottomButton, ProgressTrace, Home jump rows, StatusSpacer | SVG strokes `#251F1B` `#B69377` `#715035` `#605954` typed per-icon instead of tokens |
| 6 ✅ | Resolved → `--color-status-over-bg` | DeltaPill over, Portfolio "Over ceiling" | `#f3e2da` — a pink-tinted surface **behind danger text**. Rule 2 says bad news sits on the peach bubble, never a red-ish surface; is `#f3e2da` peach or red? Needs a call |
| 7 ✅ | Resolved → status family (green reserved for ok; journeys.tsx:446 vs-Bmk → muted + sign) | DeltaPill ok, ReviewResult "vs Bmk" | `#e4ecdf` / `#4b6141` green — the third "status colour" is not in `@theme` at all |
| 8 ✅ | Resolved → NOT a token. A dash pattern already distinguishes the border; a second hue double-encodes it. `#d9cfc3` dropped, dashed hairlines drawn with `--color-line`. Two line tokens total, both warm | GreetingDivider, Keyboard | Was: `#d9cfc3` beside `line`, `line-soft`, `bubble-edge` |
| 9 | Token missing | Pill × glyph, LockedFigure lock | `#8a7a6a` — a fifth grey between `muted` and `data-deemph` |
| 10 ✅ | Resolved → `--color-cta` / `-top` / `-bottom` (one surface, one gradient) | DarkButton, Composer send, Stop, MoneyComposer send | Was: repeated inline in four places |
| 11 ✅ | Resolved → `--color-desk` | `index.css` body | Page background `#dedbd6` |
| 12 | Token missing | Keyboard.tsx | Five keyboard-only hex values (`#fefcfa #ebd9cc #d9c5b3 #ede9e4 #e2ddd8`) |
| 13 ✅ | Resolved → `--color-track`, data-track deleted | `alloc-track` vs `data-track` | AllocationCard/HeroNumberCard/ConcentrationBar use `#edebe7`; Dumbbell/AttributionChart use sand@40%. Both are "the bar track" |
| 14 ✅ | Resolved → alloc-equity + alloc-cash deleted, rebound to `bronze`/`bubble-edge`; `--color-selected` added for the interactive fill | `alloc-cash` = `bubble-edge` = `#ebd4c3`; `alloc-equity` = `bronze` = `#b69377` | Aliases without a stated hierarchy — which is canonical? |
| 15 ✅ | Resolved → Pill (md/sm, ≥44 hit) + Badge (status/meta, 8×3, no onClick) + InlineActionRow (was ActionCard, 34→36); DecisionsStrip excluded by decision. Was: | Chip · AnswerChip(outline) · FilterChip · ActionCard buttons · DecisionsStrip pills · FundsTurn/FundExplorer inline pills · KindTag — seven pill implementations with heights 36/36/32/34/26/28/19 and rings `bubble-edge` or `line` |
| 16 | Two components, one problem | Disclaimers | DemoFooter (16 lh, muted) vs Provenance (15 lh, muted) vs StandingDisclosure (15 lh, data-deemph) vs Drawer inline "Demo data ·" line |
| 17 | Two components, one problem | Bubbles | UserBubble 14/19 py-10 vs QAPair bubble 13/18 py-8 — same shape, two sizes |
| 18 | Two components, one problem | Peach callouts | ConstraintCallout (Eyebrow + 13/19 body) vs Portfolio inline mandate callout (13px bold title, not an eyebrow) vs DisclosureBlock (12/17 body) vs ConfirmSheet/Offer inline callouts |
| 19 ✅ | Resolved by deletion, 2026-09-18 (F-11) — the owner's ruling. `package.json` is `private: true` and nothing inside the system imported it, so removing the export broke no one. Was: | CTA | StickyCTA (C17, sits alone) vs Dock `cta` slot. StickyCTA on its own would replace the composer — the code's own law says it never may. Unused; delete or keep? |
| 20 | Two components, one problem | Eyebrow | `Eyebrow` = 11px / 0.08em; Drawer section labels 11px / **0.06em**; Portfolio section labels **12px** / 0.06em — three eyebrows |
| 21 | Two components, one problem | Explainer sheets | ExplainerSheet (white, py-28, "Got it" chip) vs ConfirmSheet & OnePager (canvas bg, max-h 88%/86%, Dock inside) — two sheet chromes |
| 22 ✅ | Resolved → `--text-11-5` + `--type-meta-font` | MessageActions, ChartLegend, ChartLine, SearchField, ListRow | Was: `11.5px` font size — "only non-integer size in the system", logged as a one-off on four surfaces that no longer carry it. Seven uses were setting it by hand at the SAME weight and leading (medium / 16), differing only in colour — a role nobody had named, not a one-off. Named rather than removed: deleting it would have restyled five components, and readme.md §44 lists it among the literal sizes while §231 specifies it for the legend. ChartLine's 10.5 series name, a second non-integer that this row did not know about, snapped to `--text-10` — the pair now reads 11.5 / 10 and separates better than 11.5 / 10.5 did |
| 23 | One-off value | Composer attach disc | `rounded-[22px]` on a 42px square (effectively full) — every other disc uses `rounded-full` |
| 24 | One-off value | DetourBanner | radius 10 — appears nowhere else |
| 25 | One-off value | DataTableCard showAll, LockedFigure | radius 12 — buttons elsewhere are full or 16 |
| 26 | One-off value | Card shadow | `rgba(37,31,27,0.05)` on most cards, `0.04` on Home suggestion card and FundExplorer cards — same card, two alphas |
| 27 | One-off value | Row heights | 42 (SuggestionRow, AllocationCard) · 46 (jump rows, Drawer) · 52 (holdings, compliance) — three list-row heights |
| 28 | One-off value | Display sizes | Darker Grotesque at 24 · 27 · 40 · 64 — no scale, `27` is unique to the greeting |
| 29 | One-off value | Durations | 13 distinct motion durations (150→600ms) plus AllocationCard bar uses `easeInOut` while everything else uses `EASE`; `sentinel-shimmer` still uses CSS `ease-in-out` (`dot-pulse`, which replaced `dot-bounce` on 2026-09-17, is on `var(--ease)`) |
| 30 ✅ | Resolved → one `--scrim` derived from `--color-ink`, opacity at point of use (0.25 drawer, 0.4 sheets); modal layers only — the canvas that was the "none" case is gone (43). The black scrim is gone — it cast the warm palette grey | Drawer, sheets, canvases | Was: three scrims (`black/25`, `ink/40`, `ink/20`) |
| 31 | Font declaration | `index.css` `@font-face` | Five colon-named families (`'Urbanist:SemiBold'`) instead of one family with weights; SentinelText then sets `font-normal` (400) on a family that IS the weight — the class and the family fight |
| 32 | Copy vs copy | `Portfolio.tsx` vs risk/proposal/confirm journeys | Single-fund ceiling stated as **15%** on the review screen and **25%** everywhere else (already flagged in-code as NEEDS DECISION) |
| 33 | Copy vs copy | `Portfolio.tsx` | "71 %" with a space; every other percentage is tight ("71%") |
| 34 | Chevron colour | IconChevronRight `#B69377` · inline jump-row chevron `#b69377` · CanvasHeader back `#715035` · ProgressTrace `#605954` | Four chevrons, three colours |
| 35 | Colour vs rule | AnswerChip `muted` variant | `text-bronze-deep/60` — a translucent label, contradicting the "warm grey carries de-emphasis" rule (`data-deemph` exists for this) |
| 36 | Icon vs token | IconArrow default `stroke="white"` | Keyword `white` while every other stroke is hex; no white token exists |
| 37 | Placeholder law | Composer default placeholder | Default prop is the Home string, but `COMPOSER_PLACEHOLDER` map says the law is enforced "in one map" — the default duplicates the map's `home` entry |
| 38 ◐ | **Open debt — deliberately unpaid.** Icon grid: six source glyphs on six grids (17.33 / 18 / 15 / 12.37 / 13.33 / 12.58) with 1.33–1.5px strokes, normalising to 1.8–2.0px at 24px, beside four Lucide additions on a 24px grid at 1.5px. Same class of drift as the four pill heights — but normalising the six changes every screen in the product, so it is not worth paying today. **Two origins is an interim state, not the intended end state. Trigger: at the app's next visual refresh, redraw all six onto the 24px grid at 1.5px stroke and collapse the set to one origin.** | 45 ✅ | **Renamed 2026-09-17.** `Legend` → `ChartLegend`, and the old file is deleted rather than aliased — two names for one component is the drift the pill and token audits cleared. The rename came with two rules the old contract did not state: **left-aligned to the plot's left edge, never centred**, and **ordered by value descending, always**, so the legend doubles as the ranking and survives greyscale. `CHART_RAMP` moved to `components/data/chartMath.jsx` and is still exported on the namespace. `Legend` had exactly one caller in this project (the Data card), migrated in the same pass | `components/data/ChartLegend.*`, `components/data/data.card.html` | A consuming project importing `Legend` breaks loudly, which is the intent — there is no silent fallback to a contract that no longer exists |
| 46 ◐ | **Open debt — the honest reading of "a component with no page is not in the system" (2026-09-17).** Sixty-five components are `building`: real, compiled into the bundle, documented on a group card, and without their own nine-block page. Pass one drew seven pages (Pill + the six charts) because v10 §3 scoped it that way; drawing sixty-five in one sitting is how quality goes. The index shows the debt per row rather than in a footnote, and it cannot be hidden: `shipped` is computed from the existence of `pages/<Name>.html`. **Trigger: each subsequent pass draws the page in the same commit as the component it touches, and the `building` count falls monotonically. Any pass that leaves it flat is the rule being broken.** **Amended 2026-09-17:** the count is measured over non-icon components. Icon glyphs (`Icon*`) are documented collectively on the Iconography card by design — a nine-block page for a single 24px glyph would be ceremony, and `IconInfo`, added in v11, is the only row this amendment newly covers. v11 read 65 → 66 `building` in absolute terms **because it added ten components**; on the amended measure the pre-existing 65 is flat and every one of the ten new components has a page except `IconInfo`. Stated rather than smoothed over | `pages/*`, `scripts/build-index.js` | v10 §2.3 |
| 47 ✅ | **Ruled 2026-09-17 — the charting library.** Neither named candidate. **Chart.js: no**, on a hard constraint rather than taste — it renders to canvas, canvas cannot take a CSS custom property per element, so every token becomes a hex string in a config (the exact thing the token audit removed) and the whole chart is a single node with nothing for a screen reader, which matters precisely because the table view exists. **Recharts: viable, heavier than the job** — ~370KB, and what we need is a line with one or two series, a bar, a 100% stacked bar and a 96px sparkline; its defaults (a cartesian grid we don't draw, a legend we place ourselves, a floating tooltip v9 §2.6 ruled out on a phone) are a list of things to switch off. **Taken: `d3-scale` + `d3-shape` for the maths only** — `nice()`, `ticks()`, `scaleBand`, `line()`/`area()` with `curveMonotoneX` — because what reads as amateur in our charts is unrounded ticks and baselines off the grid, not a missing renderer. **No charting library renders a Sentinel chart**; a library may compute a scale or generate a path | `components/data/chartMath.jsx`, the Charts section of the readme | If weight is ever traded for speed, the fallback is Recharts wrapped so tightly no screen imports it |
| 48 ✅ | **Ruled and fixed 2026-09-17 — the palette carries one chart mark, not four.** WCAG 1.4.11 wants 3.0:1 against adjacent colour; measured, ramp 2 / 3 / 4 return 2.57 / 1.66 / 1.30 on canvas and adjacent steps separate by as little as 1.28. The categorical ruling (39) was right for the wrong reason: the deeper finding is that only ramp 1 is legible as a shape against the page at all, so `ChartShare` inherited the exact defect the pie was rejected for. Fixed without touching a colour — `markColor()` returns ramp 1 for anything separating from the background (so `ChartBar` is now one colour, rank carried by length), the `muted` tone swaps `--color-data-deemph` → `--color-muted` (2.52 → 6.26), and `--color-track` takes the `--border-hairline` inset everywhere. **Deviation from the audit's proposed remedy, with the number:** a 0.5px `--color-line` edge between segments cannot be the compliance mechanism either — it measures 1.06 against ramp 4, and no colour in the palette clears 3:1 against all four steps (`surface` 1.42, `muted` 1.05, `ink` 2.25 at worst). So segments separate by a `--space-2` gap and a **mandatory label**, and 1.4.11's text-equivalent exemption is what makes the chart compliant; the hairline is an aid | `components/data/chartMath.jsx`, `ChartBar`, `ChartShare`, `ChartLine`, `StatTile` | The full table is in the readme's Perceivability section |
| 49 ✅ | **Fixed 2026-09-17 — focus was removed, not forgotten.** `:focus-visible` appeared 0 times, `tabIndex` 0 times, `role=` once, `aria-*` four times, and `Pressable` — the base under thirty component files — set `outline: none` with nothing in its place. It now inherits both of `Pill`'s guarantees: a ≥44pt hit expansion measured off the rendered box, and a `:focus-visible` ring. **Two measured corrections to the brief:** `--focus-ring` alone is bronze at 24% alpha, composites to #e7ddd4 and measures **1.22:1** — it fails WCAG 2.4.13's 3.0 floor, so it is paired with a solid ring and its comment now says never to use it alone; and `--border-focus` was `var(--color-bronze)` at **2.57**, which also fails, so it is re-pointed at `--color-bronze-deep` (6.59 / 7.24) — a value already in the palette, aimed at the wrong member of its own family | `components/actions/Pressable.*`, `tokens/colors.css`, `tokens/effects.css` | 30 files became operable without being edited |
| 50 ◐ | **Open debt — dated and triggered, 2026-09-17.** The type ramp and the dimensional scale are bound (12 named roles carrying size · leading · weight · tracking; `--border-hairline` live at last; 2/3/5/13/14 labelled as exceptions), and the 17 components this pass touched were converted to them. **The rest were not**: the literal count across `components/**/*.jsx` was **719** at the start of the pass, measured with a 21-property regex — not the 289 the audit reported, which used a narrower property list. Converting all 61 files in one sitting is how a pass ships a regression. **Trigger: every subsequent pass converts the files it touches, and the literal count falls monotonically; any pass that leaves it flat is the rule being broken.** Same shape as 46, and deliberately so | `components/**` | Re-measured at the head of each pass |
| 51 ✅ | **Deleted 2026-09-17.** `--dur-canvas: 380ms`, unreferenced since contradiction 40 removed the canvas in v5. Its duration outlived the surface by two passes | `tokens/effects.css` | The closing dead-token re-run is in the report |
| 52 ◐ | **Open, and not ours to close — recorded 2026-09-17.** Three findings that are decisions rather than defects. **(a)** `danger #b4552f` on `status-over-bg #f3e2da` measures **3.90** — fine for a non-text element or large text, short of 4.5 for the 10–11px label the OVER CEILING pill actually sets. The system's own rule saves it from being a perception failure, because colour never signals alone there and the pill always carries the word; recorded as a **known, accepted 3.90** rather than left to be rediscovered. **(b) Disclosure depth** — ours is one line in the footer; GitHub puts its notice under the header, Agoda above the first message, Wise at the upload point. For a SEBI-adjacent product that is a compliance call, not a design preference. **(c) Voice input and dark mode** — absent, plausible for an advisor between meetings and for a field tool respectively; **mode chips in the composer are a deliberate no** (the model is our problem, not the advisor's). **Trigger: (a) closes if the Badge label ever rises to 12px+ or the pair is re-toned; (b) and (c) close when Rahul and the compliance reviewer decide** | `Badge`, `StandingDisclosure`, `Composer` | Not guessed, per the three locked decisions |
| 53 ✅ | **Three self-inflicted defects, caught in review and fixed the same day, 2026-09-17.** All three were the same class: a claim in a comment that the build did not keep — which is exactly what this pass existed to correct, so they are logged rather than quietly repaired. **(a)** `Pressable` painted its focus ring with `box-shadow`, and an inline `style.boxShadow` from a caller beats a stylesheet rule — measured, a chip passing its own 1px ring returned `rgb(225,222,218) 0 0 0 1px` on focus and no ring at all. Four callers did exactly that (`RangePills`, `ResponseFeedback`, `CanvasHeader`, `ScrollToBottomButton`), so "thirty files become operable" held for most and failed silently for the rest. Now a 2px solid `outline` at `outline-offset: 2px` plus the halo; `outline` is never passed inline, and the contract says callers must not pass it. **(b)** `ChartShare`'s legend guard was a no-op: `legend \|\| ranked.length <= 2` gated by `labelled && legend` reduces to `legend`, so `legend={false}` with four segments shipped four bands this very pass had documented as inseparable. Now `ranked.length > 2` forces the legend on and ignores the prop. **(c)** The blanket `height: 1 → --border-hairline` conversion also halved `ChartBar`'s baseline and `Dumbbell`'s rail — chart data-ink, not dividers — so a perceivability pass made an axis thinner at 1.22:1. Both back at `--border-1`, and the token's comment now scopes it to row dividers and card edges | `Pressable`, `ChartShare`, `ChartBar`, `Dumbbell`, `tokens/spacing.css` | A guard that exists only in a comment is not a guard |
| 54 ✅ | **Ruled 2026-09-17 — a peek share carries at most two segments.** Forcing `ChartShare`'s legend on (53) collided head-on with the 96pt peek budget: three legend rows measured a **99px** chart block inside a 96px card, so the chart could neither legally drop its labels (the label is the compliance mechanism) nor legally keep them (the peek contract every chart in the family rests on). Two stated rules were simultaneously false in the build, which is the defect, not the overrun. **The resolution is fewer segments, not smaller type:** `density="peek"` collapses the tail into one `Other`, so the bar is leader + Other, both DIRECT-labelled at the bar's ends, and the legend becomes unnecessary rather than suppressed — the same reason `legend={false}` was always honoured at two segments or fewer, so the guard defers with no special case. Peek is recognition, not reading: "Equity 71% · Other 29%" is the whole job of a 96pt preview and the breakdown is one tap away. Measured after: 8 + 6 + 15 = **29px** (bar 8 · gap 6 · one --leading-15 direct-label row). Also fixed on the page in the same commit — the "no legend" variant was demonstrating the opposite of its caption, and the anatomy block still annotated the pre-fix "1px surface gap" when the separator is a `--space-2` gap plus a hairline edge | `ChartShare.*`, `pages/ChartShare.html`, readme Sizes | Both false sentences are corrected, not deleted |
| 55 ✅ | **Fixed 2026-09-17.** `Pressable` rendered its focus and hit-expansion rules as a `<style>` child of its own `<button>` — metadata content inside phrasing content, which is an invalid content model, and duplicated once per instance: 53 identical copies on the `RangePills` page alone. The rules are global and identical for every instance, so they now live in `tokens/effects.css`, with `MotionGuard` shipping the same pair from the bundle for a consuming project that brings its own page styles. Same reasoning as the reduced-motion block, which had already solved this problem one component over and should have been the precedent the first time | `components/actions/Pressable.jsx`, `tokens/effects.css`, `MotionGuard` | −53 DOM nodes on one page |
| 56 ✅ | **Fixed 2026-09-17 — a latent bug in the page kit, not in the caption that exposed it.** `Anatomy`'s side annotations were anchored `right: -90` / `left: -90` with `whiteSpace: nowrap` and no width cap, which pins the tag's OUTER edge 90px beyond the frame and lets the box grow **inwards**. Measured: a 245px annotation computed `left: 156px` and laid 155px of itself across a 311px specimen, colliding with the legend's own value column. Every annotation wider than the 96px gutter did this on every page built on the kit — v11's longer string was simply the first one long enough to see. Fixed at the anchor: side tags now sit at `left: 100%` / `right: 100%` with a 22px margin so they grow AWAY from the specimen, wrap inside a fixed 104px gutter instead of nowrapping, and the block's horizontal padding went 96 → 128 to hold them. Re-measured on two pages: right tag 640→744 against a frame ending at 618, left tag 181→285 against a frame starting at 307, all four inside the card. Length can no longer move a tag anywhere | `pages/page-kit.jsx` | Shortening the caption would have hidden it for the next long annotation |
| components/icons/*, assets/icons/* | Documented on the Iconography card |
| 57 ◐ | **Open by decision, 2026-09-18 (F-21).** Two things in this system are called a large row: `--h-row-lg` is **46px** (Jump-back-in and drawer rows) and `ListRow size='lg'` is **72px**, now `--h-row-2l`. The token was **not renamed** to free the name. The owner's ruling, with the reason stated so it is not re-argued: `"private": true` means there is no external consumer to break, so that was never the reason — the reason is that `--h-row-lg`'s real consumers were three raw `46`s and `--h-row-xl`'s two raw `52`s in `ui_kits` (F-24's pattern again), wired to their tokens in the same commit; renaming a token while wiring its first consumers would have made the wiring unreadable in the diff. The prop name `lg` stays because it is the component's contract and a subtitle is what makes a row large. Closes when a consumer confuses the two in a review, or never | `tokens/spacing.css` `--h-row-lg` / `--h-row-2l`, `components/lists/ListRow.jsx` | `pages/ListRow.html` §6 says the same in the page |
| 43 ✅ | **Closed by deletion 2026-09-17**, not by redraw. `ui_kits/sentinel-app/portfolio.jsx` and `proposal.jsx` were product screens living in the system's kit — and that, not the canvas framing, was the real fault: they duplicated Journey A and B artboards the project already owns and keeps current, so they were stale by construction. Deleted with `app.jsx`'s `canvas` branch, the `onOpenCanvas` prop and the `kit-canvas` keyframe. **Rule recorded: the design system holds tokens, components and states — it does not hold product screens.** History: opened 2026-09-16 as scheduled-for-redraw with a trigger, after route (b) was taken in the v6 Part 1 pass — the right call to make, with an expiry; v7 §2.1 issued that expiry and changed the remedy from redraw to deletion | `ui_kits/sentinel-app/*` | The click-through of the product belongs where the product screens are |
| 44 ◐ | **Open debt — dated and triggered, 2026-09-17.** `ArtifactCard`'s state values are `'peek' \| 'expanded' \| 'filling'`; `'collapsed'` was **not** kept as an alias, deliberately — the name invited the 132px budget v7 §0.5 withdrew. A caller still passing `'collapsed'` falls through to the `peek` default, so it degrades to the right rendering rather than breaking, but it is a call site lying about the contract. **Trigger: the consuming project migrates its `state="collapsed"` call sites to `"peek"` and reports zero remaining — evidenced by reading a migrated call site back from its own copy — at which point this row closes.** No code change is owed on the system side; this is a migration debt, tracked so it does not become invisible | `components/cards/ArtifactCard.*`, consuming projects | Same shape as 41 |
| 41 ✅ | **Closed 2026-09-16.** `ArtifactCard.onOpen`, the deprecated alias for `onToggle`, is deleted from the `.jsx`, the `.d.ts` and the `.prompt.md`. Evidence that satisfied the trigger: the journey project migrated every call site to `onToggle` and reported zero `onOpen` call sites remaining. One prop name, not two | `components/cards/ArtifactCard.*` | No tombstone comment in the component — this row is the record |
| 42 ✅ | **Closed 2026-09-16.** Three kit files threw into the bundle's `__errors`. Two causes, both in kit source, neither a platform fault. (a) **React #299**, decoded against the official decoder as *"Target container is not a DOM element"* (`createRoot(...)`): `a-screens.jsx`, `b-board.jsx` and `app.jsx` each ended in an unguarded `ReactDOM.createRoot(document.getElementById('root'))`. They are page scripts and are also compiled into `_ds_bundle.js`, which is loaded by pages that have no `#root` — `getElementById` returns `null` there. Fixed by mounting only when a root exists. (b) **`B01 is not defined`**: not a rename and not a lost export — `B01` is defined and exported by `b-screens.jsx` throughout, and the B/06 repoint never touched its identifier. The bundle compiles files alphabetically, so `b-board.jsx` evaluates **before** `b-screens.jsx` and its top-level `BOARD` array referenced screens that did not exist yet. Fixed by resolving screens from `window` at render time. `b-board` carried both faults, the `B01` throw masking its own #299 | `ui_kits/journey-a-risk/a-screens.jsx`, `ui_kits/journey-b/b-board.jsx`, `ui_kits/sentinel-app/app.jsx` | Deleting the files, try/catch and manifest removal were all rejected |
| 39 ✅ | **Ruled, with evidence — do not re-open.** Categorical colour: the team asked for pie charts and "colour themes" plural. Both are requests for a categorical palette, which this system cannot supply. Validator output, not taste: the bronze→sand ramp `#A9743F · #C9A77C · #E0CDB6` gives an adjacent-pair **ΔE 11.4** for normal vision against a floor of 15, all three below the chroma floor; a muted earthy trio `#B45F3C · #6E7B4B · #4A6072` gives **ΔE 3.8** under protanopia; four muted hues give **ΔE 1.5**; a set that passes needs `#C2410C / #0F766E / #4F46E5` saturation, which is not this brand. A pie here is therefore either unreadable for colourblind readers or a different brand — the only two outcomes. **Substitute: the direct-labelled stacked bar plus `Legend`**, the form Copilot Money ships for the same job (`Mutual Fund ──── 85.4%` / `ETF ── 14.59%`, no donut, no categorical hues) and the form Sentinel's own allocation bar already takes. A ring for a dashboard tile is the two-segment meter: one ratio, one hue. **Ruled 2026-09-16; the evidence is recorded so the question is not re-litigated next quarter.** | Part 6 of the v5 spec | Charts section of the readme; `Legend`, `CHART_RAMP` |
| 40 ✅ | **Resolved by removal, 2026-09-16 (v5 Part 1).** The sheet/canvas scrim rule had two cases; the second case's subject no longer exists. The canvas is removed from the product — there is no `‹ Back to chat`, because there is nowhere to go back from. Only modal layers carry `--scrim` (sheets 40%, drawer 25%). The canvas reasoning is kept in the readme rather than deleted, because it still governs any future push surface. The replacement rule is **no nested scroll**: an expanded `ArtifactCard` takes its natural height and the thread carries the scroll. `CanvasHeader` is deprecated; its title and ⋯ moved onto the card's expanded header and its back pill is repurposed as the detour-resume and sheet-dismiss pill | readme scrim rule, `CanvasHeader`, `ArtifactCard`, `ui_kits/journey-b` B/06 | B/06 now documents a removed surface, kept only as chart-in-situ source |
