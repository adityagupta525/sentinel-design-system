/* @ds-bundle: {"format":4,"namespace":"SentinelDesignSystem_0682a2","components":[{"name":"AnswerChip","sourcePath":"components/actions/AnswerChip.jsx"},{"name":"ChipRow","sourcePath":"components/actions/ChipRow.jsx"},{"name":"ClientChip","sourcePath":"components/actions/ClientChip.jsx"},{"name":"DarkButton","sourcePath":"components/actions/DarkButton.jsx"},{"name":"DecisionsStrip","sourcePath":"components/actions/DecisionsStrip.jsx"},{"name":"DownloadAction","sourcePath":"components/actions/DownloadAction.jsx"},{"name":"InfoDot","sourcePath":"components/actions/InfoDot.jsx"},{"name":"InlineActionRow","sourcePath":"components/actions/InlineActionRow.jsx"},{"name":"Pill","sourcePath":"components/actions/Pill.jsx"},{"name":"Pressable","sourcePath":"components/actions/Pressable.jsx"},{"name":"RangePills","sourcePath":"components/actions/RangePills.jsx"},{"name":"StickyCTA","sourcePath":"components/actions/StickyCTA.jsx"},{"name":"SuggestionRow","sourcePath":"components/actions/SuggestionRow.jsx"},{"name":"AllocationCard","sourcePath":"components/cards/AllocationCard.jsx"},{"name":"ArtifactCard","sourcePath":"components/cards/ArtifactCard.jsx"},{"name":"Badge","sourcePath":"components/cards/Badge.jsx"},{"name":"ConstraintCallout","sourcePath":"components/cards/ConstraintCallout.jsx"},{"name":"DataTableCard","sourcePath":"components/cards/DataTableCard.jsx"},{"name":"DisclosureBlock","sourcePath":"components/cards/DisclosureBlock.jsx"},{"name":"ExplainerSheet","sourcePath":"components/cards/ExplainerSheet.jsx"},{"name":"HeroNumberCard","sourcePath":"components/cards/HeroNumberCard.jsx"},{"name":"InfoCard","sourcePath":"components/cards/InfoCard.jsx"},{"name":"MoveCard","sourcePath":"components/cards/MoveCard.jsx"},{"name":"RejectCallout","sourcePath":"components/cards/RejectCallout.jsx"},{"name":"DetourBanner","sourcePath":"components/chat/DetourBanner.jsx"},{"name":"GreetingDivider","sourcePath":"components/chat/GreetingDivider.jsx"},{"name":"MessageActions","sourcePath":"components/chat/MessageActions.jsx"},{"name":"ParseNote","sourcePath":"components/chat/ParseNote.jsx"},{"name":"ProgressTrace","sourcePath":"components/chat/ProgressTrace.jsx"},{"name":"QAPair","sourcePath":"components/chat/QAPair.jsx"},{"name":"ResponseFeedback","sourcePath":"components/chat/ResponseFeedback.jsx"},{"name":"SentinelBlock","sourcePath":"components/chat/SentinelBlock.jsx"},{"name":"SentinelText","sourcePath":"components/chat/SentinelText.jsx"},{"name":"SentinelThinking","sourcePath":"components/chat/SentinelThinking.jsx"},{"name":"StepTrace","sourcePath":"components/chat/StepTrace.jsx"},{"name":"UserBubble","sourcePath":"components/chat/UserBubble.jsx"},{"name":"VersionRow","sourcePath":"components/chat/VersionRow.jsx"},{"name":"Composer","sourcePath":"components/composer/Composer.jsx"},{"name":"MoneyComposer","sourcePath":"components/composer/MoneyComposer.jsx"},{"name":"AttributionChart","sourcePath":"components/data/AttributionChart.jsx"},{"name":"ChartBar","sourcePath":"components/data/ChartBar.jsx"},{"name":"ChartLegend","sourcePath":"components/data/ChartLegend.jsx"},{"name":"ChartLine","sourcePath":"components/data/ChartLine.jsx"},{"name":"ChartLineMultiples","sourcePath":"components/data/ChartLine.jsx"},{"name":"ChartReadout","sourcePath":"components/data/ChartReadout.jsx"},{"name":"ChartShare","sourcePath":"components/data/ChartShare.jsx"},{"name":"ChartTooltip","sourcePath":"components/data/ChartTooltip.jsx"},{"name":"ConcentrationBar","sourcePath":"components/data/ConcentrationBar.jsx"},{"name":"DrawnCheck","sourcePath":"components/data/DrawnCheck.jsx"},{"name":"Dumbbell","sourcePath":"components/data/Dumbbell.jsx"},{"name":"ProgressRail","sourcePath":"components/data/ProgressRail.jsx"},{"name":"StatTile","sourcePath":"components/data/StatTile.jsx"},{"name":"Sparkline","sourcePath":"components/data/StatTile.jsx"},{"name":"CHART_RAMP","sourcePath":"components/data/chartMath.jsx"},{"name":"SEGMENT_GAP","sourcePath":"components/data/chartMath.jsx"},{"name":"PLOT","sourcePath":"components/data/chartMath.jsx"},{"name":"AXIS_BAND","sourcePath":"components/data/chartMath.jsx"},{"name":"FileUpload","sourcePath":"components/forms/FileUpload.jsx"},{"name":"SearchField","sourcePath":"components/forms/SearchField.jsx"},{"name":"SelectionMark","sourcePath":"components/forms/SelectionMark.jsx"},{"name":"IconArrow","sourcePath":"components/icons/IconArrow.jsx"},{"name":"IconAttach","sourcePath":"components/icons/IconAttach.jsx"},{"name":"IconCheckCircle","sourcePath":"components/icons/IconCheckCircle.jsx"},{"name":"IconChevronRight","sourcePath":"components/icons/IconChevronRight.jsx"},{"name":"IconDownload","sourcePath":"components/icons/IconDownload.jsx"},{"name":"IconFile","sourcePath":"components/icons/IconFile.jsx"},{"name":"IconInfo","sourcePath":"components/icons/IconInfo.jsx"},{"name":"IconMenu","sourcePath":"components/icons/IconMenu.jsx"},{"name":"IconPlus","sourcePath":"components/icons/IconPlus.jsx"},{"name":"IconSparkle","sourcePath":"components/icons/IconSparkle.jsx"},{"name":"IconSpinner","sourcePath":"components/icons/IconSpinner.jsx"},{"name":"FollowUpRow","sourcePath":"components/lists/FollowUpRow.jsx"},{"name":"List","sourcePath":"components/lists/List.jsx"},{"name":"ListRow","sourcePath":"components/lists/ListRow.jsx"},{"name":"CanvasHeader","sourcePath":"components/shell/CanvasHeader.jsx"},{"name":"Dock","sourcePath":"components/shell/Dock.jsx"},{"name":"HomeIndicator","sourcePath":"components/shell/HomeIndicator.jsx"},{"name":"REDUCED_MOTION_CSS","sourcePath":"components/shell/MotionGuard.jsx"},{"name":"MotionGuard","sourcePath":"components/shell/MotionGuard.jsx"},{"name":"PhoneFrame","sourcePath":"components/shell/PhoneFrame.jsx"},{"name":"ScreenBackdrop","sourcePath":"components/shell/ScreenBackdrop.jsx"},{"name":"ScrollToBottomButton","sourcePath":"components/shell/ScrollToBottomButton.jsx"},{"name":"StatusSpacer","sourcePath":"components/shell/StatusSpacer.jsx"},{"name":"TopBar","sourcePath":"components/shell/TopBar.jsx"},{"name":"DemoFooter","sourcePath":"components/text/DemoFooter.jsx"},{"name":"Eyebrow","sourcePath":"components/text/Eyebrow.jsx"},{"name":"EyebrowDivider","sourcePath":"components/text/EyebrowDivider.jsx"},{"name":"Provenance","sourcePath":"components/text/Provenance.jsx"},{"name":"StandingDisclosure","sourcePath":"components/text/StandingDisclosure.jsx"}],"sourceHashes":{"components/actions/AnswerChip.jsx":"027fb876714b","components/actions/ChipRow.jsx":"dcdec0d15f36","components/actions/ClientChip.jsx":"d5075c377372","components/actions/DarkButton.jsx":"f6125e05fa69","components/actions/DecisionsStrip.jsx":"4790583f54eb","components/actions/DownloadAction.jsx":"38be38b8f188","components/actions/InfoDot.jsx":"aa2a7e6d23d3","components/actions/InlineActionRow.jsx":"fb7ed136b489","components/actions/Pill.jsx":"f4e6f3c917b8","components/actions/Pressable.jsx":"ba0e0f3f762d","components/actions/RangePills.jsx":"db7caa481086","components/actions/StickyCTA.jsx":"19e321cf85bf","components/actions/SuggestionRow.jsx":"5b85a097782b","components/cards/AllocationCard.jsx":"2f47a0289c3d","components/cards/ArtifactCard.jsx":"07f7473bc36d","components/cards/Badge.jsx":"58ae8d064a7f","components/cards/ConstraintCallout.jsx":"356b06643ab8","components/cards/DataTableCard.jsx":"63d3810da7b1","components/cards/DisclosureBlock.jsx":"3bcafa7504da","components/cards/ExplainerSheet.jsx":"4b15c8797a4e","components/cards/HeroNumberCard.jsx":"17fcd49b2b37","components/cards/InfoCard.jsx":"9a4d40b4712c","components/cards/MoveCard.jsx":"229b671c325f","components/cards/RejectCallout.jsx":"7e85d7aa99a8","components/chat/DetourBanner.jsx":"d9e8733c4270","components/chat/GreetingDivider.jsx":"d29e92d61b06","components/chat/MessageActions.jsx":"b7bc704b8b92","components/chat/ParseNote.jsx":"33700790af38","components/chat/ProgressTrace.jsx":"84103dbb908d","components/chat/QAPair.jsx":"f8578957e43d","components/chat/ResponseFeedback.jsx":"3f4ee4e57b1e","components/chat/SentinelBlock.jsx":"db553166a61e","components/chat/SentinelText.jsx":"4af7f8050faf","components/chat/SentinelThinking.jsx":"c6e6dcb163c8","components/chat/StepTrace.jsx":"40c51de1327c","components/chat/UserBubble.jsx":"52ca1e3a7f2c","components/chat/VersionRow.jsx":"1835daece095","components/composer/Composer.jsx":"e571dbb06db8","components/composer/MoneyComposer.jsx":"fa67e6d2a7c0","components/data/AttributionChart.jsx":"e839223720af","components/data/ChartBar.jsx":"3d3cc7cdd120","components/data/ChartLegend.jsx":"60bfc0b85ff7","components/data/ChartLine.jsx":"1c556cd17948","components/data/ChartReadout.jsx":"01c7f4839293","components/data/ChartShare.jsx":"d8426a5c6202","components/data/ChartTooltip.jsx":"600f111de100","components/data/ConcentrationBar.jsx":"19c3162e4097","components/data/DrawnCheck.jsx":"fa3efd847beb","components/data/Dumbbell.jsx":"44143af310af","components/data/ProgressRail.jsx":"e707f5c4f9f7","components/data/StatTile.jsx":"d43697d16538","components/data/chartMath.jsx":"5ff7f2f099a2","components/forms/FileUpload.jsx":"7bf2028d7ee6","components/forms/SearchField.jsx":"38191d422f8c","components/forms/SelectionMark.jsx":"baf77c7fd137","components/icons/IconArrow.jsx":"605258d69b59","components/icons/IconAttach.jsx":"d94edd1279b2","components/icons/IconCheckCircle.jsx":"ee690aa679f1","components/icons/IconChevronRight.jsx":"b531b3a05ef2","components/icons/IconDownload.jsx":"17b3067dcb2f","components/icons/IconFile.jsx":"3ff379e7d7f4","components/icons/IconInfo.jsx":"9cc7d9349507","components/icons/IconMenu.jsx":"f7f33a215bc2","components/icons/IconPlus.jsx":"e19a5642aae5","components/icons/IconSparkle.jsx":"a6f87a6ecf4d","components/icons/IconSpinner.jsx":"ca8db75e4dad","components/lists/FollowUpRow.jsx":"30e6342ec014","components/lists/List.jsx":"9d573d9c8885","components/lists/ListRow.jsx":"fa7453908b8d","components/shell/CanvasHeader.jsx":"b9c78f317c8a","components/shell/Dock.jsx":"33a1660d9b6c","components/shell/HomeIndicator.jsx":"9f449d92c75b","components/shell/MotionGuard.jsx":"00c8339a8ce9","components/shell/PhoneFrame.jsx":"67f9f8421c9a","components/shell/ScreenBackdrop.jsx":"4f47b1cb6928","components/shell/ScrollToBottomButton.jsx":"47ff8908555b","components/shell/StatusSpacer.jsx":"a9d5071ed1bf","components/shell/TopBar.jsx":"4fc432f7e830","components/text/DemoFooter.jsx":"93348d57791f","components/text/Eyebrow.jsx":"d5e56b885203","components/text/EyebrowDivider.jsx":"2152bc6e4a57","components/text/Provenance.jsx":"0998967a0ecf","components/text/StandingDisclosure.jsx":"f3c4a5358a15","pages/page-kit.jsx":"7ba5fa9402bb","scripts/build-index.js":"bcb214287e08","ui_kits/journey-a-risk/a-data.jsx":"b7c6b602789a","ui_kits/journey-a-risk/a-screens.jsx":"33120ad71ca6","ui_kits/journey-b/b-board.jsx":"88902ab7c381","ui_kits/journey-b/b-data.jsx":"cc9a371711e7","ui_kits/journey-b/b-parts.jsx":"4fb488d66f3e","ui_kits/journey-b/b-screens.jsx":"005d4041f03b","ui_kits/sentinel-app/app.jsx":"ed76750a9f4a","ui_kits/sentinel-app/chat.jsx":"314f8ef93b8d","ui_kits/sentinel-app/data.jsx":"dfc7d07313d9","ui_kits/sentinel-app/drawer.jsx":"b7f5c35191fa","ui_kits/sentinel-app/home.jsx":"47c36034666f","ui_kits/sentinel-app/journey.jsx":"730ef3ceabab"},"inlinedExternals":[],"unexposedExports":[{"name":"areaPath","sourcePath":"components/data/chartMath.jsx"},{"name":"band","sourcePath":"components/data/chartMath.jsx"},{"name":"buildIndex","sourcePath":"scripts/build-index.js"},{"name":"formatINR","sourcePath":"components/composer/MoneyComposer.jsx"},{"name":"hasD3","sourcePath":"components/data/chartMath.jsx"},{"name":"installReducedMotion","sourcePath":"components/shell/MotionGuard.jsx"},{"name":"linePath","sourcePath":"components/data/chartMath.jsx"},{"name":"linear","sourcePath":"components/data/chartMath.jsx"},{"name":"markColor","sourcePath":"components/data/chartMath.jsx"},{"name":"niceDomain","sourcePath":"components/data/chartMath.jsx"},{"name":"tabular","sourcePath":"components/data/chartMath.jsx"},{"name":"ticks","sourcePath":"components/data/chartMath.jsx"},{"name":"toneColor","sourcePath":"components/data/chartMath.jsx"},{"name":"trackInset","sourcePath":"components/data/chartMath.jsx"},{"name":"useCountUp","sourcePath":"components/cards/HeroNumberCard.jsx"}]} */

(() => {

const __ds_ns = (window.SentinelDesignSystem_0682a2 = window.SentinelDesignSystem_0682a2 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/actions/ChipRow.jsx
try { (() => {
/* C6 · wrapping chip row, 8px gap, staggered entrance. */
function ChipRow({
  children,
  animate = true
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: '100%',
      flexWrap: 'wrap',
      gap: 'var(--space-8)'
    }
  }, React.Children.map(children, (c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: animate ? {
      animation: `ds-rise var(--dur-enter) var(--ease) both`,
      animationDelay: `${i * 60}ms`
    } : undefined
  }, c)));
}
Object.assign(__ds_scope, { ChipRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ChipRow.jsx", error: String((e && e.message) || e) }); }

// components/actions/ClientChip.jsx
try { (() => {
/* The bound client, living in the composer. Tapping a client anywhere — the drawer's CLIENTS section,
   a picker sheet — does not navigate: it binds the next thread to that client and puts this chip in the
   composer. One mechanism, two entry points, and typing a name resolves to the same bound state.
   Removable by definition: a binding the advisor cannot undo in place is a trap. The ✕ carries a 44pt
   target even though the chip itself is 28px tall. */
function ClientChip({
  name,
  initial,
  onRemove,
  disabled = false
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      height: 28,
      maxWidth: 200,
      alignItems: 'center',
      gap: 'var(--space-6)',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-chip)',
      boxShadow: '0 0 0 1px var(--color-line)',
      padding: onRemove ? '0 2px 0 4px' : '0 10px 0 4px',
      boxSizing: 'border-box',
      opacity: disabled ? 0.4 : 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      width: 20,
      height: 20,
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)',
      background: 'var(--surface-avatar)',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-10)',
      color: 'var(--color-bronze-deep)'
    }
  }, initial || (name || '?').trim().charAt(0).toUpperCase()), /*#__PURE__*/React.createElement("span", {
    style: {
      minWidth: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-ink)'
    }
  }, name), onRemove && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onRemove,
    disabled: disabled,
    "aria-label": `Unbind ${name}`,
    style: {
      appearance: 'none',
      border: 'none',
      background: 'transparent',
      cursor: disabled ? 'default' : 'pointer',
      display: 'flex',
      width: 44,
      height: 44,
      margin: '0 -12px 0 -4px',
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: 'center',
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "10",
    height: "10",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-muted)",
    strokeWidth: "2.4",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  }))));
}
Object.assign(__ds_scope, { ClientChip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ClientChip.jsx", error: String((e && e.message) || e) }); }

// components/actions/DecisionsStrip.jsx
try { (() => {
/* §1.4 · read-only strip of settled facts, separated by 14px hairlines. */
function DecisionsStrip({
  tokens
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: '100%',
      alignItems: 'center',
      gap: 'var(--space-8)',
      overflowX: 'auto',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      padding: '10px 12px',
      boxShadow: '0 0 0 1px var(--color-line)',
      scrollbarWidth: 'none',
      boxSizing: 'border-box'
    }
  }, tokens.map((t, i) => /*#__PURE__*/React.createElement("div", {
    key: t.label,
    style: {
      display: 'flex',
      flexShrink: 0,
      alignItems: 'center',
      gap: 'var(--space-8)'
    }
  }, i > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      height: 14,
      width: 1,
      background: 'var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: t.onClick,
    style: {
      appearance: 'none',
      border: 'none',
      cursor: 'pointer',
      flexShrink: 0,
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-chip)',
      padding: '5px 10px',
      boxShadow: '0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-bronze-deep)'
    }
  }, t.label)))));
}
Object.assign(__ds_scope, { DecisionsStrip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/DecisionsStrip.jsx", error: String((e && e.message) || e) }); }

// components/actions/Pill.jsx
try { (() => {
/* The one tappable pill. Two sizes, no third. Visual height 36 (md) / 32 (sm); the tap target is always ≥44
   via a transparent pseudo-element that extends the hit area vertically — callers cannot get it wrong. */
const SIZES = {
  md: {
    h: 36,
    px: 13,
    font: 12,
    lh: 18
  },
  sm: {
    h: 32,
    px: 12,
    font: 12,
    lh: 16
  }
};
const TONES = {
  outline: {
    bg: 'var(--color-chip)',
    ring: 'var(--color-bubble-edge)',
    fg: 'var(--color-bronze-deep)'
  },
  smart: {
    bg: 'var(--color-bubble)',
    ring: 'var(--color-bubble-edge)',
    fg: 'var(--color-bronze-deep)'
  },
  tertiary: {
    bg: 'transparent',
    ring: null,
    dashed: true,
    fg: 'var(--color-bronze-deep)'
  },
  muted: {
    bg: 'var(--color-chip)',
    ring: 'var(--color-bubble-edge)',
    fg: 'var(--color-bronze-deep)',
    fgOpacity: 0.6
  },
  primary: {
    bg: 'var(--color-selected)',
    ring: 'var(--color-bubble-edge)',
    fg: 'var(--color-ink)'
  },
  filter: {
    bg: 'var(--color-chip)',
    ring: 'var(--color-line)',
    fg: 'var(--color-bronze-deep)'
  }
};
const SELECTED = {
  bg: 'var(--color-selected)',
  ring: 'var(--color-bronze)',
  fg: 'var(--color-ink)'
};
function Glyph({
  kind
}) {
  if (kind === 'check') return /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 13 13",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M2.5 6.8 5 9.3l5.5-5.6",
    stroke: "var(--color-ink)",
    strokeWidth: "1.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }));
  if (kind === 'tertiary') return /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-13)',
      lineHeight: 1,
      color: 'var(--color-bronze-deep)'
    }
  }, "?");
  if (kind === 'smart') return /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 13 13",
    fill: "none"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "1",
    y: "2.5",
    width: "11",
    height: "8",
    rx: "1.5",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.1"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M3.2 5.2h2.2M3.2 7.2h4.4",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.1",
    strokeLinecap: "round"
  }));
  return null;
}
/* Loading: the label HOLDS — the pill never becomes a spinner with no words, and it never changes width
   mid-press, which would move the thing under the finger. The glyph slot carries a 13px bronze spinner
   and the label drops to 60%; the press target and the width stay exactly as they were. Inert while
   loading, but not dimmed like `disabled`: a pill waiting on the network is working, not unavailable. */
function Spinner({
  fg
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      width: 13,
      height: 13,
      flexShrink: 0,
      animation: 'ds-spin 900ms linear infinite'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: fg,
    strokeWidth: "2.6",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M21 12a9 9 0 1 1-6.219-8.56"
  })));
}
function Pill({
  label,
  size = 'md',
  tone = 'outline',
  selected = false,
  onClick,
  disabled = false,
  loading = false
}) {
  const [down, setDown] = React.useState(false);
  const s = SIZES[size],
    t = selected ? SELECTED : TONES[tone];
  const hit = Math.max(0, (44 - s.h) / 2);
  const inert = disabled || loading;
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: inert ? undefined : onClick,
    disabled: inert,
    className: "ds-pill",
    "aria-busy": loading || undefined,
    onPointerDown: () => !inert && setDown(true),
    onPointerUp: () => setDown(false),
    onPointerLeave: () => setDown(false),
    style: {
      position: 'relative',
      appearance: 'none',
      border: 'none',
      cursor: inert ? 'default' : 'pointer',
      display: 'inline-flex',
      height: s.h,
      flexShrink: 0,
      alignItems: 'center',
      gap: 'var(--space-6)',
      borderRadius: 'var(--radius-full)',
      padding: `0 ${s.px}px`,
      background: t.bg,
      boxShadow: t.ring ? `0 0 0 1px ${t.ring}` : 'none',
      outline: t.dashed ? '1px dashed var(--tint-bronze-dashed)' : 'none',
      outlineOffset: -1,
      opacity: disabled ? 0.4 : 1,
      transform: down ? 'scale(0.98)' : 'none',
      transition: 'transform var(--dur-press) var(--ease), background-color var(--dur-press)',
      '--hit': `${hit}px`
    }
  }, /*#__PURE__*/React.createElement("style", null, '.ds-pill::before{content:"";position:absolute;left:0;right:0;top:calc(-1 * var(--hit));bottom:calc(-1 * var(--hit))}'), loading ? /*#__PURE__*/React.createElement(Spinner, {
    fg: t.fg
  }) : selected ? /*#__PURE__*/React.createElement(Glyph, {
    kind: "check"
  }) : /*#__PURE__*/React.createElement(Glyph, {
    kind: tone
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: s.font,
      lineHeight: `${s.lh}px`,
      color: t.fg,
      opacity: loading ? 0.6 : t.fgOpacity || 1
    }
  }, label));
}
Object.assign(__ds_scope, { Pill });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/Pill.jsx", error: String((e && e.message) || e) }); }

// components/actions/AnswerChip.jsx
try { (() => {
/* C2–C5 · answer chip.
   Without a subtitle it IS a <Pill size="md"> — kept as an alias because the journey vocabulary (src/journeys.tsx)
   speaks in AnswerChip variants. With a subtitle it becomes the full-width card chip, which is not a pill and is
   used only where the wording changes the meaning of the answer ("A monthly SIP instead — spread over months").
   Variant → Pill tone mapping is 1:1: outline · smart · tertiary · muted · primary. */
function Check() {
  return /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 13 13",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M2.5 6.8 5 9.3l5.5-5.6",
    stroke: "var(--color-ink)",
    strokeWidth: "1.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }));
}
function CardChip({
  label,
  subtitle,
  selected,
  onClick
}) {
  const [down, setDown] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    onPointerDown: () => setDown(true),
    onPointerUp: () => setDown(false),
    onPointerLeave: () => setDown(false),
    style: {
      appearance: 'none',
      border: 'none',
      cursor: 'pointer',
      display: 'flex',
      width: '100%',
      flexDirection: 'column',
      alignItems: 'flex-start',
      gap: 'var(--space-3)',
      borderRadius: 'var(--radius-16)',
      padding: '11px 14px',
      textAlign: 'left',
      background: selected ? 'var(--color-selected)' : 'var(--color-bubble)',
      boxShadow: `0 0 0 1px ${selected ? 'var(--color-bronze)' : 'var(--color-bubble-edge)'}`,
      transform: down ? 'scale(0.98)' : 'none',
      transition: 'transform var(--dur-press) var(--ease), background-color var(--dur-press)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-6)',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-19)',
      color: 'var(--color-ink)'
    }
  }, label, selected && /*#__PURE__*/React.createElement(Check, null)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-13)',
      lineHeight: 'var(--leading-18)',
      color: 'var(--color-muted)'
    }
  }, subtitle));
}
function AnswerChip({
  label,
  subtitle,
  variant = 'outline',
  selected = false,
  onClick
}) {
  if (subtitle) return /*#__PURE__*/React.createElement(CardChip, {
    label: label,
    subtitle: subtitle,
    selected: selected,
    onClick: onClick
  });
  return /*#__PURE__*/React.createElement(__ds_scope.Pill, {
    label: label,
    size: "md",
    tone: variant,
    selected: selected,
    onClick: onClick
  });
}
Object.assign(__ds_scope, { AnswerChip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/AnswerChip.jsx", error: String((e && e.message) || e) }); }

// components/actions/DownloadAction.jsx
try { (() => {
/* A thin wrapper over Pill, and deliberately thin — it exists so that "download" is one decision in
   one place rather than a loading flag wired by hand at every call site. It was blocked on Pill
   gaining `loading`; that landed in v10, so this is now four lines of real behaviour.

   The label holds through the wait (Pill's rule: the width never changes, so the target under the
   finger never moves), and confirmation happens ON the control — "Downloaded" in place, for two
   seconds — because there are no toasts in this product. Failure says what to do next rather than
   what went wrong internally. */
function DownloadAction({
  label,
  format = 'PDF',
  onDownload,
  size = 'md',
  tone = 'outline',
  disabled = false
}) {
  const [state, setState] = React.useState('idle');
  const text = label || `Download ${format}`;
  const run = async () => {
    setState('working');
    try {
      await (onDownload ? onDownload(format) : Promise.resolve());
      setState('done');
      setTimeout(() => setState('idle'), 2000);
    } catch (e) {
      setState('failed');
    }
  };
  const shown = state === 'done' ? `Saved to Files` : state === 'failed' ? 'Try again' : text;
  return /*#__PURE__*/React.createElement(__ds_scope.Pill, {
    label: shown,
    size: size,
    tone: state === 'done' ? 'smart' : tone,
    loading: state === 'working',
    disabled: disabled,
    onClick: state === 'working' ? undefined : run,
    selected: state === 'done'
  });
}
Object.assign(__ds_scope, { DownloadAction });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/DownloadAction.jsx", error: String((e && e.message) || e) }); }

// components/actions/InlineActionRow.jsx
try { (() => {
/* Not a card — no background, no radius, no padding. A flex-wrap row of Pills that follows a Sentinel reply.
   Pill owns the press state; the row owns the entrance (fade + rise, 60ms stagger — the same rhythm as ChipRow). */
function InlineActionRow({
  actions,
  animate = true
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'var(--space-8)'
    }
  }, actions.map((a, i) => /*#__PURE__*/React.createElement("div", {
    key: a.label,
    style: animate ? {
      animation: 'ds-rise var(--dur-enter) var(--ease) both',
      animationDelay: `${i * 60}ms`
    } : undefined
  }, /*#__PURE__*/React.createElement(__ds_scope.Pill, {
    label: a.label,
    size: "md",
    tone: a.tone || 'outline',
    onClick: a.onClick
  }))));
}
Object.assign(__ds_scope, { InlineActionRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/InlineActionRow.jsx", error: String((e && e.message) || e) }); }

// components/actions/Pressable.jsx
try { (() => {
/* Generic pressable with the calm press micro-state: scale 0.98 over 150ms.

   v11 · THE BASE CARRIES BOTH OPERABILITY GUARANTEES, so thirty component files inherit them without
   being edited — the instinct Pill already had, moved to where it belongs: callers cannot get it wrong.

   1 · Hit expansion. A visual box shorter than 44pt gets a transparent ::before that extends the
   target to 44 vertically (and horizontally when the box is narrow). Measured from the rendered box,
   not declared, so a 28px chip and a 42px disc both answer to a 44pt touch.

   2 · Focus. This component used to set `outline: none` with nothing in its place, which is worse than
   an omission — it switched the browser's own indicator off for everything beneath it. It now paints a
   2px solid --border-focus OUTLINE plus the --focus-ring halo, on :focus-visible only, so a mouse
   press stays quiet and a keyboard or switch-control user can see where they are.

   Why an outline and not a box-shadow: the first build used box-shadow, and an inline style.boxShadow
   from a caller beats a stylesheet declaration every time — measured, a chip passing its own 1px ring
   returned "rgb(225,222,218) 0 0 0 1px" on focus and no ring at all. Four callers did exactly that
   (RangePills, ResponseFeedback, CanvasHeader, ScrollToBottomButton), so the guarantee held for most
   components and failed silently for the rest, which is the one failure mode a base may not have.
   `outline` is never passed inline by a caller here, so it cannot be overridden by accident. Callers
   may pass boxShadow freely; they must not pass outline.

   Both rules live in tokens/effects.css, not in a <style> here: a <style> child of <button> is an
   invalid content model and duplicated once per instance (53 copies on the RangePills page). They are
   global and identical for every instance, so the stylesheet is where they belong. MotionGuard also
   installs them from the bundle, so a consuming project that skips the stylesheet still gets them.

   Why solid and not just the halo: the halo is bronze at 24% alpha, which composites to #e7ddd4 and
   measures 1.22:1 against canvas — it fails WCAG 2.4.13's 3.0 floor on its own. The solid outline is
   --color-bronze-deep at 6.59 / 7.24. Both tokens already existed; only their use is new. */
function Pressable({
  children,
  onClick,
  style,
  disabled = false,
  pressScale = 0.98,
  label,
  role,
  expand = 'auto',
  pressed,
  expanded,
  controls,
  tabIndex
}) {
  const [down, setDown] = React.useState(false);
  const ref = React.useRef(null);
  const [pad, setPad] = React.useState({
    y: 0,
    x: 0
  });
  React.useEffect(() => {
    if (expand === 'none' || !ref.current) return;
    const box = ref.current.getBoundingClientRect();
    if (!box.height) return;
    setPad({
      y: Math.max(0, (44 - box.height) / 2),
      x: Math.max(0, Math.min(12, (44 - box.width) / 2))
    });
  }, [expand, children]);
  const needsPad = pad.y > 0 || pad.x > 0;
  return /*#__PURE__*/React.createElement("button", {
    ref: ref,
    type: "button",
    onClick: onClick,
    disabled: disabled,
    className: "ds-pressable",
    "data-hit": needsPad ? '' : undefined,
    "aria-label": label,
    role: role,
    "aria-pressed": pressed,
    "aria-expanded": expanded,
    "aria-controls": controls,
    tabIndex: tabIndex,
    onPointerDown: () => !disabled && setDown(true),
    onPointerUp: () => setDown(false),
    onPointerLeave: () => setDown(false),
    style: {
      position: 'relative',
      appearance: 'none',
      border: 'none',
      background: 'transparent',
      padding: 0,
      margin: 0,
      cursor: disabled ? 'default' : 'pointer',
      color: 'inherit',
      font: 'inherit',
      textAlign: 'inherit',
      opacity: disabled ? 0.4 : 1,
      transform: down ? `scale(${pressScale})` : 'none',
      transition: 'transform var(--dur-press) var(--ease), background-color var(--dur-press) var(--ease)',
      '--hit-y': `${pad.y}px`,
      '--hit-x': `${pad.x}px`,
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Pressable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/Pressable.jsx", error: String((e && e.message) || e) }); }

// components/actions/RangePills.jsx
try { (() => {
/* The time range for a performance series. Shopee's fund detail carries `1M 3M YTD 1Y 3Y 5Y ALL`
   under the figure; our chart family had no range control at all, which for any performance series is
   not optional — a number with no period attached is not a number an advisor can defend.

   Sentinel's own language: these are Pill's filter tone at 32px in a single row, selected carrying the
   sand fill and a bronze ring, which is how selection already reads in this system. Not tabs: tabs
   change what you are looking at, a range changes the window on the same thing, and the selected pill
   is the existing vocabulary for "one of a set is on".

   Roles: role="tablist"/"tab" would promise arrow-key navigation between panels; this is a single-
   select filter over one panel, so it is a group of aria-pressed buttons. Seven ranges at 32px + gaps
   exceeds 343, so the row scrolls horizontally — permitted, because horizontal inside vertical is
   orthogonal (the no-nested-scroll law is about vertical inside vertical). */
const DEFAULT = ['1M', '3M', '1Y', '3Y', 'ALL'];
function RangePills({
  ranges = DEFAULT,
  value,
  onChange,
  label = 'Time range',
  locked = false,
  lockedNote
}) {
  const active = value || ranges[ranges.length - 1];
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    role: "group",
    "aria-label": label,
    className: "noscroll",
    style: {
      display: 'flex',
      gap: 'var(--space-6)',
      width: '100%',
      overflowX: 'auto',
      paddingBottom: 'var(--space-2)'
    }
  }, ranges.map(r => {
    const on = r === active;
    return /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
      key: r,
      onClick: locked ? undefined : () => onChange && onChange(r),
      disabled: locked,
      pressed: on,
      label: `${label}: ${r}`,
      style: {
        display: 'inline-flex',
        height: 'var(--h-filter-chip)',
        flexShrink: 0,
        alignItems: 'center',
        justifyContent: 'center',
        padding: `0 var(--space-12)`,
        borderRadius: 'var(--radius-full)',
        background: on ? 'var(--color-selected)' : 'var(--color-chip)',
        boxShadow: on ? `0 0 0 var(--border-1) var(--color-bronze)` : `0 0 0 var(--border-1) var(--color-line)`
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        font: 'var(--type-label-font)',
        letterSpacing: 'var(--tracking-pill)',
        color: on ? 'var(--color-ink)' : 'var(--color-bronze-deep)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, r));
  })), locked && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: `var(--space-6) 0 0`,
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, lockedNote || 'Ranges unavailable until the performance source is confirmed.'));
}
Object.assign(__ds_scope, { RangePills });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/RangePills.jsx", error: String((e && e.message) || e) }); }

// components/cards/AllocationCard.jsx
try { (() => {
/* Allocation card: label/value rows (42px) + a 6px stacked bar. Bars grow with scaleX, never width. */
function AllocationCard({
  segments,
  animate = true
}) {
  const total = segments.reduce((s, x) => s + x.value, 0) || 100;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxSizing: 'border-box',
      padding: '2px 12px'
    }
  }, segments.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: s.label,
    style: {
      display: 'flex',
      height: 42,
      alignItems: 'center',
      justifyContent: 'space-between',
      borderBottom: i < segments.length - 1 ? '0.5px solid var(--color-line-soft)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink)'
    }
  }, s.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink)'
    }
  }, s.value, "%"))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '10px 0 12px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: 6,
      width: '100%',
      overflow: 'hidden',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-track)'
    }
  }, segments.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: s.label,
    style: {
      height: '100%',
      width: `${s.value / total * 100}%`,
      background: s.color,
      transformOrigin: 'left',
      animation: animate ? `ds-grow var(--dur-bar) ease-in-out both` : 'none',
      animationDelay: `${i * 120}ms`
    }
  })))));
}
Object.assign(__ds_scope, { AllocationCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/AllocationCard.jsx", error: String((e && e.message) || e) }); }

// components/cards/Badge.jsx
try { (() => {
/* Never tappable. One size, one padding (8 × 3). No onClick prop exists. */
const STATUS = {
  over: {
    bg: 'var(--color-status-over-bg)',
    fg: 'var(--color-status-over-fg)'
  },
  under: {
    bg: 'var(--color-status-under-bg)',
    fg: 'var(--color-status-under-fg)'
  },
  ok: {
    bg: 'var(--color-status-ok-bg)',
    fg: 'var(--color-status-ok-fg)'
  }
};
function Badge({
  children,
  variant = 'meta',
  tone = 'under'
}) {
  const base = {
    display: 'inline-flex',
    alignItems: 'center',
    borderRadius: 'var(--radius-full)',
    padding: '3px 8px',
    whiteSpace: 'nowrap',
    fontFamily: 'var(--font-ui)'
  };
  if (variant === 'status') {
    const t = STATUS[tone];
    return /*#__PURE__*/React.createElement("span", {
      style: {
        ...base,
        background: t.bg,
        color: t.fg,
        fontWeight: 'var(--weight-bold)',
        fontSize: 'var(--text-10)',
        textTransform: 'uppercase',
        letterSpacing: '0.04em'
      }
    }, children);
  }
  return /*#__PURE__*/React.createElement("span", {
    style: {
      ...base,
      background: 'var(--color-chip)',
      boxShadow: '0 0 0 1px var(--color-line)',
      color: 'var(--color-muted)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-11)'
    }
  }, children);
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/Badge.jsx", error: String((e && e.message) || e) }); }

// components/cards/ExplainerSheet.jsx
try { (() => {
/* C20 · explainer bottom sheet — 24px top radius, 44×5 grabber, "Got it" primary chip. */
function ExplainerSheet({
  open,
  title,
  body,
  onClose
}) {
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    onClick: onClose,
    style: {
      position: 'absolute',
      inset: 0,
      zIndex: 20,
      background: 'var(--scrim)',
      opacity: 0.4,
      animation: 'ds-fade 300ms var(--ease) both'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      zIndex: 30,
      borderRadius: '24px 24px 0 0',
      background: 'var(--color-surface)',
      padding: '12px 20px 28px',
      animation: 'ds-sheet 300ms var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes ds-sheet{from{transform:translateY(100%)}to{transform:none}}@media (prefers-reduced-motion:reduce){@keyframes ds-sheet{from{opacity:0;transform:none}to{opacity:1;transform:none}}}'), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '0 auto 14px',
      height: 5,
      width: 44,
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      ...{
        font: 'var(--type-sheet-title-font)',
        color: 'var(--color-ink)'
      }
    }
  }, title), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-10)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-10)'
    }
  }, body.map((p, i) => /*#__PURE__*/React.createElement("p", {
    key: i,
    style: {
      margin: 0,
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-regular)',
        fontSize: 'var(--text-14)',
        lineHeight: 'var(--leading-20)',
        color: 'var(--color-ink-soft)'
      }
    }
  }, p))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 18
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Pill, {
    label: "Got it",
    tone: "primary",
    onClick: onClose
  }))));
}
Object.assign(__ds_scope, { ExplainerSheet });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/ExplainerSheet.jsx", error: String((e && e.message) || e) }); }

// components/cards/HeroNumberCard.jsx
try { (() => {
function useCountUp(target, ms, run = true) {
  const [v, setV] = React.useState(0);
  React.useEffect(() => {
    if (!run) return;
    if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
      setV(target);
      return;
    }
    let raf = 0;
    const start = performance.now();
    const tick = t => {
      const p = Math.min(1, (t - start) / ms);
      setV(Math.round((1 - Math.pow(1 - p, 3)) * target));
      if (p < 1) raf = requestAnimationFrame(tick);
    };
    raf = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(raf);
  }, [target, ms, run]);
  return v;
}
/* C10 · hero number card with the lowest-of-three meter.
   All three rows draw a hairline track and a fill — the card's argument is a comparison of three
   figures where the lowest binds, and an undrawn row makes the comparison unreadable. The binding
   row is distinguished by bronze fill + heavier label, never by being the only one drawn. */
function HeroNumberCard({
  title,
  meta,
  value,
  badge,
  copy,
  rows
}) {
  const n = useCountUp(value, 600, true);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-card)',
      boxSizing: 'border-box',
      padding: 'var(--space-16)',
      animation: 'ds-rise var(--dur-screen) var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-medium)',
        fontSize: 'var(--text-16)',
        lineHeight: 'var(--leading-24)',
        color: 'var(--color-ink)'
      }
    }
  }, title), /*#__PURE__*/React.createElement(__ds_scope.Badge, null, meta)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-8)',
      display: 'flex',
      alignItems: 'flex-end',
      gap: 'var(--space-12)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-display-font)',
      color: 'var(--color-bronze-deep)'
    }
  }, n), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 'var(--space-10)',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-selected)',
      padding: '4px 12px',
      boxShadow: '0 0 0 1px var(--color-bubble-edge)',
      animation: 'ds-fade 300ms var(--ease) both',
      animationDelay: '600ms'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-13)',
      color: 'var(--color-ink)'
    }
  }, badge))), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '6px 0 0',
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-regular)',
        fontSize: 'var(--text-14)',
        lineHeight: 'var(--leading-20)',
        color: 'var(--color-ink-soft)'
      }
    }
  }, copy), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-14)',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-12)'
    }
  }, rows.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: r.label
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 'var(--space-5)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: r.binding ? 700 : 500,
      fontSize: 'var(--text-13)',
      lineHeight: 'var(--leading-18)',
      color: r.binding ? 'var(--color-ink)' : 'var(--color-muted)'
    }
  }, r.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: r.binding ? 700 : 500,
      fontSize: 'var(--text-14)',
      color: r.binding ? 'var(--color-bronze-deep)' : 'var(--color-ink)'
    }
  }, r.value)), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 6,
      width: '100%',
      overflow: 'hidden',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-track)',
      boxShadow: 'inset 0 0 0 0.5px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: `${r.value}%`,
      borderRadius: 'var(--radius-full)',
      background: r.binding ? 'var(--color-bronze)' : 'var(--color-alloc-debt)',
      transformOrigin: 'left',
      animation: 'ds-grow 500ms var(--ease) both',
      animationDelay: `${600 + i * 120}ms`
    }
  }))))));
}
Object.assign(__ds_scope, { useCountUp, HeroNumberCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/HeroNumberCard.jsx", error: String((e && e.message) || e) }); }

// components/cards/MoveCard.jsx
try { (() => {
/* C16 · numbered move card — 24px Darker Grotesque numeral. */
function MoveCard({
  n,
  title,
  body
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 'var(--space-12)',
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-card)',
      boxSizing: 'border-box',
      padding: '13px 14px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-display)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 24,
      lineHeight: 1,
      color: 'var(--color-bronze-deep)'
    }
  }, n), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-medium)',
        fontSize: 'var(--text-15)',
        lineHeight: 'var(--leading-22)',
        color: 'var(--color-ink)'
      }
    }
  }, title), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '3px 0 0',
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-regular)',
        fontSize: 'var(--text-13)',
        lineHeight: 'var(--leading-18)',
        color: 'var(--color-muted)'
      }
    }
  }, body)));
}
Object.assign(__ds_scope, { MoveCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/MoveCard.jsx", error: String((e && e.message) || e) }); }

// components/chat/DetourBanner.jsx
try { (() => {
/* A7 · pinned below the header while a journey is paused. */
function DetourBanner({
  label,
  onResume
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 10,
      margin: '0 16px',
      display: 'flex',
      height: 32,
      alignItems: 'center',
      justifyContent: 'space-between',
      borderRadius: 'var(--radius-10)',
      background: 'var(--color-chip)',
      padding: '0 12px',
      boxShadow: '0 0 0 1px var(--color-line)',
      animation: 'ds-rise var(--dur-enter) var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-muted)'
    }
  }, label), /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onResume,
    style: {
      borderRadius: 'var(--radius-full)',
      padding: '0 6px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-bronze-deep)'
    }
  }, "Resume")));
}
Object.assign(__ds_scope, { DetourBanner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/DetourBanner.jsx", error: String((e && e.message) || e) }); }

// components/chat/GreetingDivider.jsx
try { (() => {
/* Home greeting: dashed hairlines either side of a 27px Darker Grotesque line. */
function GreetingDivider({
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: '100%',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 'var(--space-8)',
      padding: '8px 16px',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 0,
      flex: 1,
      borderTop: '1px dashed var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      whiteSpace: 'nowrap',
      textAlign: 'center',
      font: 'var(--type-greeting-font)',
      color: 'var(--color-bronze-deep)'
    }
  }, children), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 0,
      flex: 1,
      borderTop: '1px dashed var(--color-line)'
    }
  }));
}
Object.assign(__ds_scope, { GreetingDivider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/GreetingDivider.jsx", error: String((e && e.message) || e) }); }

// components/chat/MessageActions.jsx
try { (() => {
/* Per-message actions — the LAST message only. At 375pt, actions under every turn are noise.
   Text only at 11.5px muted, never pills: this system is text-forward and a row of ghost pills here
   would compete with the chip row directly below it. Tap targets are 44px through vertical padding;
   the row pulls that padding back out so the optical spacing stays tight under the bubble. */
const LABELS = {
  copy: 'Copy',
  edit: 'Edit',
  retry: 'Retry'
};
function MessageActions({
  role = 'assistant',
  actions,
  onAction
}) {
  const list = actions && actions.length ? actions : role === 'user' ? ['edit'] : ['copy', 'retry'];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 'var(--space-16)',
      justifyContent: role === 'user' ? 'flex-end' : 'flex-start',
      margin: '-8px 0 -10px'
    }
  }, list.map(a => /*#__PURE__*/React.createElement("button", {
    key: a,
    type: "button",
    onClick: () => onAction && onAction(a),
    style: {
      appearance: 'none',
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      display: 'flex',
      alignItems: 'center',
      minHeight: 44,
      padding: '12px 2px',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 11.5,
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, LABELS[a] || a)));
}
Object.assign(__ds_scope, { MessageActions });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/MessageActions.jsx", error: String((e && e.message) || e) }); }

// components/chat/ParseNote.jsx
try { (() => {
/* A6 · 12px grey note under the user bubble ("understood as ₹1,80,000"). */
function ParseNote({
  text
}) {
  return /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      width: '100%',
      paddingRight: 'var(--space-2)',
      textAlign: 'right',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)',
      animation: 'ds-fade 300ms var(--ease) both'
    }
  }, text);
}
Object.assign(__ds_scope, { ParseNote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/ParseNote.jsx", error: String((e && e.message) || e) }); }

// components/chat/QAPair.jsx
try { (() => {
/* C7 · collapsed question (muted, truncated) + the answer bubble (13px). */
function QAPair({
  question,
  answer
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: '100%',
      flexDirection: 'column',
      gap: 'var(--space-6)'
    }
  }, question && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, question), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: '100%',
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 280,
      borderRadius: '20px 20px 6px 20px',
      background: 'var(--color-bubble)',
      padding: '8px 12px',
      boxShadow: '0 0 0 1px var(--color-bubble-edge)'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-13)',
      lineHeight: 'var(--leading-18)',
      color: 'var(--color-ink)'
    }
  }, answer))));
}
Object.assign(__ds_scope, { QAPair });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/QAPair.jsx", error: String((e && e.message) || e) }); }

// components/chat/ResponseFeedback.jsx
try { (() => {
/* Marking an answer wrong. Sentinel had nothing, anywhere — for a product whose whole value is the
   quality of its answers, and whose answers an advisor is expected to defend to a client, that is the
   gap worth closing even though it is the least visible thing in this pass.

   The mechanism is Mindvalley's Eve AI: 👍/👎 at the foot of a response. Two things change here.
   No emoji — the system's rule — so the glyphs are drawn at the icon set's 24px grid / 1.5px stroke.
   And confirmation happens ON THE CONTROL that was pressed, never as a toast: the pressed thumb fills
   bronze and the row says "Noted", in place, because the no-toasts law means the acknowledgement has
   nowhere else to go.

   Down is not symmetrical with up, and it should not be. An advisor marking an answer wrong has a
   reason, and the reason is the whole value of the signal — so down opens a short reason row
   (existing Pill chips, not a free-text field: a field asks for an essay between meetings). Up is one
   tap and done. */
const Thumb = ({
  down,
  active
}) => /*#__PURE__*/React.createElement("svg", {
  width: "15",
  height: "15",
  viewBox: "0 0 24 24",
  fill: "none",
  "aria-hidden": "true",
  style: {
    display: 'block',
    transform: down ? 'rotate(180deg)' : 'none'
  }
}, /*#__PURE__*/React.createElement("path", {
  d: "M7 10v11H4a1 1 0 0 1-1-1v-9a1 1 0 0 1 1-1h3zm0 0 4.5-7a2 2 0 0 1 3.6 1.5L14 10h4.8a2 2 0 0 1 2 2.5l-1.7 7A2 2 0 0 1 17.1 21H7",
  stroke: active ? 'var(--color-bronze-deep)' : 'var(--color-muted)',
  fill: active ? 'var(--color-selected)' : 'none',
  strokeWidth: "1.5",
  strokeLinecap: "round",
  strokeLinejoin: "round"
}));
const REASONS = ['The number is wrong', 'Not what I asked', 'Missing a holding', 'Too long'];
function ResponseFeedback({
  value,
  onRate,
  onReason,
  reasons = REASONS,
  note
}) {
  const [rating, setRating] = React.useState(value || null);
  const [reason, setReason] = React.useState(null);
  const rate = v => {
    setRating(v);
    if (onRate) onRate(v);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-6)',
      margin: `calc(-1 * var(--space-4)) 0 calc(-1 * var(--space-6))`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-12)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: () => rate(rating === 'up' ? null : 'up'),
    pressed: rating === 'up',
    label: "This answer was right",
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: 28,
      height: 28
    }
  }, /*#__PURE__*/React.createElement(Thumb, {
    active: rating === 'up'
  })), /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: () => rate(rating === 'down' ? null : 'down'),
    pressed: rating === 'down',
    label: "Something is wrong with this answer",
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: 28,
      height: 28
    }
  }, /*#__PURE__*/React.createElement(Thumb, {
    down: true,
    active: rating === 'down'
  })), rating && /*#__PURE__*/React.createElement("span", {
    role: "status",
    style: {
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, rating === 'up' ? 'Noted' : reason ? 'Noted · ' + reason : 'Noted — what was wrong?')), rating === 'down' && !reason && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'var(--space-6)'
    }
  }, reasons.map(r => /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    key: r,
    onClick: () => {
      setReason(r);
      if (onReason) onReason(r);
    },
    label: `Reason: ${r}`,
    style: {
      display: 'inline-flex',
      height: 'var(--h-filter-chip)',
      alignItems: 'center',
      padding: `0 var(--space-12)`,
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-chip)',
      boxShadow: `0 0 0 var(--border-1) var(--color-line)`
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-label-font)',
      color: 'var(--color-bronze-deep)',
      whiteSpace: 'nowrap'
    }
  }, r)))), note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      font: 'var(--type-caption-font)',
      color: 'var(--color-data-deemph)'
    }
  }, note));
}
Object.assign(__ds_scope, { ResponseFeedback });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/ResponseFeedback.jsx", error: String((e && e.message) || e) }); }

// components/chat/SentinelText.jsx
try { (() => {
function SentinelText({
  text,
  weight = 'Medium'
}) {
  return /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: weight === 'Regular' ? 400 : 500,
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink-soft)',
      animation: 'ds-fade 300ms var(--ease) both'
    }
  }, text);
}
Object.assign(__ds_scope, { SentinelText });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/SentinelText.jsx", error: String((e && e.message) || e) }); }

// components/chat/StepTrace.jsx
try { (() => {
/* What the assistant actually did, as collapsible one-line rows with per-step state.

   The mechanism, not the layout: four AI products have converged on it — Manus indents a step's
   reasoning beneath it, Linktree opens `Analysis ⌄` onto a vertical-ruled list of ticked steps,
   ChatGPT collapses a tool call to `Ran 3 commands ›`, DeepSeek heads its trace `Thought for 5
   seconds ⌄`. For an advisor this is not decoration: "read her holdings · matched to the mandate ·
   checked the compliance shelf" IS the provenance they repeat to the client, and Sentinel currently
   buries it in a sentence.

   Sentinel's own language, not theirs: the spine and the 16px node are ProgressTrace's, the muted
   40% rule for inactive rows is ours, and the summary row is the existing collapsed-reasoning row.
   Same component serves FileUpload's staged parse — the stages ARE the parse summary arriving
   progressively, which is the "success describes the user's world" rule already in the readme.

   Failure is a first-class step, not an absence: Mimo's completed-card-then-`⚠ Error detected`-then-
   `Try to fix` sequence is the half Sentinel was missing. A failed step keeps its place in the list,
   carries the word, and offers its retry inline. */
const NODE = 16;
function Node({
  state
}) {
  const done = state === 'done',
    failed = state === 'failed',
    running = state === 'running';
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: 0,
      top: 3,
      display: 'flex',
      width: NODE,
      height: NODE,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)',
      background: done ? 'var(--color-bronze)' : failed ? 'var(--color-status-over-bg)' : 'var(--color-canvas)',
      boxShadow: done ? 'none' : `inset 0 0 0 var(--border-1) ${failed ? 'var(--color-status-over-fg)' : running ? 'var(--color-bronze)' : 'var(--color-line)'}`
    }
  }, done && /*#__PURE__*/React.createElement("svg", {
    width: "9",
    height: "9",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-surface)",
    strokeWidth: "3.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  })), failed && /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-eyebrow-font)',
      color: 'var(--color-status-over-fg)',
      lineHeight: 1
    }
  }, "!"), running && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-bronze)',
      animation: 'dot-pulse 1200ms var(--ease) infinite'
    }
  }));
}
function StepTrace({
  steps = [],
  summary,
  open: openProp,
  defaultOpen = false,
  onToggle,
  id = 'steptrace',
  dense = false
}) {
  const [openState, setOpenState] = React.useState(defaultOpen);
  const open = openProp == null ? openState : openProp;
  const toggle = () => {
    if (openProp == null) setOpenState(v => !v);
    if (onToggle) onToggle(!open);
  };
  const done = steps.filter(s => s.state === 'done').length;
  const failed = steps.some(s => s.state === 'failed');
  const running = steps.find(s => s.state === 'running');
  const head = summary || (failed ? 'Could not finish' : running ? running.label : `Read ${done} ${done === 1 ? 'source' : 'sources'}`);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: toggle,
    expanded: open,
    controls: id,
    label: `${head}. ${open ? 'Collapse' : 'Expand'} what Sentinel did.`,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-6)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-label-font)',
      color: failed ? 'var(--color-status-over-fg)' : 'var(--color-muted)'
    }
  }, head), /*#__PURE__*/React.createElement("svg", {
    width: "10",
    height: "10",
    viewBox: "0 0 24 24",
    fill: "none",
    "aria-hidden": "true",
    style: {
      display: 'block',
      transform: open ? 'rotate(180deg)' : 'none',
      transition: 'transform var(--dur-fast) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 9.5l6 6 6-6",
    stroke: failed ? 'var(--color-status-over-fg)' : 'var(--color-muted)',
    strokeWidth: "2.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), open && /*#__PURE__*/React.createElement("div", {
    id: id,
    role: "list",
    style: {
      position: 'relative',
      marginTop: 'var(--space-8)',
      display: 'flex',
      flexDirection: 'column',
      gap: dense ? 'var(--space-8)' : 'var(--space-12)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: 'absolute',
      left: 9,
      top: 'var(--space-8)',
      bottom: 'var(--space-8)',
      width: 'var(--border-1)',
      background: 'var(--color-line)'
    }
  }), steps.map((s, i) => {
    const inactive = s.state === 'pending';
    return /*#__PURE__*/React.createElement("div", {
      key: s.label + i,
      role: "listitem",
      style: {
        position: 'relative',
        paddingLeft: 'var(--space-24)',
        opacity: inactive ? 0.4 : 1,
        transition: 'opacity var(--dur-fast) var(--ease)'
      }
    }, /*#__PURE__*/React.createElement(Node, {
      state: s.state || 'pending'
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        gap: 'var(--space-8)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0,
        font: 'var(--type-row-font)',
        color: s.state === 'failed' ? 'var(--color-status-over-fg)' : 'var(--color-ink-soft)'
      }
    }, s.label), s.meta && /*#__PURE__*/React.createElement("span", {
      style: {
        flexShrink: 0,
        font: 'var(--type-caption-font)',
        color: 'var(--color-muted)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, s.meta)), s.detail && /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 'var(--space-2) 0 0',
        font: 'var(--type-caption-font)',
        color: 'var(--color-muted)'
      }
    }, s.detail), s.state === 'failed' && s.retry && /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 'var(--space-6)'
      }
    }, s.retry));
  })));
}
Object.assign(__ds_scope, { StepTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/StepTrace.jsx", error: String((e && e.message) || e) }); }

// components/chat/UserBubble.jsx
try { (() => {
/* Partner (advisor) bubble — peach, right-aligned, 20px radius with a 6px tail corner. */
function UserBubble({
  text
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: '100%',
      justifyContent: 'flex-end',
      animation: 'ds-rise var(--dur-enter) var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 280,
      borderRadius: '20px 20px 6px 20px',
      background: 'var(--color-bubble)',
      padding: '10px 12px',
      boxShadow: '0 0 0 1px var(--color-bubble-edge)'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-19)',
      color: 'var(--color-ink)'
    }
  }, text)));
}
Object.assign(__ds_scope, { UserBubble });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/UserBubble.jsx", error: String((e && e.message) || e) }); }

// components/chat/VersionRow.jsx
try { (() => {
/* Every revision of an artifact, as a row in the thread, with a revert on each.

   The mechanism is Wabi's: `V1 ↺ · V2 ↺ · V3 ↺ · V4` in the conversation itself. It solves a product
   hole rather than a visual one — an advisor who has revised a proposal twice currently has no way
   back to the version the client actually saw, and "the one I sent on Tuesday" is the version that
   matters when the client rings.

   Sentinel's own language: the row is ListRow's geometry, the current version carries the sand
   selected fill rather than a dot, and revert is a word, not a glyph alone (the text-before-a-glyph
   rule). Revert never destroys: it appends the restored version as a new revision, so the trail only
   ever grows — which is the same reasoning as MessageActions' edit warning, one level up. */
function VersionRow({
  versions = [],
  currentId,
  onSelect,
  onRevert,
  label = 'Versions'
}) {
  const current = currentId || (versions[versions.length - 1] || {}).id;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: `0 0 var(--space-6)`,
      font: 'var(--type-eyebrow-font)',
      letterSpacing: 'var(--tracking-eyebrow)',
      textTransform: 'uppercase',
      color: 'var(--color-muted)'
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    role: "list",
    style: {
      borderRadius: 'var(--radius-12)',
      background: 'var(--color-surface)',
      boxShadow: `inset 0 0 0 var(--border-1) var(--color-line)`,
      padding: `0 var(--space-12)`,
      boxSizing: 'border-box'
    }
  }, versions.map((v, i) => {
    const isCurrent = v.id === current;
    return /*#__PURE__*/React.createElement("div", {
      key: v.id,
      role: "listitem",
      style: {
        display: 'flex',
        minHeight: 'var(--h-row-md)',
        alignItems: 'center',
        gap: 'var(--space-8)',
        padding: `var(--space-8) 0`,
        borderTop: i ? `var(--border-hairline) solid var(--color-line-soft)` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        flexShrink: 0,
        alignItems: 'center',
        justifyContent: 'center',
        minWidth: 34,
        height: 22,
        borderRadius: 'var(--radius-full)',
        background: isCurrent ? 'var(--color-selected)' : 'var(--color-chip)',
        boxShadow: isCurrent ? `0 0 0 var(--border-1) var(--color-bronze)` : 'none',
        font: 'var(--type-label-font)',
        color: isCurrent ? 'var(--color-ink)' : 'var(--color-muted)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, v.name), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0,
        display: 'flex',
        flexDirection: 'column'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        overflow: 'hidden',
        textOverflow: 'ellipsis',
        whiteSpace: 'nowrap',
        font: 'var(--type-row-font)',
        color: 'var(--color-ink)'
      }
    }, v.summary), /*#__PURE__*/React.createElement("span", {
      style: {
        font: 'var(--type-caption-font)',
        color: 'var(--color-muted)'
      }
    }, v.meta, isCurrent ? ' · showing now' : '', v.sent ? ' · sent to the client' : '')), !isCurrent && onRevert && /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
      onClick: () => onRevert(v.id),
      label: `Revert to ${v.name}`,
      style: {
        flexShrink: 0,
        display: 'flex',
        alignItems: 'center',
        gap: 'var(--space-4)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        font: 'var(--type-label-font)',
        color: 'var(--color-bronze-deep)'
      }
    }, "Revert"), /*#__PURE__*/React.createElement("svg", {
      width: "11",
      height: "11",
      viewBox: "0 0 24 24",
      fill: "none",
      "aria-hidden": "true"
    }, /*#__PURE__*/React.createElement("path", {
      d: "M3 12a9 9 0 1 0 9-9 9 9 0 0 0-6.36 2.64L3 8",
      stroke: "var(--color-bronze-deep)",
      strokeWidth: "2.2",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M3 3v5h5",
      stroke: "var(--color-bronze-deep)",
      strokeWidth: "2.2",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    }))), isCurrent && onSelect && /*#__PURE__*/React.createElement("span", {
      style: {
        flexShrink: 0,
        font: 'var(--type-caption-font)',
        color: 'var(--color-data-deemph)'
      }
    }, "current"));
  })));
}
Object.assign(__ds_scope, { VersionRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/VersionRow.jsx", error: String((e && e.message) || e) }); }

// components/data/AttributionChart.jsx
try { (() => {
/* V3 · diverging attribution bar. Reads top-down as a chain from the target to today.
   One hue only; an intentional contribution ("what you did") renders in de-emphasis grey with a
   hairline outline so it separates from "what happened to you" without a second colour.
   Bars 20px thick, 2px surface gap, values direct-labelled at the tip. */
function Row({
  c,
  max,
  i,
  run
}) {
  const grey = c.intentional;
  /* run=false means "do not animate" — the bar is drawn at full length, never frozen at zero. */
  const [on, setOn] = React.useState(!run);
  React.useEffect(() => {
    if (run) {
      const t = setTimeout(() => setOn(true), 20);
      return () => clearTimeout(t);
    }
  }, [run]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-5)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 'var(--space-8)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-13)',
      lineHeight: 'var(--leading-18)',
      color: grey ? 'var(--color-data-deemph)' : 'var(--color-ink)'
    }
  }, c.label), /*#__PURE__*/React.createElement("span", {
    style: {
      flexShrink: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-13)',
      color: grey ? 'var(--color-data-deemph)' : 'var(--color-bronze-deep)'
    }
  }, c.value > 0 ? '+' : '−', Math.abs(c.value).toFixed(1))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-8)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 20,
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      bottom: 0,
      width: `${Math.abs(c.value) / max * 100}%`,
      borderRadius: '2px 6px 6px 2px',
      background: grey ? 'var(--color-track)' : 'var(--color-bronze)',
      boxShadow: grey ? 'inset 0 0 0 1px var(--color-data-deemph)' : 'none',
      transformOrigin: 'left',
      transform: on ? 'none' : 'scaleX(0)',
      transition: run ? 'transform 200ms var(--ease)' : 'none',
      transitionDelay: run ? `${i * 200}ms` : '0ms'
    }
  }))), c.note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '-1px 0 0',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, c.note));
}
function AttributionChart({
  from,
  to,
  targetLabel = 'Target',
  target,
  contributions,
  skeleton = false,
  run = true,
  todayLabel = 'Today'
}) {
  const total = __ds_scope.useCountUp(to, 600, run && !skeleton);
  const shown = run ? total : to;
  const max = Math.max(...contributions.map(c => Math.abs(c.value)), 1);
  const cap = {
    display: 'flex',
    alignItems: 'baseline',
    justifyContent: 'space-between',
    gap: 'var(--space-8)'
  };
  const capLabel = {
    margin: 0,
    fontFamily: 'var(--font-ui)',
    fontWeight: 'var(--weight-bold)',
    fontSize: 'var(--text-11)',
    textTransform: 'uppercase',
    letterSpacing: '0.08em',
    color: 'var(--color-muted)'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: cap
  }, /*#__PURE__*/React.createElement("p", {
    style: capLabel
  }, targetLabel), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-14)',
      color: 'var(--color-muted)'
    }
  }, target ?? from, "%")), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '8px 0 12px',
      height: 'var(--border-hairline)',
      width: '100%',
      background: 'var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-12)',
      opacity: skeleton ? 0.5 : 1
    }
  }, contributions.map((c, i) => /*#__PURE__*/React.createElement(Row, {
    key: c.label,
    c: c,
    max: max,
    i: i,
    run: run && !skeleton
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '12px 0 8px',
      height: 'var(--border-hairline)',
      width: '100%',
      background: 'var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: cap
  }, /*#__PURE__*/React.createElement("p", {
    style: capLabel
  }, todayLabel), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-display)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 40,
      lineHeight: 1,
      color: 'var(--color-bronze-deep)'
    }
  }, skeleton ? from : shown, "%")));
}
Object.assign(__ds_scope, { AttributionChart });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/AttributionChart.jsx", error: String((e && e.message) || e) }); }

// components/data/ConcentrationBar.jsx
try { (() => {
/* C15 · single 8px bronze bar on the alloc track + caption. */
function ConcentrationBar({
  fraction,
  label
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: 8,
      width: '100%',
      overflow: 'hidden',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-track)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: '100%',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-bronze)',
      transformOrigin: 'left',
      transform: `scaleX(${fraction})`,
      animation: 'ds-grow 500ms var(--ease) both'
    }
  })), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '6px 0 0',
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-medium)',
        fontSize: 'var(--text-12)',
        color: 'var(--color-muted)'
      }
    }
  }, label));
}
Object.assign(__ds_scope, { ConcentrationBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ConcentrationBar.jsx", error: String((e && e.message) || e) }); }

// components/data/DrawnCheck.jsx
try { (() => {
/* Step 7 · the drawn-check success mark — no confetti. */
function DrawnCheck({
  size = 22
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-bronze)',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size * 0.55,
    height: size * 0.55,
    viewBox: "0 0 12 12",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M2.2 6.2 4.8 8.8 9.8 3.2",
    stroke: "var(--color-surface)",
    strokeWidth: "1.6",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    pathLength: "1",
    style: {
      strokeDasharray: 1,
      strokeDashoffset: 1,
      animation: 'ds-draw 400ms var(--ease) forwards'
    }
  })), /*#__PURE__*/React.createElement("style", null, '@keyframes ds-draw{to{stroke-dashoffset:0}}@media (prefers-reduced-motion:reduce){@keyframes ds-draw{from{stroke-dashoffset:0;opacity:0}to{stroke-dashoffset:0;opacity:1}}}'));
}
Object.assign(__ds_scope, { DrawnCheck });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/DrawnCheck.jsx", error: String((e && e.message) || e) }); }

// components/data/Dumbbell.jsx
try { (() => {
/* V2 · dumbbell: hollow dot = target, filled dot = actual, bronze connector. */
function Dumbbell({
  label,
  target,
  actual,
  max = 100,
  targetLabel,
  actualLabel
}) {
  const lo = Math.min(target, actual),
    hi = Math.max(target, actual);
  const pct = v => `${v / max * 100}%`;
  const dot = {
    position: 'absolute',
    top: '50%',
    width: 10,
    height: 10,
    transform: 'translate(-50%,-50%)',
    borderRadius: 'var(--radius-full)',
    boxSizing: 'border-box'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, label && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '0 0 8px',
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-semibold)',
        fontSize: 'var(--text-13)',
        color: 'var(--color-ink)'
      }
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 16,
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      top: '50%',
      height: 'var(--border-1)',
      transform: 'translateY(-50%)',
      background: 'var(--color-track)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: '50%',
      height: 2,
      transform: 'translateY(-50%)',
      left: pct(lo),
      width: pct(hi - lo)
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: '100%',
      background: 'var(--color-bronze)',
      transformOrigin: 'left',
      animation: 'ds-grow 400ms var(--ease) both'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      ...dot,
      left: pct(target),
      border: '2px solid var(--color-bronze)',
      background: 'var(--color-canvas)',
      boxShadow: '0 0 0 2px var(--color-surface)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      ...dot,
      left: pct(actual),
      background: 'var(--color-bronze)',
      boxShadow: '0 0 0 2px var(--color-surface)'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-5)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-muted)'
    }
  }, actualLabel ?? `${actual}%`, " now"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-bronze-deep)'
    }
  }, "\u2192 ", targetLabel ?? `${target}%`)));
}
Object.assign(__ds_scope, { Dumbbell });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Dumbbell.jsx", error: String((e && e.message) || e) }); }

// components/data/chartMath.jsx
try { (() => {
/* Chart maths — scales, nice ticks and path generation. No marks, no styling.
   `d3-scale` and `d3-shape` do the arithmetic when they are on the page (window.d3 — load them as
   ESM from a CDN or via @visx/scale + @visx/shape). They are peer dependencies, not bundled: two
   small modules with no DOM, no defaults and no styling opinions, which is the whole reason they are
   the only chart dependency in the system. No charting library renders a Sentinel chart.
   The fallbacks below use d3's own 1/2/5 tick algorithm so a page without d3 still lands its
   baselines on the grid; what it loses is monotone curves (straight segments instead) and time
   scales. The visible difference is small; the maths discipline is not optional either way. */
const d3 = () => typeof window !== 'undefined' && window.d3 ? window.d3 : null;
function hasD3() {
  return !!(d3() && d3().scaleLinear && d3().line);
}
function step125(raw) {
  const mag = Math.pow(10, Math.floor(Math.log10(raw)));
  const norm = raw / mag;
  return (norm >= 5 ? 10 : norm >= 2 ? 5 : norm >= 1 ? 2 : 1) * mag;
}
/* A domain rounded outward to whole tick steps — never a raw data min/max, which is what puts a
   baseline half a pixel off the grid and reads as amateur. */
function niceDomain(min, max, count = 4) {
  const lib = d3();
  if (lib && lib.scaleLinear) {
    const s = lib.scaleLinear().domain([min, max]).nice(count);
    return s.domain();
  }
  if (max === min) return [min - 1, max + 1];
  const s = step125((max - min) / count);
  return [Math.floor(min / s) * s, Math.ceil(max / s) * s];
}
function ticks(min, max, count = 4) {
  const lib = d3();
  if (lib && lib.ticks) return lib.ticks(min, max, count);
  const s = step125((max - min) / count);
  const out = [];
  for (let v = Math.ceil(min / s) * s; v <= max + 1e-9; v += s) out.push(Math.round(v * 1e6) / 1e6);
  return out;
}
function linear(domain, range) {
  const lib = d3();
  if (lib && lib.scaleLinear) return lib.scaleLinear().domain(domain).range(range);
  const [d0, d1] = domain,
    [r0, r1] = range;
  const f = v => d1 === d0 ? r0 : r0 + (v - d0) / (d1 - d0) * (r1 - r0);
  f.domain = () => domain;
  f.range = () => range;
  return f;
}
/* Band scale for bar rhythm — even bands, even gaps, no arbitrary arithmetic per chart. */
function band(labels, range, padding = 0.34) {
  const lib = d3();
  if (lib && lib.scaleBand) return lib.scaleBand().domain(labels).range(range).padding(padding);
  const [r0, r1] = range;
  const stepSize = (r1 - r0) / Math.max(labels.length, 1);
  const w = stepSize * (1 - padding);
  const f = l => r0 + labels.indexOf(l) * stepSize + (stepSize - w) / 2;
  f.bandwidth = () => w;
  f.step = () => stepSize;
  return f;
}
/* Monotone path through pixel-space [x, y] pairs — curveMonotoneX never overshoots a data point,
   which matters when the line is a portfolio value and an invented dip is a lie. */
function linePath(pts) {
  const lib = d3();
  if (lib && lib.line) return lib.line().x(p => p[0]).y(p => p[1]).curve(lib.curveMonotoneX)(pts);
  return pts.map((p, i) => `${i ? 'L' : 'M'}${p[0]},${p[1]}`).join('');
}
function areaPath(pts, y0) {
  const lib = d3();
  if (lib && lib.area) return lib.area().x(p => p[0]).y0(y0).y1(p => p[1]).curve(lib.curveMonotoneX)(pts);
  return `${pts.map((p, i) => `${i ? 'L' : 'M'}${p[0]},${p[1]}`).join('')}L${pts[pts.length - 1][0]},${y0}L${pts[0][0]},${y0}Z`;
}
/* The four-step bronze ramp — the system's only chart palette, ordinal by rank. */
const CHART_RAMP = ['var(--color-bronze-deep)', 'var(--color-bronze)', 'var(--color-alloc-debt)', 'var(--color-bubble-edge)'];
/* Colour comes from a data role, never from a prop. ramp = the client's own money · muted = benchmark,
   target, prior period · status = a crossed limit and nothing else. `tone` never accepts a colour.
   v11: muted swapped from --color-data-deemph (2.52 / 2.77 — below even the 3.0 non-text floor) to
   --color-muted (6.26 / 6.88). Same role, same warm grey family, already in the palette. */
function toneColor(tone, rank = 0) {
  if (tone === 'muted') return 'var(--color-muted)';
  if (tone === 'status') return 'var(--color-status-over-fg)';
  return CHART_RAMP[Math.min(rank, CHART_RAMP.length - 1)];
}
/* v11 · PERCEIVABILITY. WCAG 1.4.11 asks a graphical object to clear 3.0:1 against adjacent colour.
   Measured against canvas #f6f4f1 / surface #ffffff:
     ramp 1 bronze-deep  6.59 / 7.24  pass
     ramp 2 bronze       2.57 / 2.83  FAIL
     ramp 3 alloc-debt   1.66 / 1.82  FAIL
     ramp 4 bubble-edge  1.30 / 1.42  FAIL
   So the palette carries exactly ONE chart mark that is legible as a shape against the page.
   `markColor` is therefore the only correct source for a mark that must separate FROM THE BACKGROUND
   — a line, a single bar, a crosshair, a sparkline. It returns ramp 1 regardless of rank.
   `CHART_RAMP` by rank stays legal only where marks separate from EACH OTHER by edge and label
   (stacked segments, adjacent bars), never from the page. No colour changed to fix this. */
function markColor(tone) {
  if (tone === 'muted') return 'var(--color-muted)';
  if (tone === 'status') return 'var(--color-status-over-fg)';
  return CHART_RAMP[0];
}
/* Adjacent-segment separation cannot be solved by colour in this palette either — measured, every
   candidate fails against at least one ramp step (line 1.06, surface 1.42, ink 2.25, muted 1.05 at
   worst). So segments are separated by SHAPE and WORD: a --space-2 gap of the card's own background
   showing through, a --border-hairline --color-line edge on the bar, and the direct label the system
   already mandates. 1.4.11 exempts a graphic whose information is also present as text, and the
   label is that text — so the label is the compliance mechanism and the gap is the perceptual aid.
   Not the reverse: a segment with no label is a violation, not a style choice. */
const SEGMENT_GAP = 'var(--space-2)';
const trackInset = {
  boxShadow: 'inset 0 0 0 var(--border-hairline) var(--color-line)'
};
const PLOT = {
  peek: 72,
  expanded: 180
};
const AXIS_BAND = 14;
const tabular = {
  fontVariantNumeric: 'tabular-nums'
};
Object.assign(__ds_scope, { hasD3, niceDomain, ticks, linear, band, linePath, areaPath, CHART_RAMP, toneColor, markColor, SEGMENT_GAP, trackInset, PLOT, AXIS_BAND, tabular });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/chartMath.jsx", error: String((e && e.message) || e) }); }

// components/data/ChartBar.jsx
try { (() => {
/* Vertical for time, horizontal for comparison. 24px maximum thickness, 4px rounded at the data end
   and square where it meets the baseline, values direct-labelled at the end in tabular figures.
   Growth is scaleY from transform-origin bottom (scaleX from left when horizontal), 480ms, staggered
   60ms down the rows, so an allocation assembles rather than appears.

   v11 · every bar in this chart sits directly on the page, so every bar must clear 3:1 against it — and
   measured, only ramp 1 does (6.59 / 7.24; ramp 2 is 2.57, ramp 3 is 1.66, ramp 4 is 1.30). So bars are
   ONE colour, from markColor. That is not a loss: rank in a bar chart is encoded by LENGTH, which is
   the strongest channel there is, and the ramp was encoding it a second time in a channel three
   quarters of which nobody can see. tone still overrides per bar for the one crossed limit. */
const FONT = 'var(--font-ui)';
const MAX_THICK = 24;
function ChartBar({
  bars = [],
  orientation = 'horizontal',
  density = 'expanded',
  tone = 'ramp',
  width = 311,
  valueFormat = v => `${v}%`,
  run = true,
  caveat
}) {
  const peek = density === 'peek';
  const plot = __ds_scope.PLOT[peek ? 'peek' : 'expanded'];
  const [on, setOn] = React.useState(!run);
  React.useEffect(() => {
    if (run) {
      const t = setTimeout(() => setOn(true), 20);
      return () => clearTimeout(t);
    }
  }, [run]);
  const [d0, d1] = __ds_scope.niceDomain(0, Math.max(...bars.map(b => b.value), 1), peek ? 2 : 4);
  if (orientation === 'vertical') {
    const labels = bars.map(b => b.label);
    const x = __ds_scope.band(labels, [0, width - 8], 0.34);
    const thick = Math.min(x.bandwidth(), MAX_THICK);
    const y = __ds_scope.linear([d0, d1], [plot, 0]);
    return /*#__PURE__*/React.createElement("div", {
      style: {
        width: '100%'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'relative',
        height: plot,
        width: '100%'
      }
    }, bars.map((b, i) => /*#__PURE__*/React.createElement("div", {
      key: b.label,
      style: {
        position: 'absolute',
        left: x(b.label) + (x.bandwidth() - thick) / 2,
        bottom: 0,
        width: thick,
        height: Math.max(plot - y(b.value), 2),
        borderRadius: 'var(--radius-4, 4px) var(--radius-4, 4px) 0 0',
        background: __ds_scope.markColor(b.tone || tone),
        transformOrigin: 'bottom',
        transform: on ? 'none' : 'scaleY(0)',
        transition: run ? 'transform var(--dur-bar) var(--ease)' : 'none',
        transitionDelay: run ? `${i * 60}ms` : '0ms'
      }
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: -0.5,
        height: 'var(--border-1)',
        background: 'var(--color-line)'
      }
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'relative',
        height: __ds_scope.AXIS_BAND,
        width: '100%'
      }
    }, bars.map(b => /*#__PURE__*/React.createElement("span", {
      key: b.label,
      style: {
        position: 'absolute',
        left: x(b.label),
        width: x.bandwidth(),
        textAlign: 'center',
        fontFamily: FONT,
        fontWeight: 'var(--weight-medium)',
        fontSize: 'var(--text-11)',
        lineHeight: `${__ds_scope.AXIS_BAND}px`,
        color: 'var(--color-muted)',
        whiteSpace: 'nowrap'
      }
    }, b.label))));
  }
  const rowH = peek ? 18 : 22;
  const barH = Math.min(rowH - 6, MAX_THICK);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: peek ? 4 : 8,
      width: '100%'
    }
  }, bars.map((b, i) => /*#__PURE__*/React.createElement("div", {
    key: b.label,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-8)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: '0 0 33%',
      minWidth: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      fontFamily: FONT,
      fontWeight: 'var(--weight-medium)',
      fontSize: peek ? 11 : 12.5,
      lineHeight: `${rowH}px`,
      color: 'var(--color-ink)'
    }
  }, b.label), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'relative',
      flex: 1,
      minWidth: 0,
      height: rowH,
      display: 'flex',
      alignItems: 'center',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-track)',
      boxShadow: 'inset 0 0 0 var(--border-hairline) var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: 0,
      top: (rowH - barH) / 2,
      height: barH,
      width: `${(b.value - d0) / (d1 - d0) * 100}%`,
      borderRadius: '0 4px 4px 0',
      background: __ds_scope.markColor(b.tone || tone),
      transformOrigin: 'left',
      transform: on ? 'none' : 'scaleX(0)',
      transition: run ? 'transform var(--dur-bar) var(--ease)' : 'none',
      transitionDelay: run ? `${i * 60}ms` : '0ms'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      ...__ds_scope.tabular,
      flexShrink: 0,
      textAlign: 'right',
      fontFamily: FONT,
      fontWeight: 'var(--weight-bold)',
      fontSize: peek ? 11 : 12.5,
      lineHeight: `${rowH}px`,
      color: 'var(--color-bronze-deep)'
    }
  }, valueFormat(b.value)))), caveat && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 'var(--space-2) 0 0',
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, caveat));
}
Object.assign(__ds_scope, { ChartBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ChartBar.jsx", error: String((e && e.message) || e) }); }

// components/data/ChartLegend.jsx
try { (() => {
/* Direct labels first. A legend is what we use when direct labelling is impossible — one series never
   has one, two series are end-labelled, so its real homes are ChartShare and the overlap bars.
   Ordered by value descending, always, never alphabetically: the legend then doubles as the ranking
   and every row carries its own number, which is what makes it readable with no colour at all.
   Left-aligned to the plot's left edge, never centred — a centred legend floats free of the card's rag.
   Text never wears the data colour: the 8px dot carries identity, the label stays ink. Labels wrap. */
const amountOf = it => typeof it.amount === 'number' ? it.amount : parseFloat(String(it.value).replace(/[^0-9.-]/g, '')) || 0;
function ChartLegend({
  items = [],
  layout = 'stacked',
  sort = true
}) {
  const rows = sort ? items.slice().sort((a, b) => amountOf(b) - amountOf(a)) : items.slice();
  const stacked = layout === 'stacked';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: stacked ? 'column' : 'row',
      flexWrap: stacked ? 'nowrap' : 'wrap',
      gap: stacked ? 6 : '6px 14px',
      width: '100%',
      alignItems: stacked ? 'stretch' : 'flex-start'
    }
  }, rows.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: it.label,
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 'var(--space-6)',
      width: stacked ? '100%' : 'auto',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flexShrink: 0,
      width: 8,
      height: 8,
      marginTop: 'var(--space-4)',
      borderRadius: 'var(--radius-full)',
      background: __ds_scope.CHART_RAMP[Math.min(it.step ? it.step - 1 : i, __ds_scope.CHART_RAMP.length - 1)]
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: stacked ? 1 : '0 1 auto',
      minWidth: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 11.5,
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-ink)',
      overflowWrap: 'break-word'
    }
  }, it.label), it.value != null && /*#__PURE__*/React.createElement("span", {
    style: {
      ...__ds_scope.tabular,
      flexShrink: 0,
      marginLeft: stacked ? 8 : 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 11.5,
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)',
      textAlign: 'right'
    }
  }, it.value))));
}
Object.assign(__ds_scope, { ChartLegend });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ChartLegend.jsx", error: String((e && e.message) || e) }); }

// components/data/ChartReadout.jsx
try { (() => {
/* For dragging along a plot. A FIXED row above the chart, never a box that follows the finger: during
   a scrub the finger is sitting on the thing you are trying to read. Nothing moves except the numbers
   and the 1px crosshair the chart draws. The row is reserved when idle, showing the latest value, so
   it never appears out of nowhere and shifts the layout — that reservation is the whole component. */
function ChartReadout({
  label,
  value,
  active = false,
  idleNote
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: 22,
      width: '100%',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 'var(--space-8)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      minWidth: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, label || idleNote || ''), /*#__PURE__*/React.createElement("span", {
    style: {
      ...__ds_scope.tabular,
      flexShrink: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-13)',
      lineHeight: 'var(--leading-18)',
      color: active ? 'var(--color-ink)' : 'var(--color-bronze-deep)'
    }
  }, value));
}
Object.assign(__ds_scope, { ChartReadout });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ChartReadout.jsx", error: String((e && e.message) || e) }); }

// components/data/ChartLine.jsx
try { (() => {
/* One or two series, never three — three or more is small multiples (one chart each, same scale,
   stacked down the card), because a third line would have to encode identity in a hue and this palette
   cannot. Both series end-labelled directly — the end label carries the value AND the series name, so
   there is no legend and no second label row: the block is plot + axis band and nothing else (194 at
   expanded, inside the 208 budget; 86 at peek, inside the 96 card). Stroke 2px round caps; series 2
   dashed 4-2, muted, drawn behind. Area fill (--tint-bronze-06) only on a single series. Baseline only
   — no gridlines — with one dashed line when there is a target to draw.
   Draw-on is stroke-dashoffset, the system's one named motion exception: ≤600ms, ease-out, first
   reveal only, never on a re-render, and nothing under prefers-reduced-motion.
   v11: both strokes come from markColor, so a line always clears 3:1 against the page — ramp 1 for
   the client's own series (6.59 / 7.24) and --color-muted for the benchmark (6.26 / 6.88), which also
   separates the two from each other. The old muted (--color-data-deemph, 2.52) drew an invisible line. */
const FONT = 'var(--font-ui)';
function ChartLine({
  series = [],
  density = 'expanded',
  tone = 'ramp',
  target,
  width = 311,
  valueFormat = v => `${v}%`,
  xFormat = x => String(x),
  scrub = false,
  run = true,
  domain
}) {
  const peek = density === 'peek';
  const plot = __ds_scope.PLOT[peek ? 'peek' : 'expanded'];
  const labelBand = peek ? 14 : __ds_scope.AXIS_BAND;
  const use = series.slice(0, 2);
  const [tip, setTip] = React.useState(null);
  const drawn = React.useRef(false);
  React.useEffect(() => {
    drawn.current = true;
  }, []);
  const animate = run && !drawn.current;
  const padR = peek ? 34 : 62;
  const w = Math.max(80, width - padR);
  const all = use.flatMap(s => s.points.map(p => p.y));
  /* An explicit domain is how small multiples share one scale — without it each chart would nice its
     own range and three charts that look comparable would not be. */
  let [d0, d1] = domain || __ds_scope.niceDomain(Math.min(...all, target ? target.value : Infinity), Math.max(...all, target ? target.value : -Infinity), peek ? 2 : 4);
  /* A target that lands on the domain floor would draw its dashed rule underneath the baseline, where
     nobody can see it. Drop the floor by one whole tick step — still on the grid, and the target reads. */
  if (target && !domain && target.value <= d0 + (d1 - d0) * 0.02) {
    const t = __ds_scope.ticks(d0, d1, peek ? 2 : 4);
    d0 -= t.length > 1 ? t[1] - t[0] : (d1 - d0) / 4;
  }
  const xs = use[0] ? use[0].points.map(p => p.x) : [0, 1];
  const x = __ds_scope.linear([Math.min(...xs), Math.max(...xs)], [1, w - 1]);
  const y = __ds_scope.linear([d0, d1], [plot - 1, 1]);
  const pts = s => s.points.map(p => [x(p.x), y(p.y)]);
  const idx = tip == null ? use[0] ? use[0].points.length - 1 : 0 : tip;
  const active = use[0] ? use[0].points[idx] : null;
  const svgRef = React.useRef(null);
  const onMove = e => {
    if (!scrub || !use[0]) return;
    const el = svgRef.current;
    if (!el) return;
    const box = el.getBoundingClientRect();
    const rel = Math.min(Math.max(e.clientX - box.left, 0), box.width);
    const step = box.width / Math.max(use[0].points.length - 1, 1);
    setTip(Math.min(Math.round(rel / step), use[0].points.length - 1));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, scrub && !peek && /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 'var(--space-6)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ChartReadout, {
    label: active ? xFormat(active.x) : '',
    value: active ? valueFormat(active.y) : '',
    active: tip != null
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      alignItems: 'flex-start',
      gap: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      flex: 1,
      minWidth: 0,
      touchAction: 'pan-y',
      paddingRight: padR
    },
    onPointerDown: onMove,
    onPointerMove: e => tip != null && onMove(e),
    onPointerUp: () => setTip(null),
    onPointerLeave: () => setTip(null)
  }, /*#__PURE__*/React.createElement("svg", {
    ref: svgRef,
    width: "100%",
    height: plot,
    viewBox: `0 0 ${w} ${plot}`,
    preserveAspectRatio: "none",
    style: {
      display: 'block',
      overflow: 'visible'
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes ds-draw-on{to{stroke-dashoffset:0}}@media (prefers-reduced-motion:reduce){@keyframes ds-draw-on{from{stroke-dashoffset:0;opacity:0}to{stroke-dashoffset:0;opacity:1}}}'), target && /*#__PURE__*/React.createElement("line", {
    x1: "0",
    x2: w,
    y1: y(target.value),
    y2: y(target.value),
    stroke: "var(--color-line)",
    strokeWidth: "1",
    strokeDasharray: "4 2",
    vectorEffect: "non-scaling-stroke"
  }), use.length === 1 && /*#__PURE__*/React.createElement("path", {
    d: __ds_scope.areaPath(pts(use[0]), plot - 1),
    fill: "var(--tint-bronze-06)"
  }), use.slice().reverse().map((s, ri) => {
    const i = use.length - 1 - ri;
    const second = i === 1;
    return /*#__PURE__*/React.createElement("path", {
      key: s.label,
      d: __ds_scope.linePath(pts(s)),
      fill: "none",
      vectorEffect: "non-scaling-stroke",
      stroke: second ? __ds_scope.markColor('muted') : __ds_scope.markColor(s.tone || tone),
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeDasharray: second ? '4 2' : animate ? '1000' : undefined,
      strokeDashoffset: second ? undefined : animate ? 1000 : undefined,
      style: !second && animate ? {
        animation: 'ds-draw-on 600ms cubic-bezier(0.2,0.8,0.2,1) both'
      } : undefined
    });
  }), /*#__PURE__*/React.createElement("line", {
    x1: "0",
    x2: w,
    y1: plot - 0.5,
    y2: plot - 0.5,
    stroke: "var(--color-line)",
    strokeWidth: "1",
    vectorEffect: "non-scaling-stroke"
  }), scrub && tip != null && active && /*#__PURE__*/React.createElement("line", {
    x1: x(active.x),
    x2: x(active.x),
    y1: "0",
    y2: plot - 1,
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1",
    vectorEffect: "non-scaling-stroke"
  })), use.map((s, i) => {
    const last = s.points[s.points.length - 1];
    const top = Math.min(Math.max(y(last.y) - (peek ? 8 : 15), 0), plot - (peek ? 16 : 44));
    return /*#__PURE__*/React.createElement("div", {
      key: s.label,
      style: {
        position: 'absolute',
        right: 0,
        top,
        width: padR - 6,
        display: 'flex',
        flexDirection: 'column'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        ...__ds_scope.tabular,
        fontFamily: FONT,
        fontWeight: 'var(--weight-bold)',
        fontSize: peek ? 11.5 : 12,
        lineHeight: 'var(--leading-15)',
        color: i === 1 ? 'var(--color-data-deemph)' : 'var(--color-bronze-deep)'
      }
    }, valueFormat(last.y)), !peek && /*#__PURE__*/React.createElement("span", {
      style: {
        overflowWrap: 'break-word',
        fontFamily: FONT,
        fontWeight: 'var(--weight-medium)',
        fontSize: 10.5,
        lineHeight: 'var(--leading-14)',
        color: i === 1 ? 'var(--color-data-deemph)' : 'var(--color-muted)'
      }
    }, s.label));
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: labelBand,
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 'var(--space-8)',
      paddingRight: padR
    }
  }, peek ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FONT,
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-14)',
      color: 'var(--color-muted)'
    }
  }, use[0] ? xFormat(use[0].points[0].x) : ''), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FONT,
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-14)',
      color: 'var(--color-muted)'
    }
  }, use[0] ? xFormat(use[0].points[use[0].points.length - 1].x) : '')) : __ds_scope.ticks(d0, d1, 2).slice(0, 2).map((t, i) => /*#__PURE__*/React.createElement("span", {
    key: t,
    style: {
      ...__ds_scope.tabular,
      fontFamily: FONT,
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-14)',
      color: 'var(--color-muted)'
    }
  }, i === 0 && use[0] ? xFormat(use[0].points[0].x) : use[0] ? xFormat(use[0].points[use[0].points.length - 1].x) : ''))));
}
/* Three or more series: one chart per series, same scale, stacked down the card. Never three colours. */
function ChartLineMultiples({
  series = [],
  width = 311,
  valueFormat,
  xFormat,
  tone = 'ramp'
}) {
  const all = series.flatMap(s => s.points.map(p => p.y));
  const domain = __ds_scope.niceDomain(Math.min(...all), Math.max(...all), 2);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-14)'
    }
  }, series.map(s => /*#__PURE__*/React.createElement("div", {
    key: s.label
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '0 0 4px',
      fontFamily: FONT,
      fontWeight: 'var(--weight-medium)',
      fontSize: 11.5,
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-ink)'
    }
  }, s.label), /*#__PURE__*/React.createElement(ChartLine, {
    series: [{
      ...s
    }],
    domain: domain,
    density: "peek",
    tone: tone,
    width: width,
    valueFormat: valueFormat,
    xFormat: xFormat,
    run: false
  }))));
}
Object.assign(__ds_scope, { ChartLine, ChartLineMultiples });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ChartLine.jsx", error: String((e && e.message) || e) }); }

// components/data/ChartShare.jsx
try { (() => {
/* The pie, answered. A 100% stacked bar with a ranked legend beneath — the same information, and it
   reads at 375pt, in greyscale and in print, which a pie in this palette does not (the validator
   measured it: adjacent ramp pairs ΔE 11.4 against a floor of 15; a muted earthy trio collapses to
   ΔE 3.8 under protanopia).

   v11 · the segments carry the same perceivability problem the pie was rejected for, and no colour in
   this palette fixes it: measured, every separator candidate fails 3:1 against at least one ramp step
   (line 1.06, surface 1.42, muted 1.05, ink 2.25 at worst). So separation is by SHAPE and WORD — a
   --space-2 gap of the card's own background between segments, a --border-hairline edge around the
   bar, and a direct label on every segment. WCAG 1.4.11 exempts a graphic whose information is also
   present as text: the labels are that text, so the LABEL is the compliance mechanism and the gap is
   the perceptual aid. A segment with no label is a violation, not a style choice — so above two
   segments the legend is FORCED ON and the `legend` prop is ignored.

   AND THEREFORE: A PEEK SHARE CARRIES AT MOST TWO SEGMENTS. Forcing the legend on collided head-on
   with the 96pt peek budget — three legend rows measured a 99px block inside a 96px card, so the
   chart could neither legally drop the labels nor legally keep them. The resolution is not a smaller
   legend; it is fewer segments. `density="peek"` collapses the tail into one "Other" segment, so the
   bar is leader + Other, both DIRECT-labelled at the bar's ends, and the legend is unnecessary rather
   than suppressed — the label requirement is satisfied by the direct labels, which is the same reason
   `legend={false}` was always honoured at two segments or fewer. Peek is recognition, not reading:
   "Equity 71% · Other 29%" is the whole job of a 96pt preview, and the breakdown is one tap away in
   expanded. The guard then defers to density with no special case, because ranked.length <= 2 by
   construction. `legend` defaults to on when expanded and OFF at peek — without that the guard falls
   through to a `true` default after the collapse (ranked.length is 2, so the > 2 branch never fires)
   and the legend renders anyway: 77px measured where the contract said 30. Measured after: 8 + 6 + 15
   = 29px, two children (the direct-label row is --leading-15, not 16 — the first draft of this figure
   rounded it and was wrong by a pixel in the other direction). */
function collapse(ranked, total, otherLabel) {
  if (ranked.length <= 2) return ranked;
  const [lead] = ranked;
  const rest = ranked.slice(1).reduce((a, s) => a + s.value, 0);
  return [lead, {
    label: otherLabel,
    value: rest,
    other: true
  }];
}
function ChartShare({
  segments = [],
  density = 'expanded',
  height,
  legend,
  valueFormat = (v, pct) => `${pct.toFixed(0)}%`,
  run = true,
  caveat,
  otherLabel = 'Other'
}) {
  const peek = density === 'peek';
  const total = segments.reduce((s, x) => s + x.value, 0) || 1;
  const sorted = segments.slice().sort((a, b) => b.value - a.value);
  const ranked = peek ? collapse(sorted, total, otherLabel) : sorted;
  const h = height || (peek ? 8 : 10);
  const [on, setOn] = React.useState(!run);
  React.useEffect(() => {
    if (run) {
      const t = setTimeout(() => setOn(true), 20);
      return () => clearTimeout(t);
    }
  }, [run]);
  /* The DEFAULT follows density, which is the whole point of collapsing: peek's two segments are
     direct-labelled at the bar's ends, so a legend there is a third row repeating what the row above
     it already says. Leaving `legend = true` as the default made the guard fall through to it after
     the collapse (ranked.length is 2, so the > 2 branch never fires) and the legend rendered anyway —
     77px where the contract said 30. Above two segments it is still forced on whatever the caller asks. */
  const showLegend = ranked.length > 2 ? true : legend == null ? !peek : legend;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    role: "img",
    "aria-label": ranked.map(s => `${s.label} ${valueFormat(s.value, s.value / total * 100)}`).join(', '),
    style: {
      display: 'flex',
      width: '100%',
      height: h,
      gap: __ds_scope.SEGMENT_GAP,
      borderRadius: 'var(--radius-full)',
      overflow: 'hidden',
      background: 'var(--color-track)',
      boxShadow: 'inset 0 0 0 var(--border-hairline) var(--color-line)',
      transformOrigin: 'left',
      transform: on ? 'none' : 'scaleX(0)',
      transition: run ? 'transform var(--dur-bar) var(--ease)' : 'none'
    }
  }, ranked.map((s, i) => /*#__PURE__*/React.createElement("span", {
    key: s.label,
    style: {
      width: `calc(${s.value / total * 100}% - ${i ? 'var(--space-2)' : '0px'})`,
      background: s.tone === 'status' ? 'var(--color-status-over-fg)' : s.other ? 'var(--color-alloc-debt)' : __ds_scope.CHART_RAMP[Math.min(i, __ds_scope.CHART_RAMP.length - 1)]
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      gap: 'var(--space-8)',
      marginTop: 'var(--space-6)'
    }
  }, ranked.slice(0, 1).map(s => /*#__PURE__*/React.createElement("span", {
    key: s.label,
    style: {
      ...__ds_scope.tabular,
      font: 'var(--type-caption-font)',
      fontWeight: 'var(--weight-bold)',
      color: 'var(--color-bronze-deep)'
    }
  }, s.label, " ", valueFormat(s.value, s.value / total * 100))), ranked.length > 1 && (s => /*#__PURE__*/React.createElement("span", {
    style: {
      ...__ds_scope.tabular,
      font: 'var(--type-caption-font)',
      fontWeight: 'var(--weight-medium)',
      color: 'var(--color-muted)'
    }
  }, s.label, " ", valueFormat(s.value, s.value / total * 100)))(ranked[ranked.length - 1])), showLegend && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-10)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ChartLegend, {
    items: ranked.map((s, i) => ({
      label: s.label,
      value: valueFormat(s.value, s.value / total * 100),
      amount: s.value,
      step: Math.min(i + 1, 4)
    }))
  })), caveat && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 'var(--space-6) 0 0',
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, caveat));
}
Object.assign(__ds_scope, { ChartShare });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ChartShare.jsx", error: String((e && e.message) || e) }); }

// components/data/ChartTooltip.jsx
try { (() => {
/* For TAPPING a single mark. Hover does not exist at 375pt: this appears on tap, sits above the mark,
   flips below near the top edge, and dismisses on the next tap anywhere.
   Box style is the HeroNumberCard idiom so it belongs: surface fill, radius 8, padding 8/10, hairline
   as inset 0 0 0 0.5px --color-line. No arrow or tail — a 1px leader line to the mark instead, because
   a tail clips at the plot's edges and a clipped tail just looks broken.
   Two obligations sit with the caller: a tap target of at least 44pt around the mark, and a table view
   in the card's ⋯, so no value is ever reachable only by touching a coloured shape. */
function ChartTooltip({
  anchor = 'above',
  label,
  value,
  meta,
  leader = 14,
  style
}) {
  const above = anchor !== 'below';
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    style: {
      position: 'absolute',
      left: '50%',
      [above ? 'bottom' : 'top']: '100%',
      transform: 'translateX(-50%)',
      [above ? 'marginBottom' : 'marginTop']: leader,
      zIndex: 20,
      pointerEvents: 'none',
      width: 'max-content',
      maxWidth: 200,
      borderRadius: 'var(--radius-8)',
      background: 'var(--color-surface)',
      padding: '8px 10px',
      boxSizing: 'border-box',
      boxShadow: 'inset 0 0 0 0.5px var(--color-line), var(--shadow-float)',
      animation: `ds-tip-${above ? 'up' : 'down'} 160ms var(--ease) both`,
      ...style
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes ds-tip-up{from{opacity:0;transform:translateX(-50%) translateY(4px)}to{opacity:1;transform:translateX(-50%)}}@keyframes ds-tip-down{from{opacity:0;transform:translateX(-50%) translateY(-4px)}to{opacity:1;transform:translateX(-50%)}}@media (prefers-reduced-motion:reduce){@keyframes ds-tip-up{from{opacity:0;transform:translateX(-50%)}to{opacity:1;transform:translateX(-50%)}}@keyframes ds-tip-down{from{opacity:0;transform:translateX(-50%)}to{opacity:1;transform:translateX(-50%)}}}'), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-15)',
      color: 'var(--color-muted)'
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      ...__ds_scope.tabular,
      margin: '1px 0 0',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-13)',
      lineHeight: 'var(--leading-18)',
      color: 'var(--color-ink)',
      whiteSpace: 'nowrap'
    }
  }, value), meta && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '3px 0 0',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-15)',
      color: 'var(--color-data-deemph)'
    }
  }, meta), /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      left: '50%',
      [above ? 'top' : 'bottom']: '100%',
      width: 1,
      height: leader,
      background: 'var(--color-line)'
    }
  }));
}
Object.assign(__ds_scope, { ChartTooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ChartTooltip.jsx", error: String((e && e.message) || e) }); }

// components/forms/SearchField.jsx
try { (() => {
/* The search field a `searchable` List renders — the composer's visual style (radius 20, white,
   1px line ring, bronze focus ring at 150ms) at one row and 44px, so it reads as a filter and not as
   a second composer. For picking one client out of 512; the composer remains the path for an advisor
   who already knows the name. `Clear` is text, not a glyph, per the text-before-a-glyph rule. */
function SearchField({
  value = '',
  onChange,
  placeholder = 'Search 512 clients',
  onClear,
  autoFocus = false
}) {
  const [focus, setFocus] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-8)',
      width: '100%',
      height: 44,
      borderRadius: 'var(--radius-20)',
      background: 'var(--color-surface)',
      padding: '0 14px',
      boxSizing: 'border-box',
      border: `1px solid ${focus ? 'var(--color-bronze)' : 'var(--color-line)'}`,
      boxShadow: focus ? 'var(--focus-ring)' : 'none',
      transition: 'box-shadow var(--dur-press), border-color var(--dur-press)'
    }
  }, /*#__PURE__*/React.createElement("input", {
    value: value,
    autoFocus: autoFocus,
    placeholder: placeholder,
    onChange: e => onChange && onChange(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      flex: 1,
      minWidth: 0,
      border: 'none',
      background: 'transparent',
      outline: 'none',
      padding: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink)'
    },
    className: "ds-search-input"
  }), /*#__PURE__*/React.createElement("style", null, '.ds-search-input::placeholder{color:var(--color-muted)}'), value ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => {
      onClear && onClear();
      onChange && onChange('');
    },
    style: {
      appearance: 'none',
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      flexShrink: 0,
      display: 'flex',
      alignItems: 'center',
      minHeight: 44,
      padding: '0 2px',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 11.5,
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, "Clear") : null);
}
Object.assign(__ds_scope, { SearchField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/SearchField.jsx", error: String((e && e.message) || e) }); }

// components/forms/SelectionMark.jsx
try { (() => {
/* Radio and checkbox forms. These are drawn inside the component that uses them — they are not icons,
   so they take no size or stroke overrides and never appear on their own. 20px box, 1.5px stroke. */
function SelectionMark({
  kind,
  selected = false,
  disabled = false
}) {
  const ring = selected ? 'var(--color-bronze)' : 'var(--color-line)';
  const base = {
    display: 'inline-flex',
    width: 20,
    height: 20,
    flexShrink: 0,
    alignItems: 'center',
    justifyContent: 'center',
    boxSizing: 'border-box',
    opacity: disabled ? 0.4 : 1,
    transition: 'background-color var(--dur-press) var(--ease)'
  };
  if (kind === 'radio') {
    return /*#__PURE__*/React.createElement("span", {
      style: {
        ...base,
        borderRadius: 'var(--radius-full)',
        boxShadow: `inset 0 0 0 1.5px ${ring}`
      }
    }, selected && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 10,
        height: 10,
        borderRadius: 'var(--radius-full)',
        background: 'var(--color-bronze)'
      }
    }));
  }
  return /*#__PURE__*/React.createElement("span", {
    style: {
      ...base,
      borderRadius: 'var(--radius-6)',
      background: selected ? 'var(--color-bronze)' : 'transparent',
      boxShadow: selected ? 'none' : `inset 0 0 0 1.5px ${ring}`
    }
  }, selected && /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-surface)",
    strokeWidth: "2.6",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "m5 12 5 5 9-9"
  })));
}
Object.assign(__ds_scope, { SelectionMark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/SelectionMark.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconArrow.jsx
try { (() => {
/* Path data verbatim from src/imports (Figma export), wrapped as in src/lib/icons.tsx. */
function IconArrow({
  stroke = 'var(--color-surface)',
  size = 18
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: Math.round(size * 0.72),
    height: Math.round(size * 0.72),
    viewBox: "0 0 13.33 11.83",
    fill: "none",
    preserveAspectRatio: "xMidYMid meet"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M0.665 5.915H12.665M7.415 11.165L12.665 5.915L7.415 0.665",
    stroke: stroke,
    strokeWidth: "1.33",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
}
Object.assign(__ds_scope, { IconArrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconArrow.jsx", error: String((e && e.message) || e) }); }

// components/actions/DarkButton.jsx
try { (() => {
/* The dark CTA in the composer-button language: 48px, radius 16, dark gradient. */
function DarkButton({
  label,
  onClick,
  full = true,
  arrow = true
}) {
  const [down, setDown] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    onPointerDown: () => setDown(true),
    onPointerUp: () => setDown(false),
    onPointerLeave: () => setDown(false),
    style: {
      appearance: 'none',
      border: 'none',
      cursor: 'pointer',
      width: full ? '100%' : 'auto',
      display: 'flex',
      height: 48,
      alignItems: 'center',
      justifyContent: 'center',
      gap: 'var(--space-8)',
      borderRadius: 'var(--radius-16)',
      padding: '0 20px',
      background: 'var(--gradient-dark-cta)',
      transform: down ? 'scale(0.98)' : 'none',
      transition: 'transform var(--dur-press) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-14)',
      color: 'var(--color-surface)'
    }
  }, label), arrow && /*#__PURE__*/React.createElement(__ds_scope.IconArrow, null));
}
Object.assign(__ds_scope, { DarkButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/DarkButton.jsx", error: String((e && e.message) || e) }); }

// components/actions/StickyCTA.jsx
try { (() => {
/* C17 · sticky primary CTA wrapper. In v3+ the Dock's cta slot replaces this. */
function StickyCTA({
  label,
  onClick
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 10,
      padding: '6px 16px 8px',
      animation: 'ds-rise var(--dur-screen) var(--ease) both',
      animationDelay: '320ms'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.DarkButton, {
    label: label,
    onClick: onClick
  }));
}
Object.assign(__ds_scope, { StickyCTA });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/StickyCTA.jsx", error: String((e && e.message) || e) }); }

// components/composer/MoneyComposer.jsx
try { (() => {
function formatINR(digits) {
  const clean = String(digits).replace(/\D/g, '');
  if (!clean) return '';
  const n = clean.slice(-3),
    rest = clean.slice(0, -3);
  return (rest ? rest.replace(/\B(?=(\d{2})+(?!\d))/g, ',') + ',' : '') + n;
}
/* C18 · ₹ prefix in a 20px slot, hairline, Indian-grouped digits. */
function MoneyComposer({
  onSend,
  placeholder = 'or type an amount'
}) {
  const [raw, setRaw] = React.useState('');
  const formatted = formatINR(raw);
  const canSend = raw.length > 0;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-20)',
      background: 'var(--color-surface)',
      padding: 'var(--space-12)',
      boxSizing: 'border-box',
      boxShadow: 'var(--shadow-composer), 0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: 20,
      flexShrink: 0,
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-16)',
      color: 'var(--color-bronze-deep)'
    }
  }, "\u20B9")), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '0 10px',
      height: 22,
      width: 1,
      flexShrink: 0,
      background: 'var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("input", {
    value: formatted,
    inputMode: "numeric",
    onChange: e => setRaw(e.target.value.replace(/\D/g, '')),
    onKeyDown: e => e.key === 'Enter' && canSend && onSend && onSend('₹' + formatted),
    placeholder: placeholder,
    className: "ds-composer-input",
    style: {
      width: '100%',
      border: 'none',
      background: 'transparent',
      outline: 'none',
      padding: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-16)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink)'
    }
  }), /*#__PURE__*/React.createElement("style", null, '.ds-composer-input::placeholder{color:var(--color-muted)}')), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)',
      display: 'flex',
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: !canSend,
    onClick: () => canSend && onSend && onSend('₹' + formatted),
    style: {
      appearance: 'none',
      border: 'none',
      display: 'flex',
      width: 42,
      height: 42,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)',
      background: 'var(--gradient-dark-cta)',
      opacity: canSend ? 1 : 0.4,
      cursor: canSend ? 'pointer' : 'default',
      padding: 0
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconArrow, null))));
}
Object.assign(__ds_scope, { formatINR, MoneyComposer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/composer/MoneyComposer.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconAttach.jsx
try { (() => {
/* Path data verbatim from src/imports (Figma export), wrapped as in src/lib/icons.tsx. */
function IconAttach({
  stroke = 'var(--color-muted)',
  size = 18
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: Math.round(size * 0.72),
    height: Math.round(size * 0.72),
    viewBox: "0 0 12.3683 13.7241",
    fill: "none",
    preserveAspectRatio: "xMidYMid meet"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M11.7033 6.89579L6.45329 12.1458C5.79837 12.7495 4.93535 13.0765 4.04483 13.0584C3.15431 13.0402 2.30531 12.6784 1.67548 12.0486C1.04566 11.4188 0.683831 10.5698 0.665715 9.67924C0.647598 8.78872 0.974598 7.9257 1.57829 7.27079L7.57829 1.27079C7.96616 0.882908 8.49224 0.665 9.04079 0.665C9.58933 0.665 10.1154 0.882908 10.5033 1.27079C10.8912 1.65867 11.1091 2.18474 11.1091 2.73329C11.1091 3.28183 10.8912 3.80791 10.5033 4.19579L4.87828 9.82079",
    stroke: stroke,
    strokeWidth: "1.33",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
}
Object.assign(__ds_scope, { IconAttach });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconAttach.jsx", error: String((e && e.message) || e) }); }

// components/composer/Composer.jsx
try { (() => {
/* The ask bar — present on every screen. Bronze focus ring, 150ms. streaming = Stop in the send slot. */
function Composer({
  value = '',
  onChange,
  onFocus,
  onSend,
  placeholder = 'Ask Sentinel about a client, a fund, or a plan',
  autoFocus = false,
  streaming = false,
  onStop
}) {
  const [focus, setFocus] = React.useState(false);
  const canSend = value.trim().length > 0 && !streaming;
  const btn = {
    appearance: 'none',
    border: 'none',
    cursor: 'pointer',
    display: 'flex',
    width: 42,
    height: 42,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 'var(--radius-full)',
    background: 'var(--gradient-dark-cta)',
    padding: 0
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-20)',
      background: 'var(--color-surface)',
      padding: 'var(--space-12)',
      boxSizing: 'border-box',
      border: `1px solid ${focus ? 'var(--color-bronze)' : 'var(--color-line)'}`,
      boxShadow: focus ? 'var(--focus-ring)' : 'var(--shadow-composer)',
      transition: 'box-shadow var(--dur-press), border-color var(--dur-press)'
    }
  }, /*#__PURE__*/React.createElement("input", {
    value: value,
    autoFocus: autoFocus,
    onChange: e => onChange && onChange(e.target.value),
    onFocus: () => {
      setFocus(true);
      onFocus && onFocus();
    },
    onBlur: () => setFocus(false),
    onKeyDown: e => e.key === 'Enter' && canSend && onSend && onSend(),
    placeholder: placeholder,
    style: {
      width: '100%',
      border: 'none',
      background: 'transparent',
      outline: 'none',
      padding: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink)'
    },
    className: "ds-composer-input"
  }), /*#__PURE__*/React.createElement("style", null, '.ds-composer-input::placeholder{color:var(--color-muted)}'), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      paddingRight: 'var(--space-2)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: 42,
      height: 42,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 22,
      background: 'var(--color-surface)',
      boxShadow: '0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconAttach, null)), streaming ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onStop,
    style: btn
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 13,
      height: 13,
      borderRadius: 3,
      background: 'var(--color-surface)'
    }
  })) : /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => canSend && onSend && onSend(),
    disabled: !canSend,
    style: {
      ...btn,
      opacity: canSend ? 1 : 0.4,
      cursor: canSend ? 'pointer' : 'default'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconArrow, null))));
}
Object.assign(__ds_scope, { Composer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/composer/Composer.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconCheckCircle.jsx
try { (() => {
/* Lucide "circle-check" (ISC). 24px grid, stroke 1.5 at 20px rendered — Lucide's 2px default is too heavy
   beside a 1px hairline divider. Geometry unmodified. */
function IconCheckCircle({
  stroke = 'var(--color-status-ok-fg)',
  size = 20
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: stroke,
    strokeWidth: "1.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"
  }), /*#__PURE__*/React.createElement("path", {
    d: "m9 12 2 2 4-4"
  })));
}
Object.assign(__ds_scope, { IconCheckCircle });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconCheckCircle.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconChevronRight.jsx
try { (() => {
/* Path data verbatim from src/imports (Figma export), wrapped as in src/lib/icons.tsx. */
function IconChevronRight({
  stroke = 'var(--color-bronze)',
  size = 15
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: Math.round(size * 0.73),
    height: Math.round(size * 0.73),
    viewBox: "0 0 15 15",
    fill: "none",
    preserveAspectRatio: "xMidYMid meet"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M5.625 11.25L9.375 7.5L5.625 3.75",
    stroke: stroke,
    strokeWidth: "1.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
}
Object.assign(__ds_scope, { IconChevronRight });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconChevronRight.jsx", error: String((e && e.message) || e) }); }

// components/actions/SuggestionRow.jsx
try { (() => {
/* Suggestion row with chevron, 42px, hairline divider. */
function SuggestionRow({
  label,
  onClick,
  last = false
}) {
  const [down, setDown] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    onPointerDown: () => setDown(true),
    onPointerUp: () => setDown(false),
    onPointerLeave: () => setDown(false),
    style: {
      appearance: 'none',
      border: 'none',
      cursor: 'pointer',
      padding: 0,
      display: 'flex',
      height: 42,
      width: '100%',
      alignItems: 'center',
      justifyContent: 'space-between',
      textAlign: 'left',
      background: down ? 'var(--tint-bronze-06)' : 'transparent',
      borderBottom: last ? 'none' : '0.5px solid var(--color-line-soft)',
      transform: down ? 'scale(0.98)' : 'none',
      transition: 'transform var(--dur-press) var(--ease), background-color var(--dur-press)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-18)',
      color: 'var(--color-ink)'
    }
  }, label), /*#__PURE__*/React.createElement(__ds_scope.IconChevronRight, null));
}
Object.assign(__ds_scope, { SuggestionRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/SuggestionRow.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconDownload.jsx
try { (() => {
/* Lucide "download" (ISC). 24px grid, stroke 1.5 at 20px rendered — Lucide's 2px default is too heavy
   beside a 1px hairline divider. Geometry unmodified. */
function IconDownload({
  stroke = 'var(--color-bronze-deep)',
  size = 20
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: stroke,
    strokeWidth: "1.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"
  }), /*#__PURE__*/React.createElement("path", {
    d: "m7 10 5 5 5-5"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M12 15V3"
  })));
}
Object.assign(__ds_scope, { IconDownload });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconDownload.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconFile.jsx
try { (() => {
/* Lucide "file" (ISC). 24px grid, stroke 1.5 at 20px rendered — Lucide's 2px default is too heavy
   beside a 1px hairline divider. Geometry unmodified. */
function IconFile({
  stroke = 'var(--color-muted)',
  size = 20
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: stroke,
    strokeWidth: "1.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M14 2v4a2 2 0 0 0 2 2h4"
  })));
}
Object.assign(__ds_scope, { IconFile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconFile.jsx", error: String((e && e.message) || e) }); }

// components/forms/FileUpload.jsx
try { (() => {
/* The file, in the thread, parsing in stages.

   The field standard for document parsing is a per-stage list, not one progress bar, because the
   stages are what the reader cares about: Alan shows `✓ Document detection` then `••• Anti-fraud
   analysis`; Monese/Veriff tick four stages with a spinner on the active one; Airwallex shows the
   file row with a determinate line. Notion is closest to Sentinel's shape — the file sits as a chip
   in the thread above the answer, with a follow-up beside it.

   This is why StepTrace earns twice: the stages ARE the success summary arriving progressively, which
   is already the system's rule ("not 'uploaded successfully' but 'read her September statement · 4
   pages · 12 holdings'"). So there is no separate success state to design — when the last stage
   ticks, the summary line IS the outcome, and the trace collapses to it.

   A failed stage keeps its place and carries its own retry, so an advisor retries the stage that
   failed rather than re-uploading a 40-page statement. */
function FileUpload({
  file,
  stages = [],
  state = 'parsing',
  summary,
  onRetry,
  onRemove,
  actions
}) {
  const failed = state === 'failed' || stages.some(s => s.state === 'failed');
  const done = state === 'done';
  const withRetry = stages.map(s => s.state === 'failed' && onRetry ? {
    ...s,
    retry: /*#__PURE__*/React.createElement(__ds_scope.Pill, {
      label: "Retry this step",
      size: "sm",
      tone: "tertiary",
      onClick: () => onRetry(s.label)
    })
  } : s);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-card)',
      padding: 'var(--space-14)',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 'var(--space-10)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexShrink: 0,
      width: 32,
      height: 32,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-8)',
      background: 'var(--color-chip)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconFile, {
    size: 16
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-2)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      font: 'var(--type-body-strong-font)',
      color: 'var(--color-ink)'
    }
  }, file.name), /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, file.meta)), onRemove && /*#__PURE__*/React.createElement(__ds_scope.Pill, {
    label: "Remove",
    size: "sm",
    tone: "muted",
    onClick: onRemove
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.StepTrace, {
    dense: true,
    id: `parse-${file.name.replace(/\W+/g, '-')}`,
    defaultOpen: !done,
    steps: withRetry,
    summary: done ? summary || 'Read it' : failed ? 'Could not read all of it' : undefined
  })), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)'
    }
  }, actions));
}
Object.assign(__ds_scope, { FileUpload });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/FileUpload.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconInfo.jsx
try { (() => {
/* Lucide "info" (ISC). 24px grid, stroke 1.5 — matches the other four additions. Geometry unmodified.
   This glyph is the one exception to the text-before-a-glyph rule, and only inside InfoDot: ⓘ beside a
   figure is a convention an advisor already reads, and the words it would otherwise need ("what is
   this?") would crowd the figure it is attached to. */
function IconInfo({
  stroke = 'var(--color-muted)',
  size = 14
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: stroke,
    strokeWidth: "1.5",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M12 16v-4"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M12 8h.01"
  })));
}
Object.assign(__ds_scope, { IconInfo });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconInfo.jsx", error: String((e && e.message) || e) }); }

// components/actions/InfoDot.jsx
try { (() => {
/* THE RULE, as a component: any figure an advisor must defend carries an ⓘ to its explanation.

   Two copy rules came out of the Monzo reading and this is the second one. The first — caveats sit at
   the number, not in a global footer — is a prop on the charts (`caveat`) and on StatTile. This one
   needed a control, because a caveat is a sentence and an explanation is a sheet.

   It opens the ExplainerSheet the system already has. It never opens a tooltip: a tooltip at 375pt is
   dismissed by the next tap and cannot hold a formula, a date range and a source, which is exactly
   what "CAGR 1Y" needs behind it. Shopee puts one of these on every stat in a fund detail; ours goes
   on any figure whose basis an advisor would be asked for.

   44pt target around a 14px glyph, from Pressable. The accessible name names the FIGURE, not the
   icon: "How is CAGR 1Y worked out?" rather than "info". */
function InfoDot({
  figure,
  onOpen,
  size = 14,
  stroke = 'var(--color-muted)'
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onOpen,
    label: `How is ${figure} worked out?`,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      verticalAlign: 'middle'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconInfo, {
    size: size,
    stroke: stroke
  }));
}
Object.assign(__ds_scope, { InfoDot });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/InfoDot.jsx", error: String((e && e.message) || e) }); }

// components/cards/InfoCard.jsx
try { (() => {
/* Fund detail, with the anatomy our stat tiles were missing: figure → chart → range row → a stat pair
   where each figure carries its own ⓘ. That is Shopee's fund-detail shape, and two gaps fell out of
   reading it: we had no range control at all, and no way for a figure to explain itself.

   The locked case is the one that matters here. The source of fund performance data is one of the
   three decisions that are not ours, so a performance series with no confirmed source renders
   VISUALLY LOCKED and says so — the radius-12 locked treatment the system already uses — rather than
   drawing a plausible line. An invented number in a wealth tool is worse than a blank. */
function InfoCard({
  name,
  meta,
  figure,
  figureNote,
  series,
  range,
  onRange,
  stats = [],
  onExplain,
  caveat,
  locked = false,
  lockReason
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-card)',
      padding: 'var(--space-14)',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 'var(--space-8)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      font: 'var(--type-title-font)',
      color: 'var(--color-ink)',
      textWrap: 'pretty'
    }
  }, name), meta && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: `var(--space-2) 0 0`,
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, meta))), locked ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)',
      borderRadius: 'var(--radius-12)',
      background: 'var(--color-canvas)',
      boxShadow: `inset 0 0 0 var(--border-1) var(--color-line)`,
      padding: 'var(--space-12)'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      font: 'var(--type-figure-font)',
      color: 'var(--color-data-deemph)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, "\u2014\u2014"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: `var(--space-4) 0 0`,
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)',
      textWrap: 'pretty'
    }
  }, lockReason || 'Locked until the fund performance source is confirmed. Sentinel will not show a figure it cannot attribute.')) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 'var(--space-6)',
      marginTop: 'var(--space-10)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      ...__ds_scope.tabular,
      font: 'var(--type-total-font)',
      color: 'var(--color-bronze-deep)'
    }
  }, figure), figureNote && /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, figureNote)), caveat && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: `var(--space-6) 0 0`,
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)',
      textWrap: 'pretty'
    }
  }, caveat), series && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-10)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ChartLine, {
    series: series,
    width: 311,
    run: false,
    valueFormat: v => `${v}%`
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-10)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.RangePills, {
    value: range,
    onChange: onRange
  }))), stats.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${Math.min(stats.length, 2)}, minmax(0, 1fr))`,
      gap: 'var(--space-8)',
      marginTop: 'var(--space-12)'
    }
  }, stats.map(s => /*#__PURE__*/React.createElement("div", {
    key: s.label,
    style: {
      borderRadius: 'var(--radius-12)',
      background: 'var(--color-canvas)',
      boxShadow: `inset 0 0 0 var(--border-1) var(--color-line)`,
      padding: 'var(--space-10)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-4)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, s.label), /*#__PURE__*/React.createElement(__ds_scope.InfoDot, {
    figure: s.label,
    onOpen: () => onExplain && onExplain(s.label)
  })), /*#__PURE__*/React.createElement("p", {
    style: {
      ...__ds_scope.tabular,
      margin: `var(--space-2) 0 0`,
      font: 'var(--type-row-font)',
      fontWeight: 'var(--weight-bold)',
      color: locked || s.locked ? 'var(--color-data-deemph)' : 'var(--color-ink)'
    }
  }, locked || s.locked ? '——' : s.value)))));
}
Object.assign(__ds_scope, { InfoCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/InfoCard.jsx", error: String((e && e.message) || e) }); }

// components/data/StatTile.jsx
try { (() => {
/* V5 · stat tile. Label sentence-case, value in the display face, one qualifying line beneath.
   Never a one-bar bar chart. `locked` renders a supplied-by-someone-else figure.
   v11: `onExplain` puts an ⓘ beside the label, and `caveat` puts the rounding or basis note AT the
   number instead of in a page footer — the two Monzo rules, at the two scales they belong to.
   The sparkline stroke moves to bronze-deep: measured, ramp 2 is 2.57 against surface and a 2px line
   at that ratio is not a mark anyone can see. */
function StatTile({
  label,
  value,
  note,
  sparkline,
  locked = false,
  onExplain,
  caveat
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flex: 1,
      minWidth: 0,
      flexDirection: 'column',
      gap: 'var(--space-4)',
      borderRadius: 'var(--radius-12)',
      background: locked ? 'var(--color-chip)' : 'var(--color-surface)',
      padding: `var(--space-10) var(--space-12)`,
      boxShadow: `0 0 0 var(--border-1) var(--color-line)`,
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-4)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-label-font)',
      fontWeight: 'var(--weight-medium)',
      color: 'var(--color-muted)'
    }
  }, label), onExplain && /*#__PURE__*/React.createElement(__ds_scope.InfoDot, {
    figure: label,
    onOpen: onExplain
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-figure-font)',
      color: locked ? 'var(--color-data-deemph)' : 'var(--color-bronze-deep)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, value), sparkline, note && /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-caption-font)',
      color: 'var(--color-muted)'
    }
  }, note), caveat && /*#__PURE__*/React.createElement("span", {
    style: {
      font: 'var(--type-caption-font)',
      color: 'var(--color-data-deemph)',
      textWrap: 'pretty'
    }
  }, caveat));
}
function Sparkline({
  points,
  width = 84,
  height = 18
}) {
  const min = Math.min(...points),
    max = Math.max(...points),
    span = max - min || 1;
  const d = points.map((p, i) => `${i / (points.length - 1) * width},${height - (p - min) / span * height}`).join(' L ');
  return /*#__PURE__*/React.createElement("svg", {
    width: width,
    height: height,
    viewBox: `0 0 ${width} ${height}`,
    fill: "none",
    style: {
      display: 'block',
      marginTop: 'var(--space-2)'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: `M ${d}`,
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }));
}
Object.assign(__ds_scope, { StatTile, Sparkline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/StatTile.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconMenu.jsx
try { (() => {
/* Path data verbatim from src/imports (Figma export), wrapped as in src/lib/icons.tsx. */
function IconMenu({
  stroke = 'var(--color-ink)',
  size = 18
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: Math.round(size * 0.72),
    height: Math.round(size * 0.72),
    viewBox: "0 0 18 18",
    fill: "none",
    preserveAspectRatio: "xMidYMid meet"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M3.0006 3.7494H14.9994M3.0006 9H14.9994M3.0006 14.2506H14.9994",
    stroke: stroke,
    strokeWidth: "1.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
}
Object.assign(__ds_scope, { IconMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconMenu.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconPlus.jsx
try { (() => {
/* Path data verbatim from src/imports (Figma export), wrapped as in src/lib/icons.tsx. */
function IconPlus({
  stroke = 'var(--color-ink)',
  size = 20
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: Math.round(size * 0.70),
    height: Math.round(size * 0.70),
    viewBox: "0 0 17.33 17.33",
    fill: "none",
    preserveAspectRatio: "xMidYMid meet"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M8.665 0.665V16.665M0.665 8.665H16.665",
    stroke: stroke,
    strokeWidth: "1.33",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
}
Object.assign(__ds_scope, { IconPlus });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconPlus.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconSparkle.jsx
try { (() => {
/* Path data verbatim from src/imports (Figma export), wrapped as in src/lib/icons.tsx. */
function IconSparkle({
  stroke = 'var(--color-bronze-deep)',
  size = 18
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: Math.round(size * 0.72),
    height: Math.round(size * 0.72),
    viewBox: "0 0 12.58 12.205",
    fill: "none",
    preserveAspectRatio: "xMidYMid meet"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6.29 0.665L7.715 4.865L11.915 5.915L7.715 7.34L6.29 11.54L4.86501 7.34L0.665005 5.915L4.86501 4.865L6.29 0.665Z",
    stroke: stroke,
    strokeWidth: "1.33",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
}
Object.assign(__ds_scope, { IconSparkle });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconSparkle.jsx", error: String((e && e.message) || e) }); }

// components/chat/SentinelBlock.jsx
try { (() => {
/* Sentinel response block: sparkle + "Sentinel" label, then children. No bubble. */
function SentinelBlock({
  children,
  shimmer = false,
  label = 'Sentinel'
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 'var(--space-10)',
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-8)',
      animation: shimmer ? 'sentinel-shimmer 1200ms ease-in-out infinite' : 'none'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconSparkle, null), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, label)), children);
}
Object.assign(__ds_scope, { SentinelBlock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/SentinelBlock.jsx", error: String((e && e.message) || e) }); }

// components/chat/ProgressTrace.jsx
try { (() => {
/* Step 5 · the real progress trace — steps name real work, then collapse to "Thought for Ns". */
function Circle({
  state
}) {
  if (state === 'done') return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      width: 16,
      height: 16,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-bronze)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "9",
    height: "9",
    viewBox: "0 0 9 9",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M1.8 4.6 3.6 6.4 7.2 2.6",
    stroke: "var(--color-surface)",
    strokeWidth: "1.3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })));
  if (state === 'active') return /*#__PURE__*/React.createElement("span", {
    style: {
      width: 16,
      height: 16,
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-canvas)',
      boxShadow: '0 0 0 2px var(--color-bronze)',
      boxSizing: 'border-box'
    }
  });
  return /*#__PURE__*/React.createElement("span", {
    style: {
      width: 16,
      height: 16,
      borderRadius: 'var(--radius-full)',
      boxShadow: '0 0 0 1px var(--color-line)'
    }
  });
}
function ProgressTrace({
  steps,
  stepMs = 850,
  reasoning,
  onDone,
  autoplay = true,
  initialActive = 0,
  seconds = 3
}) {
  const [active, setActive] = React.useState(initialActive);
  const [done, setDone] = React.useState(false);
  const [collapsed, setCollapsed] = React.useState(false);
  const [secs, setSecs] = React.useState(autoplay ? 0 : seconds);
  React.useEffect(() => {
    if (done || !autoplay) return;
    const iv = setInterval(() => setSecs(s => s + 1), 1000);
    return () => clearInterval(iv);
  }, [done, autoplay]);
  React.useEffect(() => {
    if (!autoplay) return;
    let idx = initialActive,
      t;
    const step = () => {
      idx += 1;
      if (idx >= steps.length) {
        setActive(steps.length);
        setDone(true);
        setCollapsed(true);
        onDone && onDone();
        return;
      }
      setActive(idx);
      t = setTimeout(step, stepMs);
    };
    t = setTimeout(step, stepMs);
    return () => clearTimeout(t);
  }, []);
  const chev = rot => /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none",
    style: {
      transform: rot ? 'rotate(-90deg)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4.5 3 7.5 6l-3 3",
    stroke: "var(--color-muted)",
    strokeWidth: "1.3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }));
  if (done && collapsed) return /*#__PURE__*/React.createElement(__ds_scope.SentinelBlock, null, /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: () => setCollapsed(false),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-6)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-13)',
      color: 'var(--color-muted)'
    }
  }, "Thought for ", secs, "s"), chev(false)));
  return /*#__PURE__*/React.createElement(__ds_scope.SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => done && setCollapsed(true),
    style: {
      appearance: 'none',
      border: 'none',
      background: 'transparent',
      padding: 0,
      cursor: done ? 'pointer' : 'default',
      marginBottom: 'var(--space-10)',
      display: 'flex',
      width: '100%',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-12)',
      color: done ? 'var(--color-muted)' : 'var(--color-ink)'
    }
  }, done ? `Thought for ${secs}s` : 'Working', " \xB7 ", secs, "s"), done && chev(true)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-12)',
      paddingLeft: 'var(--space-2)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 8,
      left: 9,
      top: 8,
      width: 1,
      background: 'var(--color-line)'
    }
  }), steps.map((s, i) => {
    const state = i < active ? 'done' : i === active && !done ? 'active' : done ? 'done' : 'pending';
    return /*#__PURE__*/React.createElement("div", {
      key: s,
      style: {
        position: 'relative',
        zIndex: 10,
        display: 'flex',
        alignItems: 'center',
        gap: 'var(--space-10)',
        opacity: i === active && !done ? 1 : 0.4,
        transition: 'opacity var(--dur-fast) var(--ease)'
      }
    }, /*#__PURE__*/React.createElement(Circle, {
      state: state
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-medium)',
        fontSize: 'var(--text-13)',
        lineHeight: 'var(--leading-18)',
        color: 'var(--color-ink-soft)'
      }
    }, s));
  })), done && reasoning && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)',
      borderLeft: '1px solid var(--color-line)',
      paddingLeft: 'var(--space-12)'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-13)',
      lineHeight: 'var(--leading-19)',
      color: 'var(--color-muted)'
    }
  }, reasoning))));
}
Object.assign(__ds_scope, { ProgressTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/ProgressTrace.jsx", error: String((e && e.message) || e) }); }

// components/chat/SentinelThinking.jsx
try { (() => {
/* Waiting state: shimmering label + three bronze dots on a staggered opacity pulse, 1.2s.
   Opacity, not a bounce — a 6px dot hopping beside text reads as jitter, and the delight budget is
   spent on data arriving, not on waiting. Under prefers-reduced-motion both hold at full opacity. */
function SentinelThinking() {
  return /*#__PURE__*/React.createElement(__ds_scope.SentinelBlock, {
    shimmer: true
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-5)',
      paddingLeft: 1
    }
  }, [0, 1, 2].map(i => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      width: 6,
      height: 6,
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-bronze)',
      animation: `dot-pulse 1200ms var(--ease) ${i * 150}ms infinite`
    }
  }))));
}
Object.assign(__ds_scope, { SentinelThinking });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chat/SentinelThinking.jsx", error: String((e && e.message) || e) }); }

// components/icons/IconSpinner.jsx
try { (() => {
/* Lucide "loader-circle" (ISC). 24px grid, stroke 1.5 at 20px rendered — Lucide's 2px default is too heavy
   beside a 1px hairline divider. Geometry unmodified. */
function IconSpinner({
  stroke = 'var(--color-bronze-deep)',
  size = 20
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flexShrink: 0,
      animation: 'ds-spin 900ms linear infinite'
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes ds-spin{to{transform:rotate(360deg)}}@media (prefers-reduced-motion:reduce){@keyframes ds-spin{from{transform:none}to{transform:none}}}'), /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: stroke,
    strokeWidth: "1.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M21 12a9 9 0 1 1-6.219-8.56"
  })));
}
Object.assign(__ds_scope, { IconSpinner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/icons/IconSpinner.jsx", error: String((e && e.message) || e) }); }

// components/lists/FollowUpRow.jsx
try { (() => {
/* A follow-up that runs past one line is not a pill.

   We specified at most three inline pills, but the real follow-ups in this product are long — "What
   about the 34 tiny funds?" — and a pill that wraps has stopped being a pill: its radius-full ends
   turn into a lozenge, its 44pt target becomes two rows of uneven target, and the chip row's rhythm
   breaks. Three products solve it as a row: Mindvalley groups full-width rows under a label,
   GitHub Copilot uses two-line cards with a title and a description, Agoda uses a leading icon and a
   trailing chevron.

   Sentinel's own language: ListRow's geometry and the bronze chevron, grouped under an Eyebrow, on
   the card surface. Not a pill with a bigger radius — a different vessel for a different length.

   THE RULE, which is what this component is really for: a follow-up that exceeds one line becomes a
   row, not a pill. Pill stays for short follow-ups; this takes the real questions. Rows are inline
   (they belong to the message that produced them and scroll away with it), so they sit in the
   thread under the answer, never in the dock — the dock is for what persists. */
const Chevron = () => /*#__PURE__*/React.createElement("svg", {
  width: "14",
  height: "14",
  viewBox: "0 0 24 24",
  fill: "none",
  "aria-hidden": "true",
  style: {
    display: 'block',
    flexShrink: 0
  }
}, /*#__PURE__*/React.createElement("path", {
  d: "M9 6l6 6-6 6",
  stroke: "var(--color-bronze-deep)",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round"
}));
function FollowUpRow({
  items = [],
  label = "Here's what you can ask next",
  onAsk,
  leading
}) {
  if (!items.length) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: `0 0 var(--space-6)`,
      font: 'var(--type-eyebrow-font)',
      letterSpacing: 'var(--tracking-eyebrow)',
      textTransform: 'uppercase',
      color: 'var(--color-muted)'
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 'var(--radius-12)',
      background: 'var(--color-surface)',
      boxShadow: `inset 0 0 0 var(--border-1) var(--color-line)`,
      padding: `0 var(--space-14)`,
      boxSizing: 'border-box'
    }
  }, items.map((it, i) => {
    const q = typeof it === 'string' ? {
      question: it
    } : it;
    return /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
      key: q.question,
      onClick: () => onAsk && onAsk(q.question),
      label: `Ask: ${q.question}`,
      style: {
        display: 'flex',
        width: '100%',
        minHeight: 'var(--h-row-md)',
        alignItems: 'center',
        gap: 'var(--space-10)',
        padding: `var(--space-10) 0`,
        borderTop: i ? `var(--border-hairline) solid var(--color-line-soft)` : 'none',
        textAlign: 'left'
      }
    }, (q.leading || leading) && /*#__PURE__*/React.createElement("span", {
      style: {
        flexShrink: 0,
        display: 'flex'
      }
    }, q.leading || leading), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0,
        display: 'flex',
        flexDirection: 'column',
        gap: 'var(--space-2)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        font: 'var(--type-body-strong-font)',
        color: 'var(--color-ink)',
        textWrap: 'pretty'
      }
    }, q.question), q.note && /*#__PURE__*/React.createElement("span", {
      style: {
        font: 'var(--type-caption-font)',
        color: 'var(--color-muted)',
        textWrap: 'pretty'
      }
    }, q.note)), /*#__PURE__*/React.createElement(Chevron, null));
  })));
}
Object.assign(__ds_scope, { FollowUpRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/lists/FollowUpRow.jsx", error: String((e && e.message) || e) }); }

// components/lists/ListRow.jsx
try { (() => {
/* One row with constrained pairings — not three list components.
   The legal pairings are enforced here, not left to the caller: an illegal trailing is dropped to the
   variant's default and warned about once. The chevron belongs to `nav` and nowhere else — an affordance
   meaning "goes somewhere" must never sit on something that selects, the same rule that took chevrons
   off answer chips. `static` rows take no onPress and are exempt from the touch-target rule, like Badge. */
const LEGAL = {
  nav: ['chevron', 'meta'],
  select: ['radio'],
  multi: ['checkbox'],
  action: ['menu'],
  static: ['badge', 'meta', 'none']
};
const DEFAULT_TRAILING = {
  nav: 'chevron',
  select: 'radio',
  multi: 'checkbox',
  action: 'menu',
  static: 'none'
};
const warned = {};
function resolveTrailing(variant, trailing) {
  const legal = LEGAL[variant] || LEGAL.static;
  if (!trailing) return DEFAULT_TRAILING[variant] || 'none';
  if (legal.indexOf(trailing) !== -1) return trailing;
  const key = variant + '/' + trailing;
  if (!warned[key]) {
    warned[key] = 1;
    console.warn(`ListRow: trailing="${trailing}" is not legal on variant="${variant}" (legal: ${legal.join(', ')}). Falling back to "${DEFAULT_TRAILING[variant]}".`);
  }
  return DEFAULT_TRAILING[variant];
}
function ListRow({
  variant = 'static',
  size,
  leading = 'none',
  leadingContent,
  index,
  title,
  subtitle,
  meta,
  chip,
  trailing,
  badge,
  selected = false,
  disabled = false,
  onPress,
  onMenu
}) {
  const tr = resolveTrailing(variant, trailing);
  const isStatic = variant === 'static';
  const h = (size || (subtitle ? 'lg' : 'md')) === 'lg' ? 72 : 56;
  const [down, setDown] = React.useState(false);
  const lead = leading === 'none' ? null : /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      width: leading === 'index' ? 20 : 32,
      height: leading === 'index' ? 20 : 32,
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: leading === 'index' ? 'flex-start' : 'center',
      borderRadius: 'var(--radius-full)',
      background: leading === 'avatar' ? 'var(--surface-avatar)' : 'transparent'
    }
  }, leading === 'avatar' ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-bronze-deep)'
    }
  }, leadingContent || (title || '?').trim().charAt(0).toUpperCase()) : leading === 'index' ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-muted)'
    }
  }, index) : leadingContent);
  const trail = tr === 'chevron' ? /*#__PURE__*/React.createElement(__ds_scope.IconChevronRight, {
    size: 16,
    stroke: "var(--color-bronze-deep)"
  }) : tr === 'radio' ? /*#__PURE__*/React.createElement(__ds_scope.SelectionMark, {
    kind: "radio",
    selected: selected,
    disabled: disabled
  }) : tr === 'checkbox' ? /*#__PURE__*/React.createElement(__ds_scope.SelectionMark, {
    kind: "checkbox",
    selected: selected,
    disabled: disabled
  }) : tr === 'badge' && badge ? /*#__PURE__*/React.createElement(__ds_scope.Badge, {
    variant: badge.variant,
    tone: badge.tone
  }, badge.text) : tr === 'meta' && meta ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 11.5,
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)',
      whiteSpace: 'nowrap'
    }
  }, meta) : null;
  const body = /*#__PURE__*/React.createElement(React.Fragment, null, lead, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-2)',
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-6)',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: '0 1 auto',
      minWidth: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink)'
    }
  }, title), chip), subtitle && /*#__PURE__*/React.createElement("span", {
    style: {
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, subtitle)), tr !== 'meta' && meta && /*#__PURE__*/React.createElement("span", {
    style: {
      flexShrink: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 11.5,
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)',
      whiteSpace: 'nowrap'
    }
  }, meta), trail && /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexShrink: 0,
      alignItems: 'center'
    }
  }, trail), tr === 'menu' && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: e => {
      e.stopPropagation();
      onMenu && onMenu();
    },
    disabled: disabled,
    style: {
      appearance: 'none',
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      display: 'flex',
      width: 44,
      height: 44,
      marginRight: -12,
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: 'center',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-16)',
      lineHeight: 1,
      color: 'var(--color-ink)'
    }
  }, "\u22EF"));
  const shared = {
    display: 'flex',
    width: '100%',
    minHeight: h,
    alignItems: 'center',
    gap: 'var(--space-12)',
    padding: '8px 0',
    boxSizing: 'border-box',
    opacity: disabled ? 0.4 : 1
  };
  if (isStatic || !onPress) return /*#__PURE__*/React.createElement("div", {
    style: {
      ...shared,
      background: 'transparent'
    }
  }, body);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onPress,
    disabled: disabled,
    onPointerDown: () => !disabled && setDown(true),
    onPointerUp: () => setDown(false),
    onPointerLeave: () => setDown(false),
    style: {
      ...shared,
      appearance: 'none',
      border: 'none',
      margin: 0,
      cursor: disabled ? 'default' : 'pointer',
      outline: 'none',
      font: 'inherit',
      background: down ? 'var(--tint-bronze-06)' : 'transparent',
      transition: 'background-color var(--dur-press) var(--ease)'
    }
  }, body);
}
Object.assign(__ds_scope, { ListRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/lists/ListRow.jsx", error: String((e && e.message) || e) }); }

// components/shell/CanvasHeader.jsx
try { (() => {
/* A2 artifact-canvas header — ‹ Back to chat · title · ⋯ */
function CanvasHeader({
  title,
  onBack,
  onMenu,
  backLabel = 'Back to chat'
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 10,
      display: 'flex',
      height: 44,
      width: '100%',
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 16px',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onBack,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-4)',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-surface)',
      padding: '8px 12px',
      boxShadow: '0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M7.5 3 4.5 6l3 3",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-12)',
      lineHeight: 1,
      color: 'var(--color-bronze-deep)'
    }
  }, backLabel)), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      position: 'absolute',
      left: '50%',
      transform: 'translateX(-50%)',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-14)',
      color: 'var(--color-ink)'
    }
  }, title), /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onMenu,
    style: {
      display: 'flex',
      width: 40,
      height: 40,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "20",
    height: "20",
    viewBox: "0 0 20 20",
    fill: "none"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "4",
    cy: "10",
    r: "1.4",
    fill: "var(--color-ink)"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "10",
    cy: "10",
    r: "1.4",
    fill: "var(--color-ink)"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "16",
    cy: "10",
    r: "1.4",
    fill: "var(--color-ink)"
  }))));
}
Object.assign(__ds_scope, { CanvasHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/CanvasHeader.jsx", error: String((e && e.message) || e) }); }

// components/shell/HomeIndicator.jsx
try { (() => {
function HomeIndicator({
  tone = 'bronze'
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 10,
      display: 'flex',
      height: 24,
      width: '100%',
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 5,
      width: 134,
      borderRadius: 'var(--radius-full)',
      background: tone === 'dark' ? 'var(--color-ink)' : 'var(--color-bronze)',
      opacity: tone === 'dark' ? 0.8 : 1
    }
  }));
}
Object.assign(__ds_scope, { HomeIndicator });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/HomeIndicator.jsx", error: String((e && e.message) || e) }); }

// components/shell/MotionGuard.jsx
try { (() => {
/* The reduced-motion law, in the bundle rather than only in the stylesheet.
   tokens/effects.css carries the same block, but a consuming project that loads _ds_bundle.js and
   styles its own page would get every animation at full travel — the document promised something the
   build did not keep. This module installs the block from JS the moment the bundle evaluates, so the
   promise holds for anyone who loads the bundle at all. Idempotent by id; the stylesheet version wins
   where both exist, because they say the same thing.
   Keyframes are REDEFINED, not cancelled: every looping or entrance animation lands in its final state
   with an opacity fade and nothing else — no translate, no scale, no rotation, no pulse. */
const REDUCED_MOTION_CSS = `@media (prefers-reduced-motion:reduce){
@keyframes sentinel-shimmer{0%,100%{opacity:1}}
@keyframes dot-pulse{0%,100%{opacity:1}}
@keyframes ds-rise{from{opacity:0}to{opacity:1}}
@keyframes ds-fade{from{opacity:0}to{opacity:1}}
@keyframes ds-grow{from{transform:scaleX(1)}to{transform:scaleX(1)}}
@keyframes ds-artifact{from{opacity:0}to{opacity:1}}
@keyframes ds-sheet{from{opacity:0;transform:none}to{opacity:1;transform:none}}
@keyframes ds-draw{from{stroke-dashoffset:0;opacity:0}to{stroke-dashoffset:0;opacity:1}}
@keyframes ds-draw-on{from{stroke-dashoffset:0;opacity:0}to{stroke-dashoffset:0;opacity:1}}
@keyframes ds-spin{from{transform:none}to{transform:none}}
@keyframes ds-tip-up{from{opacity:0;transform:translateX(-50%)}to{opacity:1;transform:translateX(-50%)}}
@keyframes ds-tip-down{from{opacity:0;transform:translateX(-50%)}to{opacity:1;transform:translateX(-50%)}}
@keyframes kit-slide{from{opacity:0}to{opacity:1}}
@keyframes kit-drawer{from{opacity:0;transform:none}to{opacity:1;transform:none}}
*,*::before,*::after{transition-duration:1ms!important;scroll-behavior:auto!important}
}
/* Pressable's two guarantees, shipped from the bundle for the same reason the reduced-motion block is:
   a consuming project that loads _ds_bundle.js and styles its own page would otherwise get a base
   component whose focus ring and 44pt hit expansion silently do nothing. tokens/effects.css carries
   the identical rules; whichever lands first wins and they say the same thing. */
.ds-pressable{outline:none}
.ds-pressable:focus-visible{outline:var(--border-focus-width) solid var(--border-focus);outline-offset:2px;box-shadow:var(--focus-ring)}
.ds-pressable[data-hit]::before{content:"";position:absolute;left:calc(-1 * var(--hit-x));right:calc(-1 * var(--hit-x));top:calc(-1 * var(--hit-y));bottom:calc(-1 * var(--hit-y))}`;
function installReducedMotion(doc) {
  const d = doc || (typeof document !== 'undefined' ? document : null);
  if (!d || d.getElementById('ds-reduced-motion')) return false;
  const el = d.createElement('style');
  el.id = 'ds-reduced-motion';
  el.textContent = REDUCED_MOTION_CSS;
  d.head.appendChild(el);
  return true;
}
installReducedMotion();
/* Renders nothing. Mount it only where the bundle is loaded lazily and you want the install tied to a
   React tree; loading the bundle already installed it. */
function MotionGuard() {
  React.useEffect(() => {
    installReducedMotion();
  }, []);
  return null;
}
Object.assign(__ds_scope, { REDUCED_MOTION_CSS, installReducedMotion, MotionGuard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/MotionGuard.jsx", error: String((e && e.message) || e) }); }

// components/shell/PhoneFrame.jsx
try { (() => {
/* 375 × 812 device frame, 44px radius. */
function PhoneFrame({
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: 375,
      height: 812,
      overflow: 'hidden',
      borderRadius: 'var(--radius-44)',
      background: 'var(--color-canvas)',
      boxShadow: 'var(--shadow-phone), 0 0 0 1px var(--ring-frame)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      flexDirection: 'column',
      height: '100%',
      width: '100%'
    }
  }, children));
}
Object.assign(__ds_scope, { PhoneFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/PhoneFrame.jsx", error: String((e && e.message) || e) }); }

// components/shell/ScreenBackdrop.jsx
try { (() => {
/* Aura wash + paper texture, sampled from the source frame. */
function ScreenBackdrop() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      overflow: 'hidden',
      background: 'var(--color-canvas)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      backgroundImage: 'var(--paper-texture)',
      backgroundSize: 'var(--paper-texture-size)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: -93,
      top: -105,
      width: 320,
      height: 320,
      borderRadius: '50%',
      background: 'var(--aura-bronze)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      right: -120,
      top: -40,
      width: 320,
      height: 320,
      borderRadius: '50%',
      background: 'var(--aura-shadow)'
    }
  }));
}
Object.assign(__ds_scope, { ScreenBackdrop });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/ScreenBackdrop.jsx", error: String((e && e.message) || e) }); }

// components/shell/ScrollToBottomButton.jsx
try { (() => {
function ScrollToBottomButton({
  show,
  onClick
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 8,
      left: '50%',
      zIndex: 20,
      transform: show ? 'translateX(-50%)' : 'translate(-50%, 8px)',
      opacity: show ? 1 : 0,
      pointerEvents: show ? 'auto' : 'none',
      transition: 'opacity var(--dur-fast) var(--ease), transform var(--dur-fast) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onClick,
    style: {
      display: 'flex',
      width: 36,
      height: 36,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-float), 0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "16",
    viewBox: "0 0 16 16",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M8 3v9M4 8l4 4 4-4",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))));
}
Object.assign(__ds_scope, { ScrollToBottomButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/ScrollToBottomButton.jsx", error: String((e && e.message) || e) }); }

// components/shell/StatusSpacer.jsx
try { (() => {
/* 44px status bar: 9:41, signal, wifi, battery. */
function StatusSpacer() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 10,
      display: 'flex',
      height: 44,
      width: '100%',
      flexShrink: 0,
      alignItems: 'flex-end',
      justifyContent: 'space-between',
      padding: '0 24px 6px',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-13)',
      color: 'var(--color-ink)'
    }
  }, "9:41"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-5)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      gap: 'var(--space-2)'
    }
  }, [6, 9, 12, 15].map(h => /*#__PURE__*/React.createElement("div", {
    key: h,
    style: {
      width: 3,
      height: h,
      borderRadius: 1,
      background: 'var(--color-ink)'
    }
  }))), /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "12",
    viewBox: "0 0 16 12",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M8 2.5C10 2.5 11.8 3.3 13 4.6M8 6C9 6 9.9 6.4 10.5 7M5.5 9.3 8 11l2.5-1.7",
    stroke: "var(--color-ink)",
    strokeWidth: "1.3",
    strokeLinecap: "round"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 11,
      width: 22
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      borderRadius: 3,
      border: '1px solid var(--color-ink)',
      opacity: 0.4,
      boxSizing: 'border-box'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 1.5,
      bottom: 1.5,
      left: 1.5,
      right: 5,
      borderRadius: 1,
      background: 'var(--color-ink)'
    }
  }))));
}
Object.assign(__ds_scope, { StatusSpacer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/StatusSpacer.jsx", error: String((e && e.message) || e) }); }

// components/shell/TopBar.jsx
try { (() => {
/* hamburger · Sentinel pill · new-thread */
function TopBar({
  onMenu,
  onNew,
  title = 'Sentinel'
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 10,
      display: 'flex',
      height: 44,
      width: '100%',
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 16px',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onMenu,
    style: {
      display: 'flex',
      width: 40,
      height: 40,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconMenu, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-surface)',
      padding: '12px 16px',
      boxShadow: 'var(--shadow-card), 0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-semibold)',
      fontSize: 'var(--text-14)',
      lineHeight: 1,
      textAlign: 'center',
      color: 'var(--color-ink)'
    }
  }, title)), /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onNew,
    style: {
      display: 'flex',
      width: 44,
      height: 44,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 'var(--radius-full)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.IconPlus, null)));
}
Object.assign(__ds_scope, { TopBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/TopBar.jsx", error: String((e && e.message) || e) }); }

// components/text/DemoFooter.jsx
try { (() => {
/* C19 · demo-data footer (superseded by Provenance in v4, still exported). */
function DemoFooter({
  text = 'Demo data — Meera Nair is an example. No real portfolio is shown.'
}) {
  return /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      width: '100%',
      textAlign: 'center',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-16)',
      color: 'var(--color-muted)'
    }
  }, text);
}
Object.assign(__ds_scope, { DemoFooter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/text/DemoFooter.jsx", error: String((e && e.message) || e) }); }

// components/text/Eyebrow.jsx
try { (() => {
/* Small-caps grey eyebrow — column headers, meta, dividers. */
function Eyebrow({
  children,
  color = 'var(--color-muted)',
  style
}) {
  return /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-11)',
      textTransform: 'uppercase',
      letterSpacing: '0.08em',
      color,
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/text/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/cards/ArtifactCard.jsx
try { (() => {
/* A1 · the artifact card in the thread — and now the artifact's ONLY surface. There is no canvas:
   the card expands and collapses in place, so there is nowhere to go back from.

   CONTRACT — no nested scroll. An expanded card takes its content's natural height and the THREAD
   carries the scroll. Never set overflow:auto/scroll on this card or on `children`, and never cap it
   with a maxHeight. A scroller inside the scrolling thread is the pattern the old full-bleed canvas
   existed to avoid, and re-introducing it here is the one way this component stops feeling good.
   Peek clips its preview to 96px (hidden, not scrollable); expanded clips nothing.

   PEEK IS 96px, FIXED — recognition, not reading. What fits there is a sparkline strip (plot 72,
   one end-label row 14, no axis ticks, no gridlines, no legend), a single stat row, or a table's top
   three rows plus a "+40 more" line. Never a chart with axes: the thread is 463–497px, so one taller
   card would eat half of it and two would fill it. Axes, legend and the 180px plot belong to expanded.

   Two scroll behaviours belong to the caller, not the card: on expand, scroll the thread so the
   card's header sits just under the app bar; on collapse, scroll back to the card's position. */
const Chevron = ({
  up
}) => /*#__PURE__*/React.createElement("svg", {
  width: "11",
  height: "11",
  viewBox: "0 0 24 24",
  fill: "none",
  "aria-hidden": "true",
  style: {
    display: 'block',
    transform: up ? 'rotate(180deg)' : 'none',
    transition: 'transform var(--dur-screen) var(--ease)'
  }
}, /*#__PURE__*/React.createElement("path", {
  d: "M6 9.5l6 6 6-6",
  stroke: "var(--color-bronze-deep)",
  strokeWidth: "2.4",
  strokeLinecap: "round",
  strokeLinejoin: "round"
}));
function ArtifactCard({
  state = 'peek',
  eyebrow,
  title,
  children,
  provenance,
  onToggle,
  onWhy,
  onShare,
  onMenu,
  expandLabel = 'Expand',
  collapseLabel = 'Collapse'
}) {
  const expanded = state === 'expanded';
  const filling = state === 'filling';
  const toggle = onToggle;
  const slot = {
    flex: 1,
    display: 'flex',
    alignItems: 'center',
    height: 44
  };
  const label = (t, strong) => /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-12)',
      color: strong ? 'var(--color-bronze-deep)' : 'var(--color-muted)'
    }
  }, t);
  const titleEl = /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-16)',
      lineHeight: 'var(--leading-24)',
      color: 'var(--color-ink)'
    }
  }, title);
  const body = /*#__PURE__*/React.createElement(React.Fragment, null, !expanded && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, eyebrow), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-4)'
    }
  }, titleEl)), /*#__PURE__*/React.createElement("div", {
    style: expanded ? {
      marginTop: 'var(--space-12)'
    } : {
      height: 96,
      marginTop: 'var(--space-10)',
      overflow: 'hidden'
    }
  }, filling ? /*#__PURE__*/React.createElement("div", {
    style: {
      height: 96,
      borderRadius: 'var(--radius-12)',
      background: 'var(--color-track)',
      animation: 'sentinel-shimmer 1200ms ease-in-out infinite'
    }
  }) : children), provenance && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '10px 0 0',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-15)',
      color: 'var(--color-muted)'
    }
  }, provenance));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-card)',
      boxSizing: 'border-box',
      overflow: 'hidden',
      animation: 'ds-artifact 300ms var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement("style", null, '@keyframes ds-artifact{from{opacity:0;transform:scale(.96)}to{opacity:1;transform:none}}@media (prefers-reduced-motion:reduce){@keyframes ds-artifact{from{opacity:0}to{opacity:1}}}'), expanded && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-8)',
      padding: '12px 6px 12px 14px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, eyebrow), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-2)'
    }
  }, titleEl)), /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onMenu,
    style: {
      display: 'flex',
      width: 44,
      height: 44,
      alignItems: 'center',
      justifyContent: 'center',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-16)',
      lineHeight: 1,
      color: 'var(--color-ink)'
    }
  }, "\u22EF"))), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '0 14px',
      height: 'var(--border-hairline)',
      background: 'var(--color-line-soft)'
    }
  })), expanded ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '2px 14px 0'
    }
  }, body) : /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: filling ? undefined : toggle,
    style: {
      display: 'block',
      width: '100%',
      padding: '14px 14px 0',
      textAlign: 'left'
    }
  }, body), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '12px 14px 0',
      height: 'var(--border-hairline)',
      background: 'var(--color-line-soft)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      padding: '0 14px'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: filling ? undefined : toggle,
    style: slot
  }, toggle && !filling ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-5)'
    }
  }, label(expanded ? collapseLabel : expandLabel, true), /*#__PURE__*/React.createElement(Chevron, {
    up: expanded
  })) : null), /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onWhy,
    style: slot
  }, onWhy ? label('Why?') : null), /*#__PURE__*/React.createElement(__ds_scope.Pressable, {
    onClick: onShare,
    style: slot
  }, onShare ? label('Share') : null)));
}
Object.assign(__ds_scope, { ArtifactCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/ArtifactCard.jsx", error: String((e && e.message) || e) }); }

// components/cards/ConstraintCallout.jsx
try { (() => {
/* C11 · the peach callout — carries constraints and bad news. Never a red surface. */
function ConstraintCallout({
  eyebrow,
  body
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-bubble)',
      padding: '12px 14px',
      boxShadow: '0 0 0 1px var(--color-bubble-edge)',
      boxSizing: 'border-box',
      animation: 'ds-rise 260ms var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    color: "var(--color-bronze-deep)"
  }, eyebrow), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '4px 0 0',
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-regular)',
        fontSize: 'var(--text-13)',
        lineHeight: 'var(--leading-19)',
        color: 'var(--color-ink-soft)'
      }
    }
  }, body));
}
Object.assign(__ds_scope, { ConstraintCallout });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/ConstraintCallout.jsx", error: String((e && e.message) || e) }); }

// components/cards/DataTableCard.jsx
try { (() => {
/* C13 · data table card. */
const colStyle = c => ({
  textAlign: c.align === 'right' ? 'right' : 'left',
  ...(c.key === 'name' || c.key === 'what' ? {
    flex: 1,
    minWidth: 0
  } : {
    flexShrink: 0
  })
});
function DataTableCard({
  title,
  meta,
  description,
  columns,
  rows,
  footer,
  filters,
  bar,
  showAll,
  onShowAll
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-card)',
      boxSizing: 'border-box',
      padding: 'var(--space-14)',
      animation: 'ds-rise var(--dur-screen) var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      justifyContent: 'space-between',
      gap: 'var(--space-8)'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-medium)',
        fontSize: 'var(--text-16)',
        lineHeight: 'var(--leading-22)',
        color: 'var(--color-ink)'
      }
    }
  }, title), meta && /*#__PURE__*/React.createElement(__ds_scope.Badge, null, meta)), description && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '6px 0 0',
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-regular)',
        fontSize: 'var(--text-13)',
        lineHeight: 'var(--leading-19)',
        color: 'var(--color-muted)'
      }
    }
  }, description), filters && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-10)'
    }
  }, filters), bar && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)'
    }
  }, bar), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)',
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-8)',
      borderBottom: '0.5px solid var(--color-line-soft)',
      paddingBottom: 'var(--space-6)'
    }
  }, columns.map(c => /*#__PURE__*/React.createElement("div", {
    key: c.key,
    style: colStyle(c)
  }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, c.header)))), rows.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-8)',
      padding: '9px 0',
      borderBottom: i < rows.length - 1 ? '0.5px solid var(--color-line-soft)' : 'none',
      animation: 'ds-fade var(--dur-enter) var(--ease) both',
      animationDelay: `${i * 40}ms`
    }
  }, columns.map(c => /*#__PURE__*/React.createElement("div", {
    key: c.key,
    style: colStyle(c)
  }, r[c.key])))), footer && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '10px 0 0',
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-regular)',
        fontSize: 'var(--text-12)',
        lineHeight: 'var(--leading-17)',
        color: 'var(--color-muted)'
      }
    }
  }, footer), showAll && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onShowAll,
    style: {
      appearance: 'none',
      border: 'none',
      cursor: 'pointer',
      marginTop: 'var(--space-10)',
      width: '100%',
      borderRadius: 'var(--radius-12)',
      background: 'var(--color-chip)',
      padding: '9px 0',
      boxShadow: '0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-bold)',
      fontSize: 'var(--text-12)',
      color: 'var(--color-bronze-deep)'
    }
  }, showAll)));
}
Object.assign(__ds_scope, { DataTableCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/DataTableCard.jsx", error: String((e && e.message) || e) }); }

// components/cards/DisclosureBlock.jsx
try { (() => {
/* Locked disclosure — peach callout with a lock glyph. */
function DisclosureBlock({
  text = 'Mutual fund investments are subject to market risk. Read all scheme-related documents carefully. Past performance is not indicative of future returns.',
  eyebrow = 'Required disclosure · locked'
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-bubble)',
      padding: '12px 14px',
      boxShadow: '0 0 0 1px var(--color-bubble-edge)',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 'var(--space-5)',
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-6)'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "2.5",
    y: "5",
    width: "7",
    height: "5",
    rx: "1",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.1"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M4 5V4a2 2 0 0 1 4 0v1",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.1"
  })), /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    color: "var(--color-bronze-deep)"
  }, eyebrow)), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      ...{
        fontFamily: 'var(--font-ui)',
        fontWeight: 'var(--weight-regular)',
        fontSize: 'var(--text-12)',
        lineHeight: 'var(--leading-17)',
        color: 'var(--color-ink-soft)'
      }
    }
  }, text));
}
Object.assign(__ds_scope, { DisclosureBlock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/DisclosureBlock.jsx", error: String((e && e.message) || e) }); }

// components/cards/RejectCallout.jsx
try { (() => {
/* Bucket-2 rejection: peach callout + the ways out as chips. */
function RejectCallout({
  eyebrow,
  body,
  chips = [],
  onChip
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-10)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ConstraintCallout, {
    eyebrow: eyebrow,
    body: body
  }), chips.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'var(--space-8)'
    }
  }, chips.map(c => /*#__PURE__*/React.createElement(__ds_scope.Pill, {
    key: c,
    label: c,
    onClick: () => onChip && onChip(c)
  }))));
}
Object.assign(__ds_scope, { RejectCallout });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/cards/RejectCallout.jsx", error: String((e && e.message) || e) }); }

// components/data/ProgressRail.jsx
try { (() => {
/* C1 · 2px journey rail, right-aligned "Question n of total".
   `dim` is the detour signal and it dims the RAIL ONLY — the label always holds full opacity.
   Never wrap this component in a parent `opacity`: the parent bleeds into the label and
   "QUESTION 12 OF 12" then reads fainter than "QUESTION 1 OF 12" for no stated reason. */
function ProgressRail({
  n,
  total,
  dim = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      padding: '2px 16px 6px',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 'var(--space-5)',
      display: 'flex',
      justifyContent: 'flex-end',
      opacity: 1
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, "Question ", n, " of ", total)), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 2,
      width: '100%',
      overflow: 'hidden',
      borderRadius: 'var(--radius-full)',
      background: 'var(--color-line)',
      opacity: dim ? 0.4 : 1,
      transition: 'opacity var(--dur-fast) var(--ease)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: '100%',
      borderRadius: 'var(--radius-full)',
      background: 'var(--gradient-rail)',
      transformOrigin: 'left',
      transform: `scaleX(${n / total})`,
      transition: 'transform var(--dur-screen) var(--ease)'
    }
  })));
}
Object.assign(__ds_scope, { ProgressRail });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/ProgressRail.jsx", error: String((e && e.message) || e) }); }

// components/lists/List.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The wrapper: a header eyebrow, dividers, an optional footer row ("See all 43"), grouping, and an
   empty state that is REQUIRED — every list in this product is empty on someone's first day, and a
   list that renders nothing is a bug report waiting to be filed.
   One scroll: a List never scrolls independently inside a scrolling surface. Cap it and give it a
   footer row instead — that is why the drawer's sections are capped at 3 / 7 / 8 with "See all". */
const GROUP_KEY = {
  client: 'client',
  date: 'date',
  journey: 'journey'
};
function List({
  items,
  children,
  dividers = 'inset',
  header,
  footer,
  groupBy = 'none',
  emptyState,
  loading = false,
  rowProps
}) {
  const rows = items || null;
  const isEmpty = !loading && (rows ? rows.length === 0 : React.Children.count(children) === 0);
  const line = inset => /*#__PURE__*/React.createElement("div", {
    style: {
      height: 'var(--border-hairline)',
      marginLeft: inset ? 12 : 0,
      background: 'var(--color-line-soft)'
    }
  });
  const withDividers = nodes => nodes.reduce((acc, node, i) => {
    if (i > 0 && dividers !== 'none') acc.push(/*#__PURE__*/React.createElement("div", {
      key: 'd' + i,
      style: {
        height: 0.5,
        marginLeft: dividers === 'inset' ? 12 : 0,
        background: 'var(--color-line-soft)'
      }
    }));
    acc.push(node);
    return acc;
  }, []);
  const renderRows = () => {
    if (loading) return withDividers([0, 1, 2].map(i => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        minHeight: 56,
        alignItems: 'center',
        gap: 'var(--space-12)',
        padding: '8px 0'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        height: 12,
        width: 40 + i * 30 + '%',
        borderRadius: 'var(--radius-6)',
        background: 'var(--color-track)',
        animation: 'sentinel-shimmer 1200ms var(--ease) infinite',
        animationDelay: i * 120 + 'ms'
      }
    }))));
    if (!rows) return withDividers(React.Children.toArray(children));
    if (groupBy === 'none') return withDividers(rows.map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.ListRow, _extends({
      key: r.id || i
    }, rowProps, r))));
    const key = GROUP_KEY[groupBy];
    const order = [];
    const buckets = {};
    rows.forEach(r => {
      const g = r[key] || 'Other';
      if (!buckets[g]) {
        buckets[g] = [];
        order.push(g);
      }
      buckets[g].push(r);
    });
    return order.map((g, gi) => /*#__PURE__*/React.createElement("div", {
      key: g
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        padding: gi === 0 ? '0 0 6px' : '14px 0 6px'
      }
    }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, g)), withDividers(buckets[g].map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.ListRow, _extends({
      key: r.id || i
    }, rowProps, r))))));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, header && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 0 6px'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, header)), /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 'var(--radius-16)',
      background: 'var(--color-surface)',
      boxShadow: '0 0 0 1px var(--color-line)',
      padding: '0 12px',
      boxSizing: 'border-box'
    }
  }, isEmpty ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 2px'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-medium)',
      fontSize: 'var(--text-14)',
      lineHeight: 'var(--leading-20)',
      color: 'var(--color-ink)'
    }
  }, emptyState.title), emptyState.body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: '4px 0 0',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-12)',
      lineHeight: 'var(--leading-17)',
      color: 'var(--color-muted)'
    }
  }, emptyState.body), emptyState.action && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'var(--space-12)'
    }
  }, emptyState.action)) : renderRows(), footer && !isEmpty && /*#__PURE__*/React.createElement(React.Fragment, null, line(false), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      minHeight: 44,
      alignItems: 'center'
    }
  }, footer))));
}
Object.assign(__ds_scope, { List });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/lists/List.jsx", error: String((e && e.message) || e) }); }

// components/text/EyebrowDivider.jsx
try { (() => {
/* C9 · hairline — eyebrow — hairline */
function EyebrowDivider({
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      width: '100%',
      alignItems: 'center',
      gap: 'var(--space-10)',
      padding: '2px 0'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 'var(--border-hairline)',
      flex: 1,
      background: 'var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, children), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 'var(--border-hairline)',
      flex: 1,
      background: 'var(--color-line)'
    }
  }));
}
Object.assign(__ds_scope, { EyebrowDivider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/text/EyebrowDivider.jsx", error: String((e && e.message) || e) }); }

// components/text/Provenance.jsx
try { (() => {
/* v4 §6 — every figure names where it came from. */
function Provenance({
  text
}) {
  return /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-15)',
      color: 'var(--color-muted)'
    }
  }, text);
}
Object.assign(__ds_scope, { Provenance });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/text/Provenance.jsx", error: String((e && e.message) || e) }); }

// components/text/StandingDisclosure.jsx
try { (() => {
/* Persistent under-composer disclosure — visually locked, de-emphasised grey.
   One line, deliberately: it repeats on all 27 artboards, so it is the heaviest repeated element in
   the product. [PLACEHOLDER — compliance to supply] — this wording is a stand-in until compliance
   supplies the final line; whatever they supply must still fit one line at 11px inside 343px. */
function StandingDisclosure({
  text = 'Sentinel assists an advisor · not investment advice'
}) {
  return /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: '0 8px',
      textAlign: 'center',
      fontFamily: 'var(--font-ui)',
      fontWeight: 'var(--weight-regular)',
      fontSize: 'var(--text-11)',
      lineHeight: 'var(--leading-15)',
      color: 'var(--color-data-deemph)'
    }
  }, text);
}
Object.assign(__ds_scope, { StandingDisclosure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/text/StandingDisclosure.jsx", error: String((e && e.message) || e) }); }

// components/shell/Dock.jsx
try { (() => {
/* The three-row dock: chips → CTA → composer. The CTA never replaces the composer. */
function Dock({
  chips,
  cta,
  composer,
  disclosure = true
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 10,
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-8)',
      padding: '6px 16px 8px'
    }
  }, chips, cta, composer, disclosure && /*#__PURE__*/React.createElement(__ds_scope.StandingDisclosure, null));
}
Object.assign(__ds_scope, { Dock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/shell/Dock.jsx", error: String((e && e.message) || e) }); }

// pages/page-kit.jsx
try { (() => {
/* The component-page kit — the nine blocks, in the one order, so every page reads identically and a
   reviewer builds a habit instead of hunting. Blocks, in order:
   1 Specimen · 2 Anatomy · 3 Variants · 4 States · 5 In context · 6 Tokens · 7 Props · 8 Do/Don't · 9 Motion
   Rules the kit enforces rather than asks for: the specimen sits alone on the product background;
   anatomy annotates OUTSIDE the frame (B/05's rule); the Tokens block resolves each name against the
   live stylesheet, so a value that is not a token shows up as missing; Props prints the real .d.ts. */
const {
  useState,
  useEffect
} = React;
const FONT = 'var(--font-ui)';
const lbl = {
  fontFamily: FONT,
  fontWeight: 700,
  fontSize: 10,
  letterSpacing: '0.08em',
  textTransform: 'uppercase',
  color: 'var(--color-muted)',
  margin: 0
};
const note = {
  fontFamily: FONT,
  fontWeight: 400,
  fontSize: 12,
  lineHeight: '17px',
  color: 'var(--color-muted)',
  margin: '6px 0 0',
  maxWidth: 560
};
function PageShell({
  name,
  group,
  status,
  summary,
  requires = [],
  children
}) {
  const tone = status === 'shipped' ? 'ok' : status === 'building' ? 'under' : 'over';
  const ns = window.SentinelDesignSystem_0682a2 || {};
  const missing = requires.filter(n => !ns[n]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 980,
      margin: '0 auto',
      padding: '28px 24px 64px',
      display: 'flex',
      flexDirection: 'column',
      gap: 28
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: FONT,
      fontWeight: 600,
      fontSize: 26,
      lineHeight: '32px',
      color: 'var(--color-ink)'
    }
  }, name), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      borderRadius: 9999,
      padding: '3px 8px',
      background: `var(--color-status-${tone}-bg)`,
      color: `var(--color-status-${tone}-fg)`,
      fontFamily: FONT,
      fontWeight: 700,
      fontSize: 10,
      textTransform: 'uppercase',
      letterSpacing: '0.04em'
    }
  }, status), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FONT,
      fontWeight: 500,
      fontSize: 11.5,
      color: 'var(--color-muted)'
    }
  }, group), /*#__PURE__*/React.createElement("a", {
    href: "./00-Index.html",
    style: {
      marginLeft: 'auto',
      fontFamily: FONT,
      fontWeight: 700,
      fontSize: 11.5,
      color: 'var(--color-bronze-deep)',
      textDecoration: 'none'
    }
  }, "00 Index \u203A")), /*#__PURE__*/React.createElement("p", {
    style: {
      ...note,
      margin: 0,
      fontSize: 13,
      lineHeight: '19px',
      color: 'var(--color-ink-soft)'
    }
  }, summary)), missing.length ? /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 12,
      background: 'var(--color-status-over-bg)',
      padding: 16
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: FONT,
      fontWeight: 700,
      fontSize: 11,
      textTransform: 'uppercase',
      letterSpacing: '0.04em',
      color: 'var(--color-status-over-fg)'
    }
  }, "Not in the bundle yet"), /*#__PURE__*/React.createElement("p", {
    style: {
      ...note,
      color: 'var(--color-ink-soft)'
    }
  }, "This page needs ", missing.map((n, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: n
  }, i ? ', ' : '', /*#__PURE__*/React.createElement("code", null, n))), ", which ", /*#__PURE__*/React.createElement("code", null, "_ds_bundle.js"), " does not carry yet. The bundle recompiles from source at the end of a turn \u2014 reload then. The page is not broken; the build is behind it.")) : /*#__PURE__*/React.createElement(PageBoundary, null, children));
}
/* A page whose component has gone says so, the way the index does — never a blank screen. */
class PageBoundary extends React.Component {
  constructor(p) {
    super(p);
    this.state = {
      err: null
    };
  }
  static getDerivedStateFromError(err) {
    return {
      err
    };
  }
  render() {
    if (!this.state.err) return this.props.children;
    return /*#__PURE__*/React.createElement("div", {
      style: {
        borderRadius: 12,
        background: 'var(--color-status-over-bg)',
        padding: 16
      }
    }, /*#__PURE__*/React.createElement("p", {
      style: {
        margin: 0,
        fontFamily: FONT,
        fontWeight: 700,
        fontSize: 11,
        textTransform: 'uppercase',
        letterSpacing: '0.04em',
        color: 'var(--color-status-over-fg)'
      }
    }, "This page failed to render"), /*#__PURE__*/React.createElement("p", {
      style: {
        ...note,
        color: 'var(--color-ink-soft)'
      }
    }, String(this.state.err.message || this.state.err)));
  }
}
function Block({
  n,
  title,
  sub,
  children,
  frame = false
}) {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
    style: lbl
  }, String(n).padStart(2, '0'), " \xB7 ", title), sub && /*#__PURE__*/React.createElement("p", {
    style: note
  }, sub)), /*#__PURE__*/React.createElement("div", {
    style: frame ? {
      borderRadius: 16,
      background: 'var(--color-canvas)',
      boxShadow: 'inset 0 0 0 1px var(--color-line)',
      padding: 24
    } : undefined
  }, children));
}
/* 1 — the component alone, at real size, on the product background. Nothing else in frame. */
function Specimen({
  children,
  width = 375
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'center',
      borderRadius: 16,
      background: 'var(--color-canvas)',
      boxShadow: 'inset 0 0 0 1px var(--color-line)',
      padding: '32px 24px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width,
      maxWidth: '100%'
    }
  }, children));
}
/* 2 — the same specimen with dimensions annotated OUTSIDE the frame, per B/05.
   Side tags are anchored at left:100% / right:100% so they grow AWAY from the specimen. Anchoring them
   with right:-90 (the first version) pins the tag's right edge 90px outside the frame and lets the box
   grow leftwards instead — measured, a 245px annotation computed left:156px and laid 155px of itself
   across a 311px specimen, over the legend's own value column. Any annotation wider than the gutter
   did it; this one was just long enough to be seen. Tags wrap inside a fixed gutter width rather than
   nowrapping, so length cannot push them anywhere. */
const GUTTER = 128;
function Anatomy({
  children,
  width = 343,
  top,
  bottom,
  left,
  right
}) {
  const tick = {
    position: 'absolute',
    background: 'var(--color-bronze)',
    opacity: 0.5
  };
  const tag = {
    position: 'absolute',
    fontFamily: FONT,
    fontWeight: 700,
    fontSize: 10,
    lineHeight: '14px',
    color: 'var(--color-bronze-deep)'
  };
  const side = {
    ...tag,
    width: GUTTER - 24,
    textWrap: 'pretty'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'center',
      borderRadius: 16,
      background: 'var(--color-canvas)',
      boxShadow: 'inset 0 0 0 1px var(--color-line)',
      padding: `44px ${GUTTER}px 40px`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width,
      maxWidth: '100%'
    }
  }, children, top && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      ...tick,
      left: 0,
      right: 0,
      top: -14,
      height: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...tag,
      bottom: '100%',
      marginBottom: 18,
      left: 0,
      maxWidth: '100%',
      textWrap: 'pretty'
    }
  }, top)), bottom && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      ...tick,
      left: 0,
      right: 0,
      bottom: -14,
      height: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...tag,
      top: '100%',
      marginTop: 18,
      left: 0,
      maxWidth: '100%',
      textWrap: 'pretty'
    }
  }, bottom)), left && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      ...tick,
      top: 0,
      bottom: 0,
      left: -14,
      width: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...side,
      right: '100%',
      marginRight: 22,
      top: '50%',
      transform: 'translateY(-50%)',
      textAlign: 'right'
    }
  }, left)), right && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      ...tick,
      top: 0,
      bottom: 0,
      right: -14,
      width: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      ...side,
      left: '100%',
      marginLeft: 22,
      top: '50%',
      transform: 'translateY(-50%)'
    }
  }, right))));
}
/* 3 / 4 — labelled grid; States takes only the states that genuinely exist. */
function Grid({
  items,
  cols = 2,
  itemWidth
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${cols}, minmax(0, 1fr))`,
      gap: 20
    }
  }, items.map(it => /*#__PURE__*/React.createElement("div", {
    key: it.label,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      borderRadius: 12,
      background: 'var(--color-canvas)',
      boxShadow: 'inset 0 0 0 1px var(--color-line)',
      padding: 16
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: lbl
  }, it.label), /*#__PURE__*/React.createElement("div", {
    style: {
      width: itemWidth,
      maxWidth: '100%'
    }
  }, it.node), it.note && /*#__PURE__*/React.createElement("p", {
    style: {
      ...note,
      margin: 0,
      fontSize: 11
    }
  }, it.note))));
}
/* 5 — inside a 375pt thread fragment: real width, real neighbours. Catches anything that only works alone. */
function Thread({
  children,
  before,
  after,
  dock
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: 375,
      borderRadius: 24,
      background: 'var(--color-canvas)',
      boxShadow: '0 0 0 1px var(--ring-frame)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      padding: '20px 16px 16px'
    }
  }, before, children, after), dock && /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: '1px solid var(--color-line-soft)',
      padding: '8px 16px 12px'
    }
  }, dock)));
}
/* 6 — every token the component consumes, resolved against the live stylesheet. A name that does not
   resolve renders as MISSING, which is the page failing loudly instead of quietly. */
function Tokens({
  names
}) {
  const [vals, setVals] = useState({});
  useEffect(() => {
    const cs = getComputedStyle(document.documentElement);
    const out = {};
    names.forEach(n => {
      out[n] = cs.getPropertyValue(n).trim();
    });
    setVals(out);
  }, [names.join()]);
  const isColor = v => /^(#|rgb|hsl)/.test(v);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fill, minmax(230px, 1fr))',
      gap: '8px 20px'
    }
  }, names.map(n => /*#__PURE__*/React.createElement("div", {
    key: n,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flexShrink: 0,
      width: 14,
      height: 14,
      borderRadius: 4,
      background: isColor(vals[n]) ? vals[n] : 'transparent',
      boxShadow: 'inset 0 0 0 1px var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("code", {
    style: {
      fontFamily: 'ui-monospace, monospace',
      fontSize: 11,
      color: 'var(--color-ink)',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, n), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: FONT,
      fontWeight: 500,
      fontSize: 10.5,
      color: vals[n] ? 'var(--color-muted)' : 'var(--color-status-over-fg)',
      whiteSpace: 'nowrap'
    }
  }, vals[n] || 'MISSING'))));
}
/* 7 — the .d.ts, printed. Fetched, never retyped, so the page cannot drift from the contract. */
function Props({
  src
}) {
  const [text, setText] = useState('loading…');
  useEffect(() => {
    fetch(src).then(r => r.ok ? r.text() : Promise.reject(r.status)).then(setText).catch(e => setText(`could not read ${src} (${e})`));
  }, [src]);
  return /*#__PURE__*/React.createElement("pre", {
    style: {
      margin: 0,
      padding: 16,
      borderRadius: 12,
      background: 'var(--color-surface)',
      boxShadow: 'inset 0 0 0 1px var(--color-line)',
      fontFamily: 'ui-monospace, monospace',
      fontSize: 11.5,
      lineHeight: '17px',
      color: 'var(--color-ink-soft)',
      overflowX: 'auto',
      whiteSpace: 'pre-wrap'
    }
  }, text);
}
/* 8 — one pair, the sharpest one. A page of eight don'ts teaches nothing. */
function DoDont({
  doNode,
  doCaption,
  dontNode,
  dontCaption
}) {
  const cell = (ok, caption, node) => /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      borderRadius: 12,
      background: 'var(--color-canvas)',
      boxShadow: `inset 0 0 0 1px ${ok ? 'var(--color-status-ok-fg)' : 'var(--color-status-over-fg)'}`,
      padding: 16
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      fontFamily: FONT,
      fontWeight: 700,
      fontSize: 11,
      color: ok ? 'var(--color-status-ok-fg)' : 'var(--color-status-over-fg)'
    }
  }, ok ? 'Do' : "Don't"), /*#__PURE__*/React.createElement("div", null, node), /*#__PURE__*/React.createElement("p", {
    style: {
      ...note,
      margin: 0,
      fontSize: 11.5
    }
  }, caption));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
      gap: 20
    }
  }, cell(true, doCaption, doNode), cell(false, dontCaption, dontNode));
}
/* 9 — the motion rows that apply, each with what it does under reduced motion. */
function Motion({
  rows
}) {
  const th = {
    textAlign: 'left',
    padding: '8px 12px',
    fontFamily: FONT,
    fontWeight: 700,
    fontSize: 10,
    letterSpacing: '0.08em',
    textTransform: 'uppercase',
    color: 'var(--color-muted)',
    borderBottom: '1px solid var(--color-line)'
  };
  const td = {
    padding: '9px 12px',
    fontFamily: FONT,
    fontWeight: 500,
    fontSize: 11.5,
    lineHeight: '16px',
    color: 'var(--color-ink)',
    borderBottom: '0.5px solid var(--color-line-soft)',
    verticalAlign: 'top'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 12,
      background: 'var(--color-surface)',
      boxShadow: 'inset 0 0 0 1px var(--color-line)',
      overflowX: 'auto'
    }
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: '100%',
      borderCollapse: 'collapse'
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", {
    style: th
  }, "Motion"), /*#__PURE__*/React.createElement("th", {
    style: th
  }, "Property"), /*#__PURE__*/React.createElement("th", {
    style: th
  }, "Duration"), /*#__PURE__*/React.createElement("th", {
    style: th
  }, "Under reduced motion"))), /*#__PURE__*/React.createElement("tbody", null, rows.map(r => /*#__PURE__*/React.createElement("tr", {
    key: r.name
  }, /*#__PURE__*/React.createElement("td", {
    style: td
  }, r.name), /*#__PURE__*/React.createElement("td", {
    style: {
      ...td,
      fontFamily: 'ui-monospace, monospace',
      fontSize: 11
    }
  }, r.property), /*#__PURE__*/React.createElement("td", {
    style: td
  }, r.duration), /*#__PURE__*/React.createElement("td", {
    style: {
      ...td,
      color: 'var(--color-muted)'
    }
  }, r.reduced))))));
}
/* Pages mount after d3-scale / d3-shape land, so a chart never renders its fallback maths here. */
function mountPage(App) {
  const go = () => ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));
  if (window.d3 && window.d3.scaleLinear) go();else {
    let done = false;
    const once = () => {
      if (!done) {
        done = true;
        go();
      }
    };
    window.addEventListener('d3ready', once);
    setTimeout(once, 2500);
  }
}
Object.assign(window, {
  PageShell,
  PageBoundary,
  Block,
  Specimen,
  Anatomy,
  Grid,
  Thread,
  Tokens,
  Props,
  DoDont,
  Motion,
  mountPage,
  pageLbl: lbl,
  pageNote: note
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "pages/page-kit.jsx", error: String((e && e.message) || e) }); }

// scripts/build-index.js
try { (() => {
/* Regenerates pages/_index.json by scanning the tree. Run it after adding, renaming or deleting a
   component or a page; the index page renders nothing but this file.
   The index is generated from what is on disk, never from a hand-kept list, so it cannot tell us a
   component is there when it isn't: a row exists because <Name>.jsx and <Name>.d.ts both exist, and
   `shipped` requires pages/<Name>.html to exist too — a component with no page is not in the system.
   `specified` rows are the one exception and they are marked as such: they have no source file by
   definition, so they carry the document that specifies them instead of a path.

   Usage: paste into the project's script runner (it uses ls / readFile / saveFile), or adapt the three
   helpers to node:fs. Scanning + reading 70 sources exceeds a 30s budget in one pass, so it runs in
   three steps and writes pages/_index.json at the end. */
const DIRS = ['actions', 'cards', 'chat', 'composer', 'data', 'forms', 'icons', 'lists', 'shell', 'text'];
const GROUP_BY_DIR = {
  actions: 'Input',
  forms: 'Input',
  cards: 'Chat',
  chat: 'Chat',
  composer: 'Chat',
  data: 'Data',
  icons: 'Foundations',
  lists: 'Navigation',
  shell: 'Foundations',
  text: 'Foundations'
};
/* Where the directory is the wrong answer. Groups are v10 §2.4: Foundations (tokens, type, motion) ·
   Chat (thread, composer, dock, artifact card) · Data (charts, table, overlap, stat tiles) ·
   Input (pill, chip, search, upload) · Navigation (drawer, sheet, rail). */
const OVERRIDE = {
  ArtifactCard: 'Chat',
  AllocationCard: 'Data',
  DataTableCard: 'Data',
  HeroNumberCard: 'Data',
  MoveCard: 'Data',
  Badge: 'Data',
  ConstraintCallout: 'Chat',
  RejectCallout: 'Chat',
  DisclosureBlock: 'Chat',
  ExplainerSheet: 'Navigation',
  Dock: 'Chat',
  TopBar: 'Navigation',
  ScrollToBottomButton: 'Chat',
  CanvasHeader: 'Navigation',
  ProgressRail: 'Navigation',
  ProgressTrace: 'Chat',
  StatTile: 'Data',
  ClientChip: 'Input',
  SearchField: 'Input',
  SelectionMark: 'Input',
  MotionGuard: 'Foundations'
};
/* The backlog: specified in a document, no component on disk. Every row here is visible debt. */
const SPECIFIED = [{
  name: 'FileUpload',
  group: 'Input',
  spec: 'Component request spec · Part 2'
}, {
  name: 'DownloadAction',
  group: 'Input',
  spec: 'Component request spec · Part 2 — a Pill wrapper, unblocked now Pill has loading'
}, {
  name: 'ResultCard',
  group: 'Chat',
  spec: 'Component request spec · Part 4, re-pointed by v9 §1.1'
}, {
  name: 'InfoCard',
  group: 'Data',
  spec: 'Component request spec · Part 6'
}, {
  name: 'DataTable',
  group: 'Data',
  spec: 'Component request spec · Part 7, nudge added by v9 §1.3'
}, {
  name: 'OverlapView',
  group: 'Data',
  spec: 'Component request spec · Part 8 — pairs mode'
}];
const ORDER = ['Foundations', 'Chat', 'Data', 'Input', 'Navigation'];
async function buildIndex({
  ls,
  readFile,
  saveFile,
  today
}) {
  /* The token vocabulary, read from tokens/ rather than assumed — the filter below is only honest if
     this set is the real one. */
  const defined = new Set();
  for (const f of (await ls('tokens')).filter(n => /\.css$/.test(n))) {
    const css = await readFile('tokens/' + f);
    (css.match(/--[a-z0-9-]+\s*:/gi) || []).forEach(d => defined.add(d.replace(/\s*:$/, '')));
  }
  const listing = {};
  for (const d of DIRS) {
    try {
      listing[d] = await ls('components/' + d);
    } catch (e) {
      listing[d] = [];
    }
  }
  const pageFiles = (await ls('pages')).filter(f => /\.html$/.test(f));
  const rows = [];
  for (const dir of DIRS) {
    const files = listing[dir] || [];
    for (const f of files) {
      const m = /^([A-Z][A-Za-z0-9]*)\.jsx$/.exec(f);
      if (!m || !files.includes(m[1] + '.d.ts')) continue; /* no .d.ts, no contract, no row */
      const name = m[1];
      const src = await readFile(`components/${dir}/${f}`);
      const referenced = Array.from(new Set((src.match(/var\(--[a-z0-9-]+\)/g) || []).map(t => t.slice(4, -1)))).sort();
      /* Split what the source references into tokens and component-local custom properties. The column
         is headed "tokens it consumes", so it may only contain names that `tokens/` actually defines;
         a local like Pill's `--hit` (the ≥44px hit-area variable) is real but is not a token, and
         listing it there would be the index claiming something the token layer does not hold. */
      const tokens = referenced.filter(t => defined.has(t));
      const locals = referenced.filter(t => !defined.has(t));
      const page = pageFiles.includes(name + '.html') ? `${name}.html` : null;
      rows.push({
        name,
        group: OVERRIDE[name] || GROUP_BY_DIR[dir],
        status: page ? 'shipped' : 'building',
        page,
        file: `components/${dir}/${f}`,
        tokens,
        locals
      });
    }
  }
  for (const s of SPECIFIED) rows.push({
    ...s,
    status: 'specified',
    page: null,
    file: null,
    tokens: []
  });
  rows.sort((a, b) => ORDER.indexOf(a.group) - ORDER.indexOf(b.group) || a.name.localeCompare(b.name));
  const counts = rows.reduce((o, r) => (o[r.status] = (o[r.status] || 0) + 1, o), {});
  const out = {
    generated: today,
    generator: 'scripts/build-index.js',
    counts,
    rows
  };
  await saveFile('pages/_index.json', JSON.stringify(out, null, 1));
  return out;
}
Object.assign(__ds_scope, { buildIndex });
})(); } catch (e) { __ds_ns.__errors.push({ path: "scripts/build-index.js", error: String((e && e.message) || e) }); }

// ui_kits/journey-a-risk/a-data.jsx
try { (() => {
/* Journey A — Meera Nair only. Copy verbatim from src/journeys.tsx (Risk / 01 … Risk / 16).
   Sixteen steps = intro + twelve questions + two interjections + the result. */
window.DS = window.SentinelDesignSystem_0682a2;
const q = (n, sentinel, chips, opts = {}) => ({
  n,
  sentinel,
  chips,
  ...opts
});
window.A = {
  intro: {
    provenance: 'As of 15 Sep · from her account record and September statement',
    lines: ['Meera Nair, 38, Kochi. Your client since 2019. ₹18.4 L across 43 funds.', 'Her risk profile was never completed, so nothing after it can be checked against anything. Let us fix that first — twelve short questions, mostly one tap each.'],
    chips: [["Let's go", 'primary'], ['Why twelve questions?', 'tertiary'], ['Skip to the result', 'muted']]
  },
  steps: [q(1, 'How old is Meera?', [['Use her KYC age — 38', 'smart'], ['Type it instead']], {
    composer: 'or type an age',
    short: 'How old is Meera?',
    answer: 'Use her KYC age — 38'
  }), q(2, 'How steady is her income?', [['Fixed salary'], ['Salary plus bonus'], ['Own business'], ['Freelance, varies'], ['Rent or pension']], {
    short: 'How steady is her income?',
    answer: 'Freelance, varies',
    subtitled: true
  }), q(3, 'What does she earn a month?', [['From her last ITR — ₹1,80,000', 'smart'], ['₹1,50,000'], ['₹2,00,000']], {
    money: true,
    short: 'What does she earn a month?',
    answer: '₹1,80,000',
    parse: 'understood as ₹1,80,000'
  }), q(4, 'And what does she spend a month?', [['₹75,000'], ['₹95,000'], ['₹1,20,000']], {
    money: true,
    short: 'And what does she spend a month?',
    answer: '₹95,000'
  }), q(5, 'How much has she kept aside for emergencies?', [['₹3,00,000'], ['₹6,00,000'], ['₹10,00,000']], {
    sub: 'Money she could reach by tomorrow.',
    money: true,
    short: 'How much for emergencies?',
    answer: '₹6,00,000'
  }), q(6, 'How many people depend on this income?', [['Nobody'], ['1'], ['2'], ['3'], ['4 or more']], {
    short: 'How many people depend on this?',
    answer: '2'
  }), q(7, 'When will she need this money?', [['Within a year'], ['1 to 3 years'], ['3 to 7 years'], ['7 to 15 years'], ['More than 15 years']], {
    short: 'When will she need this money?',
    answer: '7 to 15 years'
  }), q(8, 'What is she saving towards, in rupees?', [['₹50 lakh'], ['₹1 crore'], ['₹2 crore'], ["She isn't sure yet", 'muted']], {
    sub: 'Without a figure we cannot work out the return she needs, and the risk number.',
    money: true,
    short: 'What is she saving towards?',
    answer: '₹2 crore'
  }), q(9, 'The last time markets fell 20% or more — what did she actually do?', [['Invested more'], ['Stayed put'], ['Sold some'], ['Sold everything']], {
    sub: 'What she did, not what she says she would do.',
    short: 'The last 20% fall — what did she do?',
    answer: 'Stayed put'
  }), q(10, 'How big a fall could she sit through without calling you?', [['Almost none'], ['About 10%'], ['About 20%'], ['About 30%'], ['More than that']], {
    short: 'How big a fall could she sit through?',
    answer: 'About 20%'
  }), q(11, 'What is she asking this money to do?', [['Protect it'], ['Give steady income'], ['Grow steadily'], ['Grow it properly'], ['Push for the most']], {
    short: 'What is this money asked to do?',
    answer: 'Grow steadily'
  }), q(12, 'How well does she understand what she holds?', [['New to this'], ['Knows the basics'], ['Fairly experienced'], ['Works in finance']], {
    sub: 'This limits how complicated the portfolio may get — not how much risk she takes.',
    short: 'How well does she understand it?',
    answer: 'Knows the basics'
  })],
  reflect1: {
    after: 5,
    text: 'That is about six months of her spending. Comfortable — it means a fall in the market will not force her to sell.'
  },
  reflect2: {
    after: 9,
    text: 'Good — that is the most useful answer in the whole set. Saying you can handle a fall and living through one are different things.'
  },
  subtitles: {
    'Fixed salary': 'Same every month',
    'Salary plus bonus': 'Most of it fixed, some of it not',
    'Own business': 'Draws a variable amount from profits',
    'Freelance, varies': 'Income swings more than 25% month to month',
    'Rent or pension': 'Fixed, and not tied to her working'
  },
  result: {
    lines: ['We score three things and take the lowest: what her finances can absorb (71), what she can sit through calmly (54) and what her ₹2 crore goal needs (62).', 'The lowest is what binds. She is a 54, Moderate — her finances could carry more, but she would not sleep through it.'],
    chips: [['How is 54 worked out?', 'tertiary'], ['Share with Meera on WhatsApp', 'outline']],
    cta: 'Build her a portfolio',
    meters: [{
      label: 'What her finances can absorb',
      value: 71
    }, {
      label: 'What she can sit through calmly',
      value: 54,
      binding: true
    }, {
      label: 'What her ₹2 crore goal needs',
      value: 62
    }]
  }
};
window.PLACEHOLDER = {
  thread: 'Ask Sentinel',
  answering: 'or type your answer',
  amount: 'or type an amount',
  result: 'Ask about this'
};
window.f = (weight, size, lh, color) => ({
  margin: 0,
  fontFamily: 'var(--font-ui)',
  fontWeight: weight,
  fontSize: size,
  lineHeight: lh ? lh + 'px' : 'normal',
  color: color || 'var(--color-ink)'
});
window.cardS = {
  width: '100%',
  borderRadius: 16,
  background: 'var(--color-surface)',
  boxShadow: 'var(--shadow-card)',
  boxSizing: 'border-box'
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/journey-a-risk/a-data.jsx", error: String((e && e.message) || e) }); }

// ui_kits/journey-a-risk/a-screens.jsx
try { (() => {
const {
  ScreenBackdrop,
  StatusSpacer,
  TopBar,
  ProgressRail,
  SentinelBlock,
  SentinelText,
  QAPair,
  ParseNote,
  Pill,
  AnswerChip,
  ChipRow,
  Composer,
  MoneyComposer,
  Dock,
  HomeIndicator,
  EyebrowDivider,
  HeroNumberCard,
  DarkButton,
  Provenance
} = window.DS;
function Artboard({
  name,
  sub,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "ab"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ab-name"
  }, name), sub && /*#__PURE__*/React.createElement("div", {
    className: "ab-sub"
  }, sub), /*#__PURE__*/React.createElement("div", {
    className: "frame"
  }, children));
}
const Shell = ({
  children
}) => /*#__PURE__*/React.createElement("div", {
  style: {
    position: 'relative',
    display: 'flex',
    height: '100%',
    width: '100%',
    flexDirection: 'column'
  }
}, /*#__PURE__*/React.createElement(ScreenBackdrop, null), children);
/* Bottom-anchored AND pinned: marginTop auto handles a short thread, the ref pins a long one to the
   newest turn — the live question must never be the thing scrolled out of view. */
function Thread({
  children
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    const el = ref.current;
    if (el) el.scrollTop = el.scrollHeight;
  });
  return /*#__PURE__*/React.createElement("div", {
    ref: ref,
    className: "noscroll",
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      flex: 1,
      flexDirection: 'column',
      overflowY: 'auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      padding: '14px 16px 20px'
    }
  }, children));
}
/* An artboard abbreviates the thread to its last three answered questions — the product keeps all of them,
   but a static frame must show the LIVE question, not the top of a scroll it cannot move. */
const HISTORY_SHOWN = 3;
function History({
  upto
}) {
  const all = A.steps.slice(0, upto);
  const shown = all.slice(-HISTORY_SHOWN);
  const hidden = all.length - shown.length;
  return /*#__PURE__*/React.createElement(React.Fragment, null, hidden > 0 ? /*#__PURE__*/React.createElement("p", {
    style: f(500, 11, 15, 'var(--color-data-deemph)')
  }, hidden + (hidden === 1 ? ' earlier answer' : ' earlier answers'), " \xB7 scroll up") : /*#__PURE__*/React.createElement(QAPair, {
    answer: "Let's go"
  }), shown.map(s => /*#__PURE__*/React.createElement(React.Fragment, {
    key: s.n
  }, /*#__PURE__*/React.createElement(QAPair, {
    question: s.short,
    answer: s.answer
  }), s.parse && /*#__PURE__*/React.createElement(ParseNote, {
    text: s.parse
  }))));
}
const Chips = ({
  step
}) => step.subtitled ? /*#__PURE__*/React.createElement("div", {
  style: {
    display: 'flex',
    flexDirection: 'column',
    gap: 8
  }
}, step.chips.map(([label, tone], i) => /*#__PURE__*/React.createElement(AnswerChip, {
  key: label,
  label: label,
  subtitle: A.subtitles[label],
  variant: tone || 'outline',
  selected: i === 3
}))) : /*#__PURE__*/React.createElement(ChipRow, {
  animate: false
}, step.chips.map(([label, tone]) => /*#__PURE__*/React.createElement(Pill, {
  key: label,
  label: label,
  tone: tone || 'outline'
})));
function A01() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(SentinelText, {
    text: A.intro.lines[0]
  }), /*#__PURE__*/React.createElement(SentinelText, {
    text: A.intro.lines[1],
    weight: "Regular"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(Provenance, {
    text: A.intro.provenance
  })))), /*#__PURE__*/React.createElement(Dock, {
    chips: /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, A.intro.chips.map(([l, t]) => /*#__PURE__*/React.createElement(Pill, {
      key: l,
      label: l,
      tone: t
    }))),
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: PLACEHOLDER.answering
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}
function Question({
  i,
  reflectBefore
}) {
  const step = A.steps[i];
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1
    }
  }, /*#__PURE__*/React.createElement(ProgressRail, {
    n: reflectBefore ? step.n - 1 || 1 : step.n,
    total: 12,
    dim: !!reflectBefore
  })), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(History, {
    upto: reflectBefore ? i : i
  }), reflectBefore ? /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement(SentinelText, {
    text: reflectBefore
  })) : /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(SentinelText, {
    text: step.sentinel
  }), step.sub && /*#__PURE__*/React.createElement("p", {
    style: f(400, 13, 18, 'var(--color-muted)')
  }, step.sub)))), /*#__PURE__*/React.createElement(Dock, {
    chips: reflectBefore ? /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, /*#__PURE__*/React.createElement(Pill, {
      label: "Carry on",
      tone: "primary"
    })) : /*#__PURE__*/React.createElement(Chips, {
      step: step
    }),
    composer: step.money && !reflectBefore ? /*#__PURE__*/React.createElement(MoneyComposer, {
      onSend: () => {}
    }) : /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: step.composer || PLACEHOLDER.answering
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}
function A16() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement("div", {
    className: "noscroll",
    style: {
      position: 'relative',
      zIndex: 1,
      flex: 1,
      overflowY: 'auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      padding: '12px 16px 16px'
    }
  }, /*#__PURE__*/React.createElement(EyebrowDivider, null, "Risk profile locked"), /*#__PURE__*/React.createElement(HeroNumberCard, {
    title: "Meera's risk number",
    meta: "Locked \xB7 15 Sep 2026",
    value: 54,
    badge: "Moderate",
    copy: "We look at three things and go with the lowest of them. Her money can take more risk than she can.",
    rows: A.result.meters
  }), /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(SentinelText, {
    text: A.result.lines[0]
  }), /*#__PURE__*/React.createElement(SentinelText, {
    text: A.result.lines[1],
    weight: "Regular"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(Provenance, {
    text: "As of 15 Sep \xB7 from her twelve answers, her ITR and her September statement"
  }))))), /*#__PURE__*/React.createElement(Dock, {
    chips: /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, A.result.chips.map(([l, t]) => /*#__PURE__*/React.createElement(Pill, {
      key: l,
      label: l,
      tone: t
    }))),
    cta: /*#__PURE__*/React.createElement(DarkButton, {
      label: A.result.cta
    }),
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: PLACEHOLDER.result
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, {
    tone: "dark"
  }));
}
const BOARD = [['A / 01 Intro', 'No rail yet — nothing has been asked. Provenance names where her record came from.', /*#__PURE__*/React.createElement(A01, null)], ['A / 02 Q1 Age', 'Smart chip offers the KYC figure; the composer switches to "or type an age".', /*#__PURE__*/React.createElement(Question, {
  i: 0
})], ['A / 03 Q2 Income', 'The one question whose wording changes the meaning — card-with-subtitle chips.', /*#__PURE__*/React.createElement(Question, {
  i: 1
})], ['A / 04 Q3 Earns', 'MoneyComposer: ₹ in its own slot, Indian grouping.', /*#__PURE__*/React.createElement(Question, {
  i: 2
})], ['A / 05 Q4 Spends', 'Q3 has collapsed to a one-line question with its parse note beneath.', /*#__PURE__*/React.createElement(Question, {
  i: 3
})], ['A / 06 Q5 Emergencies', 'Sub-line qualifies the question: "Money she could reach by tomorrow."', /*#__PURE__*/React.createElement(Question, {
  i: 4
})], ['A / 07 Reflect', 'Interjection — Sentinel reflects, it does not ask. Rail holds and drops to 40%.', /*#__PURE__*/React.createElement(Question, {
  i: 5,
  reflectBefore: A.reflect1.text
})], ['A / 08 Q6 Dependents', 'Back to asking; the rail advances to 6.', /*#__PURE__*/React.createElement(Question, {
  i: 5
})], ['A / 09 Q7 Horizon', 'Five options, all self-evident — pills, no subtitles.', /*#__PURE__*/React.createElement(Question, {
  i: 6
})], ['A / 10 Q8 Goal', 'The muted chip is the escape hatch: "She isn\'t sure yet".', /*#__PURE__*/React.createElement(Question, {
  i: 7
})], ['A / 11 Q9 Behaviour', 'The most useful question in the set. Sub-line insists on behaviour, not intent.', /*#__PURE__*/React.createElement(Question, {
  i: 8
})], ['A / 12 Reflect', 'Second interjection, after the behaviour answer.', /*#__PURE__*/React.createElement(Question, {
  i: 9,
  reflectBefore: A.reflect2.text
})], ['A / 13 Q10 Drawdown', 'What she can sit through — the answer that binds the score.', /*#__PURE__*/React.createElement(Question, {
  i: 9
})], ['A / 14 Q11 Objective', 'Five objectives, ordered protect → push.', /*#__PURE__*/React.createElement(Question, {
  i: 10
})], ['A / 15 Q12 Knowledge', 'Sub-line states what this limits: complexity, not risk.', /*#__PURE__*/React.createElement(Question, {
  i: 11
})], ['A / 16 Result', 'Rail retires. 64px display numeral counts up; only the binding meter is full bronze.', /*#__PURE__*/React.createElement(A16, null)]];
function App() {
  return /*#__PURE__*/React.createElement("div", {
    className: "board"
  }, BOARD.map(([name, sub, el]) => /*#__PURE__*/React.createElement(Artboard, {
    key: name,
    name: name,
    sub: sub
  }, el)));
}
/* Mount only when a page provides #root. This file is also compiled into _ds_bundle.js, which is loaded
   by pages that have no #root of their own — an unguarded createRoot(null) throws React #299 there. */
const aRoot = document.getElementById('root');
if (aRoot) ReactDOM.createRoot(aRoot).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/journey-a-risk/a-screens.jsx", error: String((e && e.message) || e) }); }

// ui_kits/journey-b/b-board.jsx
try { (() => {
/* Screens are resolved from window at RENDER time, not at module-evaluation time. b-screens.jsx registers
   them (Object.assign(window, {B01…})), and the bundle compiles files alphabetically — b-board before
   b-screens — so a top-level reference to B01 here throws "B01 is not defined" in the bundle. */
const BOARD = [['B / 01 Home', 'Default. Three suggestion rows restored verbatim — external, generative, diagnostic.', 'B01'], ['B / 02 Query', 'Thread bottom-anchored; the sentence stays editable in the composer.', 'B02'], ['B / 03 Trace', 'Steps name real work. Stop sits in the send slot — the composer never leaves.', 'B03'], ['B / 04 Filling in', 'Trace collapsed to "Thought for 4s"; the card fills in with live row labels.', 'B04'], ['B / 05 Artifact', 'The result as an object in the thread: 96px preview, three-slot footer, provenance.', 'B05'], ['B / 06 Expanded', 'The artifact expanded in place: its own header row with ⋯, Collapse ⌃, and the thread carrying the scroll.', 'B06'], ['B / 07 Follow-up', 'A follow-up while the card stays expanded: the thread scrolls past it, so only its foot is in view.', 'B07'], ['B / 08 Rebalance', 'Two moves and what they cost. CTA stacks above the composer.', 'B08'], ['B / 09 Confirm', 'Modal, so it keeps the scrim. Disclosure above the numbers; compliance as rows.', 'B09'], ['B / 10 Success', 'Drawn check, timestamp, settlement line. No confetti.', 'B10'], ['B / 11 Share', 'Where Share goes: the message draft as an expanded artifact in the thread, disclosure locked above the body.', 'B11']];
function App() {
  return /*#__PURE__*/React.createElement("div", {
    className: "board"
  }, BOARD.map(([name, sub, key]) => {
    const S = window[key];
    return /*#__PURE__*/React.createElement(Artboard, {
      key: name,
      name: name,
      sub: sub
    }, S ? /*#__PURE__*/React.createElement(S, null) : null);
  }));
}
/* Mount only when a page provides #root. This file is also compiled into _ds_bundle.js, which is loaded
   by pages that have no #root of their own — an unguarded createRoot(null) throws React #299 there. */
const bRoot = document.getElementById('root');
if (bRoot) ReactDOM.createRoot(bRoot).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/journey-b/b-board.jsx", error: String((e && e.message) || e) }); }

// ui_kits/journey-b/b-data.jsx
try { (() => {
/* Journey B — R. Sharma only. Copy verbatim from Sentinel_v2_Spec §3.2 and the pattern plates. */
window.DS = window.SentinelDesignSystem_0682a2;
window.B = {
  query: "Why did Sharma's portfolio drift this quarter?",
  thinkingLine: 'Comparing 30 Jun against today across 18 holdings…',
  steps: ["Reading Sharma's holdings — 18 funds", 'Comparing against his mandate', 'Checking Q2 statements', 'Attributing the drift'],
  reasoning: 'Equity moved from 62% to 71% against a 60% target. Three inputs contributed; only one was a decision of yours.',
  answer: 'Equity went from 62% to 71% against a 60% target. Three things moved it, and only one of them was a decision.',
  cardEyebrow: 'Drift attribution · Q2 → Q3',
  cardTitle: '62% → 71%, mostly the market',
  provenance: 'As of 30 Sep · from his Q3 statement and mandate on file',
  canvasTitle: "What moved Sharma's equity",
  contributions: [{
    label: 'Small-cap rally',
    value: 6.1,
    note: 'the market, not a decision of yours'
  }, {
    label: 'His own top-up in July',
    value: 3.4,
    note: 'went into Quant Small Cap'
  }, {
    label: 'Your 4 Aug switch out',
    value: -1.2,
    note: 'this one was intentional',
    intentional: true
  }, {
    label: 'Debt fund NAV drift',
    value: 0.7
  }],
  verdict: 'Two-thirds of the drift is the small-cap rally. You did not cause this, and selling into it has a cost — that is the trade-off below.',
  followUpQ: 'What would fixing it cost?',
  followUpA: 'Two moves, not seven. This alone brings equity from 71% back to 58%, and it costs ₹11,200.',
  moves: [{
    n: 1,
    title: 'Move ₹1,85,000 out of Quant Small Cap',
    body: 'Into ICICI Corporate Bond. Brings equity from 67% to 58%.'
  }, {
    n: 2,
    title: 'Redirect her ₹30,000 monthly SIP',
    body: 'No exit load, no tax, and it stops the drift returning.'
  }],
  costs: [{
    dir: 'down',
    label: 'Exit load',
    value: '₹0'
  }, {
    dir: 'down',
    label: 'Short-term capital gains',
    value: '₹11,200'
  }, {
    dir: 'eq',
    label: 'Total to fix now',
    value: '₹11,200'
  }],
  confirmDisclosure: 'Sharma receives this under your ARN. He will see both switches, the new SIP destination and the disclosure — not your working, or the sixteen trades screened out.',
  /* A rebalance checks different things from a proposal — the single-fund ceiling was answered upstream. */
  compliance: [{
    label: 'Mandate after these moves',
    value: 'Equity 58% · within band',
    tone: 'ok'
  }, {
    label: 'Exit load and tax',
    value: '₹11,240',
    tone: 'ok'
  }, {
    label: 'Client consent',
    value: 'Required',
    tone: 'over'
  }],
  draft: {
    eyebrow: 'Message draft · 1 recipient',
    title: 'Why your equity moved',
    body: "Hello Mr. Sharma — your equity share moved from 62% to 71% this quarter. Most of that was the small-cap rally rather than anything we changed; your own top-up in July added to it. I have proposed two switches to bring it back to the 60% we agreed, and I will walk you through both.",
    recipient: 'R. Sharma · WhatsApp'
  },
  successTitle: 'Two moves queued',
  successMeta: 'Placed 16 Sep, 9:41 · settles T+2',
  successBody: 'Sharma gets a note explaining both moves. Nothing else in his book changed.',
  suggestions: ['Show me Diwali offer from HDFC AMC', 'Build a proposal for Mr. Amit Aggrawal', "Why did Sharma's portfolio drift this quarter?"],
  homeChips: ['Review a portfolio', 'Proposal', 'Fund explorer'],
  jump: [{
    label: "Meera's risk profile",
    meta: '12 questions'
  }, {
    label: '₹25 L proposal',
    meta: '6 funds'
  }, {
    label: 'What Meera holds',
    meta: '43 funds'
  }, {
    label: "Sharma's rebalance",
    meta: '2 moves'
  }]
};
/* One context-keyed placeholder map — the composer law, enforced in one place (§ readme placement law). */
window.PLACEHOLDER = {
  home: 'Ask Sentinel about a client, a fund, or a plan',
  thread: 'Ask Sentinel',
  answering: 'or type your answer',
  amount: 'or type an amount',
  canvas: 'Ask about this',
  expanded: 'Ask about this',
  sheet: 'Ask a follow-up',
  drawer: 'Ask Sentinel',
  streaming: 'Generating…',
  result: 'Ask about this'
};
window.f = (weight, size, lh, color) => ({
  margin: 0,
  fontFamily: 'var(--font-ui)',
  fontWeight: weight,
  fontSize: size,
  lineHeight: lh ? lh + 'px' : 'normal',
  color: color || 'var(--color-ink)'
});
window.cardS = {
  width: '100%',
  borderRadius: 16,
  background: 'var(--color-surface)',
  boxShadow: 'var(--shadow-card)',
  boxSizing: 'border-box'
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/journey-b/b-data.jsx", error: String((e && e.message) || e) }); }

// ui_kits/journey-b/b-parts.jsx
try { (() => {
const {
  Eyebrow,
  Pill,
  InlineActionRow,
  DarkButton,
  Composer,
  Dock,
  HomeIndicator,
  StatusSpacer,
  TopBar,
  ScreenBackdrop,
  DrawnCheck,
  Badge,
  StatTile,
  Sparkline,
  Pressable
} = window.DS;

/* Artboard shell — every artboard is a real 375×812 screen. */
function Artboard({
  name,
  sub,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "ab"
  }, /*#__PURE__*/React.createElement("div", {
    className: "ab-name"
  }, name), sub && /*#__PURE__*/React.createElement("div", {
    className: "ab-sub"
  }, sub), /*#__PURE__*/React.createElement("div", {
    className: "frame"
  }, children));
}

/* Skeleton row for the artifact card filling in (Plate 1, right) — each carries its own live label. */
function SkeletonRow({
  label,
  wide
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: 42,
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 10,
      borderBottom: '0.5px solid var(--color-line-soft)'
    }
  }, label ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      ...f(500, 13, 18, 'var(--color-muted)')
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: 9999,
      boxShadow: 'inset 0 0 0 1.2px var(--color-bronze)',
      animation: 'sentinel-shimmer 1200ms ease-in-out infinite'
    }
  }), label) : /*#__PURE__*/React.createElement("span", {
    style: {
      height: 8,
      width: wide || 120,
      borderRadius: 9999,
      background: 'var(--color-track)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      height: 8,
      width: 34,
      borderRadius: 9999,
      background: 'var(--color-track)'
    }
  }));
}

/* Compact attribution preview for the artifact card — 96px, never scrollable. */
function AttributionPreview() {
  const rows = [['Small-cap rally', 6.1, 1], ['His July top-up', 3.4, 0.56], ['Your switch out', -1.2, 0.2]];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      paddingTop: 2
    }
  }, rows.map(([l, v, frac], i) => /*#__PURE__*/React.createElement("div", {
    key: l,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 108,
      flexShrink: 0,
      ...f(500, 12, 16, i === 2 ? 'var(--color-data-deemph)' : 'var(--color-ink)')
    }
  }, l), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 14,
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: '0 auto 0 0',
      width: frac * 100 + '%',
      borderRadius: '2px 5px 5px 2px',
      background: i === 2 ? 'var(--color-track)' : 'var(--color-bronze)',
      boxShadow: i === 2 ? 'inset 0 0 0 1px var(--color-data-deemph)' : 'none'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 34,
      textAlign: 'right',
      ...f(700, 12, null, i === 2 ? 'var(--color-data-deemph)' : 'var(--color-bronze-deep)')
    }
  }, v > 0 ? '+' : '−', Math.abs(v)))));
}

/* Compliance status card (Plate 4) — status is stated as a row, never implied. */
function ComplianceCard({
  rows
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...cardS,
      boxShadow: '0 0 0 1px var(--color-line)',
      padding: '0 14px'
    }
  }, rows.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: r.label,
    style: {
      display: 'flex',
      minHeight: 52,
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 10,
      padding: '8px 0',
      borderBottom: i < rows.length - 1 ? '0.5px solid var(--color-line-soft)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: f(500, 13, 18)
  }, r.label), r.note && /*#__PURE__*/React.createElement("p", {
    style: {
      ...f(400, 11, 15, 'var(--color-data-deemph)'),
      marginTop: 2
    }
  }, r.note)), /*#__PURE__*/React.createElement("span", {
    style: {
      flexShrink: 0,
      ...f(700, 13, null, r.tone === 'over' ? 'var(--color-status-over-fg)' : 'var(--color-ink)')
    }
  }, r.value))));
}

/* Cost breakdown on the rail — reuses the trace structure (Plate 4, middle). */
function CostBreakdown({
  items
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 13, 18, 'var(--color-ink)')
  }, "What this costs"), /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none",
    style: {
      transform: 'rotate(180deg)'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M3 4.5 6 7.5 9 4.5",
    stroke: "var(--color-muted)",
    strokeWidth: "1.3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      paddingLeft: 2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 8,
      bottom: 8,
      left: 9,
      width: 1,
      background: 'var(--color-line)'
    }
  }), items.map(it => /*#__PURE__*/React.createElement("div", {
    key: it.label,
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      width: 16,
      height: 16,
      flexShrink: 0,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 9999,
      background: 'var(--color-canvas)',
      boxShadow: 'inset 0 0 0 1.4px var(--color-bronze)'
    }
  }, it.dir === 'eq' ? /*#__PURE__*/React.createElement("svg", {
    width: "8",
    height: "8",
    viewBox: "0 0 8 8",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M1.5 3h5M1.5 5h5",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.2",
    strokeLinecap: "round"
  })) : /*#__PURE__*/React.createElement("svg", {
    width: "8",
    height: "8",
    viewBox: "0 0 8 8",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4 1v6M1.8 4.8 4 7l2.2-2.2",
    stroke: "var(--color-bronze-deep)",
    strokeWidth: "1.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      ...f(it.dir === 'eq' ? 600 : 500, 13, 18)
    }
  }, it.label), /*#__PURE__*/React.createElement("span", {
    style: f(it.dir === 'eq' ? 700 : 500, 13, 18, it.dir === 'eq' ? 'var(--color-bronze-deep)' : 'var(--color-ink)')
  }, it.value)))));
}

/* The standing dock for a canvas / result — chips, CTA, composer, disclosure. Composer always last and always present. */
function ResultDock({
  chips,
  cta,
  placeholder,
  value = '',
  streaming = false
}) {
  return /*#__PURE__*/React.createElement(Dock, {
    chips: chips,
    cta: cta,
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: value,
      onChange: () => {},
      placeholder: placeholder,
      streaming: streaming
    })
  });
}
/* Frozen trace. The kit renders its own static trace rather than relying on ProgressTrace's autoplay gate —
   an artboard must never show a running clock, whatever the bundle carries. */
function FrozenTrace({
  steps,
  active,
  seconds
}) {
  const circle = state => {
    if (state === 'done') return /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        width: 16,
        height: 16,
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 9999,
        background: 'var(--color-bronze)'
      }
    }, /*#__PURE__*/React.createElement("svg", {
      width: "9",
      height: "9",
      viewBox: "0 0 9 9",
      fill: "none"
    }, /*#__PURE__*/React.createElement("path", {
      d: "M1.8 4.6 3.6 6.4 7.2 2.6",
      stroke: "var(--color-surface)",
      strokeWidth: "1.3",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    })));
    if (state === 'active') return /*#__PURE__*/React.createElement("span", {
      style: {
        width: 16,
        height: 16,
        borderRadius: 9999,
        background: 'var(--color-canvas)',
        boxShadow: '0 0 0 2px var(--color-bronze)',
        boxSizing: 'border-box'
      }
    });
    return /*#__PURE__*/React.createElement("span", {
      style: {
        width: 16,
        height: 16,
        borderRadius: 9999,
        boxShadow: '0 0 0 1px var(--color-line)'
      }
    });
  };
  return /*#__PURE__*/React.createElement(window.DS.SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      ...f(500, 12, 16, 'var(--color-ink)'),
      marginBottom: 10
    }
  }, "Working \xB7 ", seconds, "s"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      paddingLeft: 2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 8,
      bottom: 8,
      left: 9,
      width: 1,
      background: 'var(--color-line)'
    }
  }), steps.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: s,
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      opacity: i === active ? 1 : 0.4
    }
  }, circle(i < active ? 'done' : i === active ? 'active' : 'pending'), /*#__PURE__*/React.createElement("span", {
    style: f(500, 13, 18, 'var(--color-ink-soft)')
  }, s))))));
}
Object.assign(window, {
  Artboard,
  SkeletonRow,
  AttributionPreview,
  ComplianceCard,
  CostBreakdown,
  ResultDock,
  FrozenTrace
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/journey-b/b-parts.jsx", error: String((e && e.message) || e) }); }

// ui_kits/journey-b/b-screens.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const {
  ScreenBackdrop,
  StatusSpacer,
  TopBar,
  GreetingDivider,
  SuggestionRow,
  Eyebrow,
  EyebrowDivider,
  Pill,
  ChipRow,
  InlineActionRow,
  DarkButton,
  Composer,
  Dock,
  HomeIndicator,
  UserBubble,
  SentinelBlock,
  SentinelText,
  ProgressTrace,
  AllocationCard,
  AttributionChart,
  ArtifactCard,
  MoveCard,
  ConstraintCallout,
  DisclosureBlock,
  StatTile,
  Sparkline,
  DrawnCheck,
  Badge,
  Provenance,
  StandingDisclosure,
  ScrollToBottomButton,
  Pressable
} = window.DS;
const Shell = ({
  children
}) => /*#__PURE__*/React.createElement("div", {
  style: {
    position: 'relative',
    display: 'flex',
    height: '100%',
    width: '100%',
    flexDirection: 'column'
  }
}, /*#__PURE__*/React.createElement(ScreenBackdrop, null), children);
/* Bottom-anchored AND pinned to the newest turn (see journey-a-risk for the reasoning). */
function Thread({
  children,
  pad = '16px 16px 24px'
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    const el = ref.current;
    if (el) el.scrollTop = el.scrollHeight;
  });
  return /*#__PURE__*/React.createElement("div", {
    ref: ref,
    className: "noscroll",
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      flex: 1,
      flexDirection: 'column',
      overflowY: 'auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      padding: pad
    }
  }, children));
}

/* B / 01 — Home / Default. The three suggestion rows restored verbatim: one external, one generative, one diagnostic. */
function B01() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      paddingTop: 4
    }
  }, /*#__PURE__*/React.createElement(GreetingDivider, null, "Good afternoon, Ashish")), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: '24px 16px 0'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      ...cardS,
      boxShadow: 'var(--shadow-card-soft)',
      padding: '0 12px'
    }
  }, B.suggestions.map((s, i) => /*#__PURE__*/React.createElement(SuggestionRow, {
    key: s,
    label: s,
    last: i === 2
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: '20px 16px 0'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '0 4px 8px'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Jump back in")), /*#__PURE__*/React.createElement("div", {
    style: {
      ...cardS,
      boxShadow: 'var(--shadow-card-soft)',
      padding: '0 12px'
    }
  }, B.jump.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: r.label,
    style: {
      display: 'flex',
      height: 46,
      alignItems: 'center',
      justifyContent: 'space-between',
      borderBottom: i < 3 ? '0.5px solid var(--color-line-soft)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(600, 13, 18)
  }, r.label), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 11.5, 16, 'var(--color-muted)')
  }, r.meta), /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4.5 3 7.5 6l-3 3",
    stroke: "var(--color-bronze)",
    strokeWidth: "1.3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'center',
      gap: '13px 8px',
      padding: '0 16px 12px'
    }
  }, B.homeChips.map(c => /*#__PURE__*/React.createElement(Pill, {
    key: c,
    label: c
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: '0 16px 12px'
    }
  }, /*#__PURE__*/React.createElement(Composer, {
    value: "",
    onChange: () => {},
    placeholder: PLACEHOLDER.home
  })), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 02 — the typed query. Thread bottom-anchored; composer focused with the sentence still editable. */
function B02() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(SentinelText, {
    text: "Ask about a client, a fund or a plan. I will tell you if I do not follow.",
    weight: "Regular"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: '6px 16px 8px',
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Composer, {
    value: B.query,
    onChange: () => {},
    placeholder: PLACEHOLDER.thread,
    autoFocus: true
  }), /*#__PURE__*/React.createElement(StandingDisclosure, null)), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 03 — the progress trace. Steps name real work; Stop occupies the send slot so the composer never leaves. */
function B03() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(UserBubble, {
    text: B.query
  }), /*#__PURE__*/React.createElement(FrozenTrace, {
    steps: B.steps,
    active: 2,
    seconds: 3
  })), /*#__PURE__*/React.createElement(Dock, {
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: PLACEHOLDER.thread,
      streaming: true
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 04 — trace collapsed, artifact card filling in. Never a blank wait: each skeleton row carries its live label. */
function B04() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement(Pressable, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 13, null, 'var(--color-muted)')
  }, "Thought for 4s"), /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4.5 3 7.5 6l-3 3",
    stroke: "var(--color-muted)",
    strokeWidth: "1.3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10,
      borderLeft: '1px solid var(--color-line)',
      paddingLeft: 12
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: f(400, 13, 19, 'var(--color-muted)')
  }, B.reasoning)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement(SentinelText, {
    text: B.answer
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      ...cardS,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 14px 0'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, B.cardEyebrow), /*#__PURE__*/React.createElement("p", {
    style: {
      ...f(500, 16, 24),
      marginTop: 4
    }
  }, B.cardTitle)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '10px 14px 0'
    }
  }, /*#__PURE__*/React.createElement(SkeletonRow, {
    label: null,
    wide: 132
  }), /*#__PURE__*/React.createElement(SkeletonRow, {
    label: "Reading his July top-up"
  }), /*#__PURE__*/React.createElement(SkeletonRow, {
    label: null,
    wide: 98
  })))), /*#__PURE__*/React.createElement(Dock, {
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: PLACEHOLDER.streaming,
      streaming: true
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 05 — artifact card complete in the thread, with the three-slot footer. */
function B05() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement(SentinelText, {
    text: B.answer
  })), /*#__PURE__*/React.createElement(ArtifactCard, {
    eyebrow: B.cardEyebrow,
    title: B.cardTitle,
    provenance: B.provenance,
    onToggle: () => {},
    onWhy: () => {},
    onShare: () => {}
  }, /*#__PURE__*/React.createElement(AttributionPreview, null)), /*#__PURE__*/React.createElement("p", {
    style: f(400, 11, 15, 'var(--color-data-deemph)')
  }, "Share opens the message draft \u2014 B / 11.")), /*#__PURE__*/React.createElement(Dock, {
    chips: /*#__PURE__*/React.createElement(InlineActionRow, {
      actions: [{
        label: 'Why is 71% a problem at all',
        tone: 'tertiary'
      }, {
        label: 'Show me the 18 holdings'
      }],
      animate: false
    }),
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: PLACEHOLDER.thread
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 06 — the EXPANDED artifact card. Replaces the A2 canvas artboard: that surface was removed from the
   product (v5 Part 1), so this documents the pattern that took its job — the card's own header row
   (eyebrow, title, ⋯), the V3 chart and stat tiles in situ, the Collapse ⌃ footer slot, and the
   no-nested-scroll contract: the THREAD scrolls, the card never does. */
function B06() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement(SentinelText, {
    text: B.answer
  })), /*#__PURE__*/React.createElement(ArtifactCard, {
    state: "expanded",
    eyebrow: B.cardEyebrow,
    title: B.cardTitle,
    provenance: B.provenance,
    onToggle: () => {},
    onWhy: () => {},
    onShare: () => {},
    onMenu: () => {}
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(AttributionChart, {
    from: 62,
    to: 71,
    target: 60,
    contributions: B.contributions
  }), /*#__PURE__*/React.createElement("p", {
    style: f(400, 13, 19, 'var(--color-ink-soft)')
  }, B.verdict), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(StatTile, {
    label: "Cost of doing nothing",
    value: "\u20B94,40,000",
    note: "in a 20% fall"
  }), /*#__PURE__*/React.createElement(StatTile, {
    label: "Cost of fixing it now",
    value: "\u20B911,200",
    note: "exit load + STCG"
  })), /*#__PURE__*/React.createElement(StatTile, {
    label: "Drift added since 30 Jun",
    value: "+9 points",
    note: "62% to 71% against a 60% target",
    sparkline: /*#__PURE__*/React.createElement(Sparkline, {
      points: [62, 64, 67, 69, 71],
      width: 120
    })
  }))), /*#__PURE__*/React.createElement("p", {
    style: f(400, 11, 15, 'var(--color-data-deemph)')
  }, "No nested scroll: the card takes its natural height and the thread carries the scroll. On expand the thread brings this header under the app bar; on collapse it returns to the card. The table view lives in the \u22EF.")), /*#__PURE__*/React.createElement(ResultDock, {
    chips: /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, /*#__PURE__*/React.createElement(Pill, {
      label: "Why is 71% a problem at all",
      tone: "tertiary"
    }), /*#__PURE__*/React.createElement(Pill, {
      label: "Show me the 18 holdings"
    })),
    cta: /*#__PURE__*/React.createElement(DarkButton, {
      label: "Fix it"
    }),
    placeholder: PLACEHOLDER.expanded
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 07 — a follow-up asked while the artifact is expanded. The card does not collapse and nothing is
   pushed aside: the thread scrolls past it, so only its foot is in view and the newest turn sits at the
   bottom. That is the no-nested-scroll contract seen from the other end. */
function B07() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, {
    pad: "0 16px"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 16,
      background: 'var(--color-surface)',
      boxShadow: 'var(--shadow-card)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 14px'
    }
  }, /*#__PURE__*/React.createElement(AttributionPreview, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '12px 14px 0',
      height: 1,
      background: 'var(--color-line-soft)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      padding: '0 14px'
    }
  }, [['Collapse ⌃', true], ['Why?', false], ['Share', false]].map(([t, strong]) => /*#__PURE__*/React.createElement("span", {
    key: t,
    style: {
      flex: 1,
      display: 'flex',
      height: 44,
      alignItems: 'center',
      ...f(700, 12, 16, strong ? 'var(--color-bronze-deep)' : 'var(--color-muted)')
    }
  }, t)))), /*#__PURE__*/React.createElement(UserBubble, {
    text: B.followUpQ
  }), /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement(SentinelText, {
    text: B.followUpA
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12,
      ...cardS,
      boxShadow: '0 0 0 1px var(--color-line)',
      padding: '0 14px'
    }
  }, [['Cost of doing nothing', '₹4,40,000'], ['Cost of fixing it now', '₹11,200']].map(([l, v], i) => /*#__PURE__*/React.createElement("div", {
    key: l,
    style: {
      display: 'flex',
      height: 52,
      alignItems: 'center',
      justifyContent: 'space-between',
      borderBottom: i === 0 ? '0.5px solid var(--color-line-soft)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 14, 20)
  }, l), /*#__PURE__*/React.createElement("span", {
    style: f(600, 16, 20, 'var(--color-bronze-deep)')
  }, v)))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(Provenance, {
    text: "As of 30 Sep \xB7 exit load from scheme documents, STCG at 20%"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement(InlineActionRow, {
    actions: [{
      label: 'Show the five we skipped'
    }],
    animate: false
  })))), /*#__PURE__*/React.createElement(ResultDock, {
    chips: /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, /*#__PURE__*/React.createElement(Pill, {
      label: "Why 20% STCG?",
      tone: "tertiary"
    })),
    cta: /*#__PURE__*/React.createElement(DarkButton, {
      label: "Fix it"
    }),
    placeholder: PLACEHOLDER.expanded
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 08 — the rebalance response: two move cards and the cost breakdown, ending in the CTA above the composer. */
function B08() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(UserBubble, {
    text: "Fix it"
  }), /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement(EyebrowDivider, null, "Two moves, not seven")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, B.moves.map(m => /*#__PURE__*/React.createElement(MoveCard, _extends({
    key: m.n
  }, m)))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14
    }
  }, /*#__PURE__*/React.createElement(CostBreakdown, {
    items: B.costs
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement(Provenance, {
    text: "As of 30 Sep \xB7 exit load from scheme documents, STCG at 20%"
  })))), /*#__PURE__*/React.createElement(Dock, {
    chips: /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, /*#__PURE__*/React.createElement(Pill, {
      label: "Show the five we skipped"
    })),
    cta: /*#__PURE__*/React.createElement(DarkButton, {
      label: "Approve both moves"
    }),
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: PLACEHOLDER.thread
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}

/* B / 09 — the confirm sheet. Modal, so it keeps the scrim. Disclosure ABOVE the numbers; compliance stated as rows.
   The one surface with no composer: commit or dismiss, no third path (§ readme, confirm-sheet exception). */
function B09() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, {
    pad: "16px 16px 10px"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      opacity: 0.5
    }
  }, /*#__PURE__*/React.createElement(MoveCard, B.moves[0]))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      zIndex: 20,
      background: 'var(--scrim)',
      opacity: 0.4
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      zIndex: 30,
      maxHeight: '93%',
      display: 'flex',
      flexDirection: 'column',
      borderRadius: '24px 24px 0 0',
      background: 'var(--color-canvas)',
      boxShadow: '0 -8px 40px -12px rgba(37,31,27,.28)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '12px auto 10px',
      height: 5,
      width: 44,
      borderRadius: 9999,
      background: 'var(--color-line)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "noscroll",
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '0 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: f(600, 18, 24)
  }, "Approve \u2014 R. Sharma"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 10,
      borderRadius: 16,
      background: 'var(--color-surface)',
      padding: '9px 12px',
      boxShadow: '0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 13, 18)
  }, "2 moves \xB7 \u20B91,85,000 \xB7 equity 71% \u2192 58%"), /*#__PURE__*/React.createElement(Pill, {
    label: "Edit",
    size: "sm",
    tone: "filter"
  })), /*#__PURE__*/React.createElement(ConstraintCallout, {
    eyebrow: "Before you approve",
    body: B.confirmDisclosure
  }), /*#__PURE__*/React.createElement(DisclosureBlock, null), /*#__PURE__*/React.createElement(ComplianceCard, {
    rows: B.compliance
  })), /*#__PURE__*/React.createElement(Dock, {
    chips: /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, /*#__PURE__*/React.createElement(Pill, {
      label: "What Sharma will not see",
      tone: "tertiary"
    })),
    cta: /*#__PURE__*/React.createElement(DarkButton, {
      label: "Approve both moves"
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, {
    tone: "dark"
  })));
}

/* B / 10 — success. A drawn check, a timestamp, a settlement line. No confetti: wrong register for money. */
function B10() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, null, /*#__PURE__*/React.createElement(UserBubble, {
    text: "Approve both moves"
  }), /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      ...cardS,
      padding: '13px 14px'
    }
  }, /*#__PURE__*/React.createElement(DrawnCheck, null), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
    style: f(600, 15, 22)
  }, B.successTitle), /*#__PURE__*/React.createElement("p", {
    style: {
      ...f(500, 12, 16, 'var(--color-muted)'),
      marginTop: 2
    }
  }, B.successMeta)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginLeft: 'auto'
    }
  }, /*#__PURE__*/React.createElement(Badge, {
    variant: "status",
    tone: "ok"
  }, "Queued"))), /*#__PURE__*/React.createElement("p", {
    style: {
      ...f(400, 14, 20, 'var(--color-ink-soft)'),
      marginTop: 10
    }
  }, B.successBody), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(Provenance, {
    text: "Placed 16 Sep, 9:41 \xB7 confirmation from the RTA on T+2"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement(InlineActionRow, {
    actions: [{
      label: 'Tell Sharma now'
    }, {
      label: 'Back to his portfolio'
    }],
    animate: false
  })))), /*#__PURE__*/React.createElement(Dock, {
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: "",
      onChange: () => {},
      placeholder: PLACEHOLDER.thread
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}
/* B / 11 — where Share goes: the message draft as an expanded ArtifactCard in the thread. No canvas, no
   back pill. Thread padding is 0 top/bottom — a box shorter than its card is anchored to the bottom,
   never padded toward fitting. Disclosure stays locked above the body. */
function B11() {
  return /*#__PURE__*/React.createElement(Shell, null, /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, null), /*#__PURE__*/React.createElement(Thread, {
    pad: "0 16px"
  }, /*#__PURE__*/React.createElement(ArtifactCard, {
    state: "expanded",
    eyebrow: B.draft.eyebrow,
    title: B.draft.title,
    provenance: "As of 30 Sep \xB7 drafted from the drift attribution. You send it, not Sentinel.",
    onToggle: () => {},
    onWhy: () => {},
    onShare: () => {},
    onMenu: () => {}
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement(DisclosureBlock, null), /*#__PURE__*/React.createElement("div", {
    style: {
      ...cardS,
      boxShadow: '0 0 0 1px var(--color-line)',
      padding: 10
    }
  }, /*#__PURE__*/React.createElement("p", {
    style: f(400, 14, 20, 'var(--color-ink-soft)')
  }, B.draft.body))))), /*#__PURE__*/React.createElement(ResultDock, {
    chips: /*#__PURE__*/React.createElement(ChipRow, {
      animate: false
    }, /*#__PURE__*/React.createElement(Pill, {
      label: "What must the disclosure say?",
      tone: "tertiary"
    })),
    cta: /*#__PURE__*/React.createElement(DarkButton, {
      label: "Send to R. Sharma"
    }),
    placeholder: PLACEHOLDER.expanded
  }), /*#__PURE__*/React.createElement(HomeIndicator, null));
}
Object.assign(window, {
  B01,
  B02,
  B03,
  B04,
  B05,
  B06,
  B07,
  B08,
  B09,
  B10,
  B11
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/journey-b/b-screens.jsx", error: String((e && e.message) || e) }); }

// ui_kits/sentinel-app/app.jsx
try { (() => {
const {
  PhoneFrame
} = window.DS;
/* The conversation is the only surface. There is no canvas branch and no product screens in this kit:
   Home, the thread, the risk journey, the drawer — the states a design system needs to show. Product
   screens (client review, proposal) live with the product, where they are kept current. */
function App() {
  const [screen, setScreen] = React.useState('home');
  const [seed, setSeed] = React.useState('');
  const [drawer, setDrawer] = React.useState(false);
  const openChat = t => {
    setSeed(t);
    setScreen('chat');
  };
  const SEED = {
    proposal: 'Build a proposal for Mr. Amit Aggrawal',
    portfolio: 'What does Meera hold?',
    chat: 'Rebalance Sharma to his mandate'
  };
  const go = where => {
    setDrawer(false);
    if (where === 'journey') setScreen('journey');else openChat(SEED[where] || where);
  };
  const submit = t => /risk|profil|meera's risk/i.test(t) ? setScreen('journey') : openChat(t);
  const home = () => {
    setDrawer(false);
    setScreen('home');
  };
  const menu = () => setDrawer(true);
  return /*#__PURE__*/React.createElement(PhoneFrame, null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: '100%',
      width: '100%',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    key: screen,
    style: {
      position: 'absolute',
      inset: 0,
      animation: screen === 'home' ? 'none' : 'kit-slide var(--dur-screen) var(--ease) both'
    }
  }, screen === 'home' && /*#__PURE__*/React.createElement(HomeScreen, {
    onMenu: menu,
    onSubmit: submit,
    onGo: go
  }), screen === 'chat' && /*#__PURE__*/React.createElement(ChatScreen, {
    seed: seed,
    onMenu: menu,
    onNew: home
  }), screen === 'journey' && /*#__PURE__*/React.createElement(JourneyScreen, {
    onMenu: menu,
    onNew: home
  })), /*#__PURE__*/React.createElement(DrawerPanel, {
    open: drawer,
    onClose: () => setDrawer(false),
    onNew: home,
    onGo: go
  })));
}
/* Mount only when a page provides #root. This file is also compiled into _ds_bundle.js, which is loaded
   by pages that have no #root of their own — an unguarded createRoot(null) throws React #299 there. */
const appRoot = document.getElementById('root');
if (appRoot) ReactDOM.createRoot(appRoot).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/sentinel-app/app.jsx", error: String((e && e.message) || e) }); }

// ui_kits/sentinel-app/chat.jsx
try { (() => {
const {
  ScreenBackdrop,
  StatusSpacer,
  TopBar,
  UserBubble,
  SentinelBlock,
  SentinelText,
  ProgressTrace,
  InlineActionRow,
  AllocationCard,
  AttributionChart,
  ArtifactCard,
  Sparkline,
  MoveCard,
  AnswerChip,
  DarkButton,
  Composer,
  Dock,
  HomeIndicator,
  DrawnCheck,
  ExplainerSheet
} = window.DS;
/* The drift thread: seed → trace → attribution → rebalance → approve → success.
   The artifact expands and collapses in place — there is no canvas to open. */
function ChatScreen({
  seed,
  onMenu,
  onNew
}) {
  const drift = /drift|sharma|rebalance/i.test(seed);
  const [phase, setPhase] = React.useState(drift ? 'loading' : 'static');
  const [msgs, setMsgs] = React.useState(drift ? ['user', 'trace'] : ['user', 'unknown']);
  const [v, setV] = React.useState('');
  const [sheet, setSheet] = React.useState(null);
  const [artifact, setArtifact] = React.useState('peek');
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (ref.current) ref.current.scrollTop = ref.current.scrollHeight;
  }, [msgs, phase]);
  const toRebalance = () => {
    setMsgs(m => [...m, 'user2', 'rebalance']);
    setPhase('rebalance');
  };
  const approve = () => {
    setMsgs(m => [...m, 'success']);
    setPhase('done');
  };
  const send = () => {
    const q = v.trim();
    if (!q) return;
    setV('');
    if (/drift|sharma/i.test(q)) {
      setMsgs(m => [...m, ['u', q], 'trace']);
      setPhase('loading');
    } else setMsgs(m => [...m, ['u', q], 'unknown']);
  };
  const render = (m, i) => {
    if (m === 'user') return /*#__PURE__*/React.createElement(UserBubble, {
      key: i,
      text: seed
    });
    if (Array.isArray(m)) return /*#__PURE__*/React.createElement(UserBubble, {
      key: i,
      text: m[1]
    });
    if (m === 'user2') return /*#__PURE__*/React.createElement(UserBubble, {
      key: i,
      text: "What would fixing it cost?"
    });
    if (m === 'trace') return /*#__PURE__*/React.createElement(ProgressTrace, {
      key: i,
      steps: ["Reading Sharma's holdings — 18 funds", 'Comparing against his mandate', 'Checking Q2 statements', 'Attributing the drift'],
      reasoning: "Equity went from 62% to 71% against a 60% target. Three things moved it, and only one of them was a decision.",
      onDone: () => {
        setMsgs(mm => mm.map(x => x === 'trace' ? 'attribution' : x));
        setPhase('attribution');
      }
    });
    if (m === 'attribution') return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 12
      }
    }, /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement(SentinelText, {
      text: "Two-thirds of the drift is the small-cap rally. You did not cause it, and selling into it has a cost."
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 12
      }
    }, /*#__PURE__*/React.createElement(AllocationCard, {
      segments: KIT.alloc
    }))), /*#__PURE__*/React.createElement(ArtifactCard, {
      state: artifact,
      eyebrow: "Drift attribution \xB7 Q2 \u2192 Q3",
      title: "62% \u2192 71%, mostly the market",
      provenance: "As of 30 Sep \xB7 from his Q3 statement and mandate on file",
      onToggle: () => setArtifact(a => a === 'expanded' ? 'peek' : 'expanded'),
      onWhy: () => setSheet({
        title: 'Why is 71% a problem?',
        body: ['Equity is 11% over the 60% you agreed. In a normal year that barely shows.', 'In a 20% fall it costs him more than the agreed mix would have.']
      }),
      onShare: () => setSheet({
        title: 'Share this',
        body: ['Sentinel drafts the note. You send it from your own client channel.']
      }),
      onMenu: () => setSheet({
        title: 'Table view',
        body: ['Small-cap rally +6.1 pts', 'His July top-up +2.0 pts', 'Funds crept up-cap +0.9 pts']
      })
    }, artifact === 'expanded' ? /*#__PURE__*/React.createElement(AttributionChart, {
      from: 62,
      to: 71,
      contributions: KIT.drift
    }) : /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'flex-end',
        gap: 10
      }
    }, /*#__PURE__*/React.createElement(Sparkline, {
      points: [62, 64, 67, 69, 71],
      width: 240,
      height: 72
    }), /*#__PURE__*/React.createElement("span", {
      style: f(700, 13, 18, 'var(--color-bronze-deep)')
    }, "71%"))));
    if (m === 'rebalance') return /*#__PURE__*/React.createElement(SentinelBlock, {
      key: i
    }, /*#__PURE__*/React.createElement(SentinelText, {
      text: "Two moves, not seven. This alone brings equity from 71% back to 58% and costs \u20B911,200."
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 12,
        display: 'flex',
        flexDirection: 'column',
        gap: 8
      }
    }, /*#__PURE__*/React.createElement(MoveCard, {
      n: 1,
      title: "Move \u20B91,85,000 out of Quant Small Cap",
      body: "Into ICICI Corporate Bond. Brings equity from 67% to 58%."
    }), /*#__PURE__*/React.createElement(MoveCard, {
      n: 2,
      title: "Redirect her \u20B930,000 monthly SIP",
      body: "No exit load, no tax, and it stops the drift returning."
    })));
    if (m === 'success') return /*#__PURE__*/React.createElement(SentinelBlock, {
      key: i
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        ...card,
        padding: '13px 14px'
      }
    }, /*#__PURE__*/React.createElement(DrawnCheck, null), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("p", {
      style: f(600, 15)
    }, "Two moves queued"), /*#__PURE__*/React.createElement("p", {
      style: {
        ...f(500, 12, null, 'var(--color-muted)'),
        marginTop: 2
      }
    }, "Placed 16 Sep, 9:41 \xB7 settles T+2"))), /*#__PURE__*/React.createElement("p", {
      style: {
        ...f(400, 14, 20, 'var(--color-ink-soft)'),
        marginTop: 10
      }
    }, "Sharma gets a note explaining both moves. Nothing else in his book changed."), /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 12
      }
    }, /*#__PURE__*/React.createElement(InlineActionRow, {
      actions: [{
        label: 'Tell Sharma now',
        onClick: () => setSheet({
          title: 'Note to R. Sharma',
          body: ['Before it sends: this goes to Sharma under your ARN. Review the wording, then send from your own client channel — this build drafts it, it doesn\'t send.', '“Hi Sharma — I\'ve rebalanced your portfolio back to the mix we agreed. Two switches, cost ₹11,200, and your monthly SIP now goes to debt so the drift doesn\'t return.”']
        })
      }, {
        label: 'Start a review for Sharma',
        onClick: () => setV("Review Sharma's portfolio")
      }]
    })));
    return /*#__PURE__*/React.createElement(SentinelBlock, {
      key: i
    }, /*#__PURE__*/React.createElement(SentinelText, {
      text: "I did not follow that. I can look up a client, build a proposal, explain a drift, or search funds \u2014 which is closest?"
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 12
      }
    }, /*#__PURE__*/React.createElement(InlineActionRow, {
      actions: [{
        label: 'Explain a drift',
        onClick: () => {
          setMsgs(mm => [...mm, ['u', "Why did Sharma's portfolio drift this quarter?"], 'trace']);
          setPhase('loading');
        }
      }, {
        label: 'Build a proposal',
        onClick: () => setV('Build a proposal for ')
      }, {
        label: 'Look up a client',
        onClick: () => setV('Look up ')
      }]
    })));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      height: '100%',
      width: '100%',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(ScreenBackdrop, null), /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, {
    onMenu: onMenu,
    onNew: onNew
  }), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    className: "noscroll",
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      flex: 1,
      flexDirection: 'column',
      overflowY: 'auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      padding: '16px 16px 24px'
    }
  }, msgs.map(render))), /*#__PURE__*/React.createElement(Dock, {
    chips: phase === 'attribution' ? /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexWrap: 'wrap',
        gap: 8
      }
    }, /*#__PURE__*/React.createElement(AnswerChip, {
      label: "Why is 71% a problem?",
      variant: "tertiary",
      onClick: () => setSheet({
        title: 'Why is 71% a problem?',
        body: ['Equity is 11% over the 60% you agreed. In a normal year that barely shows.', 'In a 20% fall it costs him more than the agreed mix would have — the drift only bites when markets drop.']
      })
    }), /*#__PURE__*/React.createElement(AnswerChip, {
      label: "Show the 18 holdings",
      onClick: () => setArtifact('expanded')
    })) : phase === 'rebalance' ? /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 8
      }
    }, /*#__PURE__*/React.createElement(AnswerChip, {
      label: "Show the five we skipped",
      onClick: () => setSheet({
        title: 'The five I skipped',
        body: ['I recommended the two moves that do the most with the least cost and tax.', 'The other candidate trades aren\'t itemised in this build — I won\'t list moves I can\'t cost.']
      })
    })) : undefined,
    cta: phase === 'attribution' ? /*#__PURE__*/React.createElement(DarkButton, {
      label: "Rebalance to his mandate",
      onClick: toRebalance
    }) : phase === 'rebalance' ? /*#__PURE__*/React.createElement(DarkButton, {
      label: "Approve both moves",
      onClick: approve
    }) : undefined,
    composer: /*#__PURE__*/React.createElement(Composer, {
      value: v,
      onChange: setV,
      onSend: send,
      placeholder: "Ask Sentinel",
      streaming: phase === 'loading',
      onStop: () => setPhase('static')
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, null), /*#__PURE__*/React.createElement(ExplainerSheet, {
    open: !!sheet,
    title: sheet ? sheet.title : '',
    body: sheet ? sheet.body : [],
    onClose: () => setSheet(null)
  }));
}
window.ChatScreen = ChatScreen;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/sentinel-app/chat.jsx", error: String((e && e.message) || e) }); }

// ui_kits/sentinel-app/data.jsx
try { (() => {
/* Demo data lifted from src/screens + src/journeys.tsx. Demo only — no real client. */
window.DS = window.SentinelDesignSystem_0682a2;
window.KIT = {
  suggestions: ['Show me Diwali offer from HDFC AMC', 'Build a proposal for Mr. Amit Aggrawal', "Why did Sharma's portfolio drift this quarter?"],
  chips: ['Review a portfolio', 'Proposal', 'Fund explorer'],
  jump: [{
    label: "Meera's risk profile",
    meta: '12 questions',
    go: 'journey'
  }, {
    label: '₹25 L proposal',
    meta: '6 funds',
    go: 'proposal'
  }, {
    label: 'What Meera holds',
    meta: '43 funds',
    go: 'portfolio'
  }, {
    label: "Sharma's rebalance",
    meta: '2 moves',
    go: 'chat'
  }],
  alloc: [{
    label: 'Equity',
    value: 71,
    color: 'var(--color-bronze)'
  }, {
    label: 'Debt',
    value: 24,
    color: 'var(--color-alloc-debt)'
  }, {
    label: 'Cash',
    value: 5,
    color: 'var(--color-bubble-edge)'
  }],
  drift: [{
    label: 'Small-cap rally',
    value: 6.1,
    note: 'the market, not a decision of yours'
  }, {
    label: 'His July top-up',
    value: 2.0,
    note: 'went into Quant Small Cap'
  }, {
    label: 'Funds crept up-cap',
    value: 0.9,
    note: 'managers drifted toward large caps'
  }],
  holdings: [['HDFC Flexi Cap', 34, true], ['Axis Bluechip', 19, true], ['SBI Corporate Bond', 18, false], ['ICICI Balanced Adv.', 14, false], ['Parag Parikh Flexi', 10, false], ['Cash & liquid', 5, false]],
  proposal: [['Parag Parikh Flexi Cap', 'Flexi cap', '22%', '₹5,50,000'], ['HDFC Large Cap', 'Large cap', '18%', '₹4,50,000'], ['Motilal Oswal Midcap', 'Mid cap', '10%', '₹2,50,000'], ['Nippon Small Cap', 'Small cap', '5%', '₹1,25,000'], ['ICICI Corporate Bond', 'Debt', '30%', '₹7,50,000'], ['SBI Liquid', 'Liquid', '15%', '₹3,75,000']],
  history: ['R. Sharma — portfolio drift', 'Mr. Amit Aggrawal — proposal draft', 'HDFC Diwali offer', 'Meera Nair — Q3 review'],
  clients: ['Meera Nair', 'Mr. Amit Aggrawal', 'Sunita Nair', 'R. Sharma'],
  risk: [{
    q: 'How old is Meera?',
    short: 'How old is Meera?',
    chips: [['Use her KYC age — 38', 'smart'], ['Type it instead']],
    composer: 'or type an age'
  }, {
    q: 'How steady is her income?',
    short: 'How steady is her income?',
    chips: [['Fixed salary'], ['Salary plus bonus'], ['Own business'], ['Freelance, varies'], ['Rent or pension']]
  }, {
    q: 'What does she earn a month?',
    short: 'What does she earn a month?',
    chips: [['From her last ITR — ₹1,80,000', 'smart'], ['₹1,50,000'], ['₹2,00,000']],
    money: true
  }, {
    q: 'How much has she kept aside for emergencies?',
    sub: 'Money she could reach by tomorrow.',
    short: 'How much for emergencies?',
    chips: [['₹3,00,000'], ['₹6,00,000'], ['₹10,00,000']],
    money: true
  }, {
    q: 'The last time markets fell 20% or more — what did she actually do?',
    sub: 'What she did, not what she says she would do.',
    short: 'The last 20% fall — what did she do?',
    chips: [['Invested more'], ['Stayed put'], ['Sold some'], ['Sold everything']]
  }]
};
window.card = {
  width: '100%',
  borderRadius: 16,
  background: 'var(--color-surface)',
  boxShadow: 'var(--shadow-card)',
  boxSizing: 'border-box'
};
window.f = (w, s, l, c) => ({
  fontFamily: 'var(--font-ui)',
  fontWeight: w,
  fontSize: s,
  lineHeight: l ? l + 'px' : 'normal',
  color: c || 'var(--color-ink)',
  margin: 0
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/sentinel-app/data.jsx", error: String((e && e.message) || e) }); }

// ui_kits/sentinel-app/drawer.jsx
try { (() => {
const {
  IconSparkle,
  IconPlus,
  IconChevronRight,
  Pressable,
  Composer
} = window.DS;
function DrawerPanel({
  open,
  onClose,
  onNew,
  onGo
}) {
  const [v, setV] = React.useState('');
  if (!open) return null;
  const Sec = ({
    t
  }) => /*#__PURE__*/React.createElement("p", {
    style: {
      ...f(700, 11, null, 'var(--color-muted)'),
      textTransform: 'uppercase',
      letterSpacing: '0.06em',
      padding: '8px 4px 6px'
    }
  }, t);
  const Row = ({
    children,
    last,
    onClick
  }) => /*#__PURE__*/React.createElement(Pressable, {
    onClick: onClick,
    style: {
      display: 'flex',
      height: 46,
      width: '100%',
      alignItems: 'center',
      justifyContent: 'space-between',
      textAlign: 'left',
      borderBottom: last ? 'none' : '0.5px solid var(--color-line-soft)'
    }
  }, children);
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    onClick: onClose,
    style: {
      position: 'absolute',
      inset: 0,
      zIndex: 30,
      background: 'var(--scrim)',
      opacity: 0.25,
      animation: 'ds-fade 250ms var(--ease) both'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      zIndex: 40,
      display: 'flex',
      width: 300,
      flexDirection: 'column',
      background: 'var(--color-canvas)',
      backgroundImage: 'var(--paper-texture)',
      backgroundSize: 'var(--paper-texture-size)',
      boxShadow: 'var(--shadow-drawer)',
      animation: 'kit-drawer var(--dur-screen) var(--ease) both'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '52px 20px 8px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(IconSparkle, null), /*#__PURE__*/React.createElement("span", {
    style: f(700, 16)
  }, "Sentinel")), /*#__PURE__*/React.createElement(Pressable, {
    onClick: onNew,
    style: {
      display: 'flex',
      width: 36,
      height: 36,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 9999,
      background: 'var(--color-surface)',
      boxShadow: '0 0 0 1px var(--color-line)'
    }
  }, /*#__PURE__*/React.createElement(IconPlus, null))), /*#__PURE__*/React.createElement("div", {
    className: "noscroll",
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '8px 16px 0'
    }
  }, /*#__PURE__*/React.createElement(Sec, {
    t: "Saved work"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      ...card,
      boxShadow: 'none',
      padding: '0 12px'
    }
  }, KIT.jump.map((r, i) => /*#__PURE__*/React.createElement(Row, {
    key: r.label,
    last: i === 3,
    onClick: () => onGo(r.go)
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 13)
  }, r.label), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 11.5, null, 'var(--color-muted)')
  }, r.meta), /*#__PURE__*/React.createElement(IconChevronRight, null))))), /*#__PURE__*/React.createElement(Sec, {
    t: "Recent"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      ...card,
      boxShadow: 'none',
      padding: '0 12px',
      marginTop: -6
    }
  }, KIT.history.map((h, i) => /*#__PURE__*/React.createElement(Row, {
    key: h,
    last: i === 3,
    onClick: onClose
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 13)
  }, h), /*#__PURE__*/React.createElement(IconChevronRight, null)))), /*#__PURE__*/React.createElement(Sec, {
    t: "Clients"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      ...card,
      boxShadow: 'none',
      padding: '0 12px',
      marginTop: -6
    }
  }, KIT.clients.map((c, i) => /*#__PURE__*/React.createElement(Row, {
    key: c,
    last: i === 3,
    onClick: () => onGo('portfolio')
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      width: 28,
      height: 28,
      alignItems: 'center',
      justifyContent: 'center',
      borderRadius: 9999,
      background: 'var(--surface-avatar)',
      boxShadow: '0 0 0 1px var(--color-bubble-edge)',
      ...f(700, 11, null, 'var(--color-bronze-deep)')
    }
  }, c.split(' ').map(w => w[0]).join('')), /*#__PURE__*/React.createElement("span", {
    style: f(500, 13)
  }, c))))), /*#__PURE__*/React.createElement("p", {
    style: {
      ...f(400, 11, 15, 'var(--color-muted)'),
      padding: '18px 4px 4px'
    }
  }, "Demo data \xB7 no real client portfolios are shown")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '8px 16px 20px'
    }
  }, /*#__PURE__*/React.createElement(Composer, {
    value: v,
    onChange: setV,
    onSend: () => setV(''),
    placeholder: "Ask Sentinel"
  }))));
}
window.DrawerPanel = DrawerPanel;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/sentinel-app/drawer.jsx", error: String((e && e.message) || e) }); }

// ui_kits/sentinel-app/home.jsx
try { (() => {
const {
  ScreenBackdrop,
  StatusSpacer,
  TopBar,
  GreetingDivider,
  SuggestionRow,
  Eyebrow,
  Pill,
  Composer,
  HomeIndicator
} = window.DS;
function HomeScreen({
  onMenu,
  onSubmit,
  onGo
}) {
  const [v, setV] = React.useState('');
  const rise = i => ({
    animation: 'ds-rise var(--dur-screen) var(--ease) both',
    animationDelay: i * 60 + 'ms'
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      height: '100%',
      width: '100%',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(ScreenBackdrop, null), /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, {
    onMenu: onMenu,
    onNew: () => setV('')
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      paddingTop: 4,
      ...rise(0)
    }
  }, /*#__PURE__*/React.createElement(GreetingDivider, null, "Good afternoon, Ashish")), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: '24px 16px 0',
      ...rise(1)
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      ...card,
      boxShadow: 'var(--shadow-card-soft)',
      padding: '0 12px'
    }
  }, KIT.suggestions.map((s, i) => /*#__PURE__*/React.createElement(SuggestionRow, {
    key: s,
    label: s,
    last: i === 2,
    onClick: () => onSubmit(s)
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: '20px 16px 0',
      ...rise(2)
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '0 4px 8px'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Jump back in")), /*#__PURE__*/React.createElement("div", {
    style: {
      ...card,
      boxShadow: 'var(--shadow-card-soft)',
      padding: '0 12px'
    }
  }, KIT.jump.map((r, i) => /*#__PURE__*/React.createElement("button", {
    key: r.label,
    type: "button",
    onClick: () => onGo(r.go),
    style: {
      appearance: 'none',
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      padding: 0,
      display: 'flex',
      height: 46,
      width: '100%',
      alignItems: 'center',
      justifyContent: 'space-between',
      textAlign: 'left',
      borderBottom: i < 3 ? '0.5px solid var(--color-line-soft)' : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(600, 13, 18)
  }, r.label), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: f(500, 11.5, 16, 'var(--color-muted)')
  }, r.meta), /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 12 12",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4.5 3 7.5 6l-3 3",
    stroke: "var(--color-bronze)",
    strokeWidth: "1.3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'center',
      gap: '13px 8px',
      padding: '0 16px 12px'
    }
  }, KIT.chips.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: c,
    style: rise(2 + i)
  }, /*#__PURE__*/React.createElement(Pill, {
    label: c,
    onClick: () => onSubmit(c)
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1,
      padding: '0 16px 12px',
      ...rise(5)
    }
  }, /*#__PURE__*/React.createElement(Composer, {
    value: v,
    onChange: setV,
    onSend: () => v.trim() && onSubmit(v.trim())
  })), /*#__PURE__*/React.createElement(HomeIndicator, null));
}
window.HomeScreen = HomeScreen;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/sentinel-app/home.jsx", error: String((e && e.message) || e) }); }

// ui_kits/sentinel-app/journey.jsx
try { (() => {
const {
  ScreenBackdrop,
  StatusSpacer,
  TopBar,
  ProgressRail,
  QAPair,
  ParseNote,
  SentinelThinking,
  SentinelBlock,
  SentinelText,
  ChipRow,
  AnswerChip,
  Composer,
  MoneyComposer,
  Dock,
  HomeIndicator,
  EyebrowDivider,
  HeroNumberCard,
  DarkButton
} = window.DS;
/* Journey A · Meera's risk profile (five of the twelve questions, then the result). */
function JourneyScreen({
  onMenu,
  onNew
}) {
  const [cursor, setCursor] = React.useState(0);
  const [answered, setAnswered] = React.useState([{
    a: 'Start with Meera'
  }]);
  const [thinking, setThinking] = React.useState(true);
  const [text, setText] = React.useState('');
  const ref = React.useRef(null);
  React.useEffect(() => {
    setThinking(true);
    const t = setTimeout(() => setThinking(false), 620);
    return () => clearTimeout(t);
  }, [cursor]);
  React.useEffect(() => {
    if (ref.current) ref.current.scrollTop = ref.current.scrollHeight;
  }, [answered, thinking]);
  const step = KIT.risk[cursor];
  const total = KIT.risk.length;
  const advance = (label, note) => {
    setAnswered(a => [...a, {
      q: step.short,
      a: label,
      note
    }]);
    setCursor(c => c + 1);
    setText('');
  };
  const result = !step;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      display: 'flex',
      height: '100%',
      width: '100%',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(ScreenBackdrop, null), /*#__PURE__*/React.createElement(StatusSpacer, null), /*#__PURE__*/React.createElement(TopBar, {
    onMenu: onMenu,
    onNew: onNew
  }), !result && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      zIndex: 1
    }
  }, /*#__PURE__*/React.createElement(ProgressRail, {
    n: cursor + 1,
    total: 12
  })), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    className: "noscroll",
    style: {
      position: 'relative',
      zIndex: 1,
      display: 'flex',
      flex: 1,
      flexDirection: 'column',
      overflowY: 'auto'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'auto',
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      padding: '14px 16px 24px'
    }
  }, answered.map((p, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement(QAPair, {
    question: p.q,
    answer: p.a
  }), p.note && /*#__PURE__*/React.createElement(ParseNote, {
    text: p.note
  }))), thinking ? /*#__PURE__*/React.createElement(SentinelThinking, null) : result ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(EyebrowDivider, null, "Risk profile locked"), /*#__PURE__*/React.createElement(HeroNumberCard, {
    title: "Meera's risk number",
    meta: "Locked \xB7 15 Sep 2026",
    value: 54,
    badge: "Moderate",
    copy: "We look at three things and go with the lowest of them. Her money can take more risk than she can.",
    rows: [{
      label: 'What her finances can absorb',
      value: 71
    }, {
      label: 'What she can sit through calmly',
      value: 54,
      binding: true
    }, {
      label: 'What her ₹2 crore goal needs',
      value: 62
    }]
  }), /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement(SentinelText, {
    text: "She is a 54, Moderate. Her finances could carry more, but she would not sleep through it \u2014 so 54 is what we build against."
  }))) : /*#__PURE__*/React.createElement(SentinelBlock, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(SentinelText, {
    text: step.q
  }), step.sub && /*#__PURE__*/React.createElement("p", {
    style: f(400, 13, 18, 'var(--color-muted)')
  }, step.sub))))), !thinking && /*#__PURE__*/React.createElement(Dock, {
    chips: !result && /*#__PURE__*/React.createElement(ChipRow, null, step.chips.map(([l, vnt]) => /*#__PURE__*/React.createElement(AnswerChip, {
      key: l,
      label: l,
      variant: vnt || 'outline',
      onClick: () => advance(l)
    }))),
    cta: result ? /*#__PURE__*/React.createElement(DarkButton, {
      label: "Build her a portfolio \u2192",
      onClick: onNew
    }) : undefined,
    composer: step && step.money ? /*#__PURE__*/React.createElement(MoneyComposer, {
      onSend: vv => advance(vv, 'understood as ' + vv)
    }) : /*#__PURE__*/React.createElement(Composer, {
      value: text,
      onChange: setText,
      onSend: () => text.trim() && advance(text.trim()),
      placeholder: result ? 'Ask about this' : step.composer || 'or type your answer'
    })
  }), /*#__PURE__*/React.createElement(HomeIndicator, {
    tone: result ? 'dark' : 'bronze'
  }));
}
window.JourneyScreen = JourneyScreen;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/sentinel-app/journey.jsx", error: String((e && e.message) || e) }); }

__ds_ns.AnswerChip = __ds_scope.AnswerChip;

__ds_ns.ChipRow = __ds_scope.ChipRow;

__ds_ns.ClientChip = __ds_scope.ClientChip;

__ds_ns.DarkButton = __ds_scope.DarkButton;

__ds_ns.DecisionsStrip = __ds_scope.DecisionsStrip;

__ds_ns.DownloadAction = __ds_scope.DownloadAction;

__ds_ns.InfoDot = __ds_scope.InfoDot;

__ds_ns.InlineActionRow = __ds_scope.InlineActionRow;

__ds_ns.Pill = __ds_scope.Pill;

__ds_ns.Pressable = __ds_scope.Pressable;

__ds_ns.RangePills = __ds_scope.RangePills;

__ds_ns.StickyCTA = __ds_scope.StickyCTA;

__ds_ns.SuggestionRow = __ds_scope.SuggestionRow;

__ds_ns.AllocationCard = __ds_scope.AllocationCard;

__ds_ns.ArtifactCard = __ds_scope.ArtifactCard;

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.ConstraintCallout = __ds_scope.ConstraintCallout;

__ds_ns.DataTableCard = __ds_scope.DataTableCard;

__ds_ns.DisclosureBlock = __ds_scope.DisclosureBlock;

__ds_ns.ExplainerSheet = __ds_scope.ExplainerSheet;

__ds_ns.HeroNumberCard = __ds_scope.HeroNumberCard;

__ds_ns.InfoCard = __ds_scope.InfoCard;

__ds_ns.MoveCard = __ds_scope.MoveCard;

__ds_ns.RejectCallout = __ds_scope.RejectCallout;

__ds_ns.DetourBanner = __ds_scope.DetourBanner;

__ds_ns.GreetingDivider = __ds_scope.GreetingDivider;

__ds_ns.MessageActions = __ds_scope.MessageActions;

__ds_ns.ParseNote = __ds_scope.ParseNote;

__ds_ns.ProgressTrace = __ds_scope.ProgressTrace;

__ds_ns.QAPair = __ds_scope.QAPair;

__ds_ns.ResponseFeedback = __ds_scope.ResponseFeedback;

__ds_ns.SentinelBlock = __ds_scope.SentinelBlock;

__ds_ns.SentinelText = __ds_scope.SentinelText;

__ds_ns.SentinelThinking = __ds_scope.SentinelThinking;

__ds_ns.StepTrace = __ds_scope.StepTrace;

__ds_ns.UserBubble = __ds_scope.UserBubble;

__ds_ns.VersionRow = __ds_scope.VersionRow;

__ds_ns.Composer = __ds_scope.Composer;

__ds_ns.MoneyComposer = __ds_scope.MoneyComposer;

__ds_ns.AttributionChart = __ds_scope.AttributionChart;

__ds_ns.ChartBar = __ds_scope.ChartBar;

__ds_ns.ChartLegend = __ds_scope.ChartLegend;

__ds_ns.ChartLine = __ds_scope.ChartLine;

__ds_ns.ChartLineMultiples = __ds_scope.ChartLineMultiples;

__ds_ns.ChartReadout = __ds_scope.ChartReadout;

__ds_ns.ChartShare = __ds_scope.ChartShare;

__ds_ns.ChartTooltip = __ds_scope.ChartTooltip;

__ds_ns.ConcentrationBar = __ds_scope.ConcentrationBar;

__ds_ns.DrawnCheck = __ds_scope.DrawnCheck;

__ds_ns.Dumbbell = __ds_scope.Dumbbell;

__ds_ns.ProgressRail = __ds_scope.ProgressRail;

__ds_ns.StatTile = __ds_scope.StatTile;

__ds_ns.Sparkline = __ds_scope.Sparkline;

__ds_ns.CHART_RAMP = __ds_scope.CHART_RAMP;

__ds_ns.SEGMENT_GAP = __ds_scope.SEGMENT_GAP;

__ds_ns.PLOT = __ds_scope.PLOT;

__ds_ns.AXIS_BAND = __ds_scope.AXIS_BAND;

__ds_ns.FileUpload = __ds_scope.FileUpload;

__ds_ns.SearchField = __ds_scope.SearchField;

__ds_ns.SelectionMark = __ds_scope.SelectionMark;

__ds_ns.IconArrow = __ds_scope.IconArrow;

__ds_ns.IconAttach = __ds_scope.IconAttach;

__ds_ns.IconCheckCircle = __ds_scope.IconCheckCircle;

__ds_ns.IconChevronRight = __ds_scope.IconChevronRight;

__ds_ns.IconDownload = __ds_scope.IconDownload;

__ds_ns.IconFile = __ds_scope.IconFile;

__ds_ns.IconInfo = __ds_scope.IconInfo;

__ds_ns.IconMenu = __ds_scope.IconMenu;

__ds_ns.IconPlus = __ds_scope.IconPlus;

__ds_ns.IconSparkle = __ds_scope.IconSparkle;

__ds_ns.IconSpinner = __ds_scope.IconSpinner;

__ds_ns.FollowUpRow = __ds_scope.FollowUpRow;

__ds_ns.List = __ds_scope.List;

__ds_ns.ListRow = __ds_scope.ListRow;

__ds_ns.CanvasHeader = __ds_scope.CanvasHeader;

__ds_ns.Dock = __ds_scope.Dock;

__ds_ns.HomeIndicator = __ds_scope.HomeIndicator;

__ds_ns.REDUCED_MOTION_CSS = __ds_scope.REDUCED_MOTION_CSS;

__ds_ns.MotionGuard = __ds_scope.MotionGuard;

__ds_ns.PhoneFrame = __ds_scope.PhoneFrame;

__ds_ns.ScreenBackdrop = __ds_scope.ScreenBackdrop;

__ds_ns.ScrollToBottomButton = __ds_scope.ScrollToBottomButton;

__ds_ns.StatusSpacer = __ds_scope.StatusSpacer;

__ds_ns.TopBar = __ds_scope.TopBar;

__ds_ns.DemoFooter = __ds_scope.DemoFooter;

__ds_ns.Eyebrow = __ds_scope.Eyebrow;

__ds_ns.EyebrowDivider = __ds_scope.EyebrowDivider;

__ds_ns.Provenance = __ds_scope.Provenance;

__ds_ns.StandingDisclosure = __ds_scope.StandingDisclosure;

})();
