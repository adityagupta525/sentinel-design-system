StandingDisclosure — the always-present line under the composer in the Dock, set in data-deemph grey. One line only; it repeats on every screen. Copy is `[PLACEHOLDER — compliance to supply]` until compliance supplies the final wording, which must also fit one line.
```jsx
<StandingDisclosure />
```
