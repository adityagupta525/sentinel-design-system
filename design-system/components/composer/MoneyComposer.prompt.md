MoneyComposer — the composer variant for rupee questions: bronze ₹ prefix, hairline, 16px input with Indian digit grouping (1,80,000). No attach disc.
```jsx
<MoneyComposer onSend={(v)=>answer(v)} />
```
