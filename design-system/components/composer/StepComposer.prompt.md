# StepComposer

One line where a journey used to branch. `money` picks `MoneyComposer`; everything else gets
`Composer`.

Use it on every question a journey asks. Rule 3 says the composer is always there; this says which
one, and it says it from the step rather than from the page — so a question that becomes a money
question does not need the screen edited.
