SentinelThinking — the short (≈620ms) wait state: shimmering sparkle label + three 6px bronze dots bouncing 3px.
```jsx
<SentinelThinking />
```
For work that names real steps use ProgressTrace instead.
