SentinelBlock — Sentinel's turn: bronze sparkle + 12px bold muted "Sentinel", then content set directly on the canvas (never in a bubble).
```jsx
<SentinelBlock><SentinelText text="Two moves, not seven." /></SentinelBlock>
```
