Dock — bottom stack on every screen: chips, then CTA, then the composer, then the standing disclosure. Composer is mandatory.
```jsx
<Dock chips={<ChipRow>…</ChipRow>} cta={<DarkButton label="Approve both moves" />} composer={<Composer … />} />
```
