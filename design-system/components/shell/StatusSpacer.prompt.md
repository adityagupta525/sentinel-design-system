StatusSpacer — the 44px iOS status row (9:41 · signal · wifi · battery) in ink. `time` defaults to
'9:41'; pass one only where the screen states a time of day.
```jsx
<StatusSpacer />
<StatusSpacer time="19:04" />
```
