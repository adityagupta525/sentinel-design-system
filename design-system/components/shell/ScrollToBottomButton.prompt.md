ScrollToBottomButton — 36px white floating disc with a bronze down-arrow; appears when the thread is scrolled away from the bottom.
```jsx
<ScrollToBottomButton show={!nearBottom} onClick={scrollToBottom} />
```
