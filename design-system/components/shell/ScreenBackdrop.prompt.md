ScreenBackdrop — canvas fill + 3px dotted paper texture + two radial auras (bronze top-left, shadow top-right). First child of every screen.
```jsx
<div style={{position:'relative'}}><ScreenBackdrop />…</div>
```
