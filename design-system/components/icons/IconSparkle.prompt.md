IconSparkle — the sparkle glyph from the Figma export; use where src/lib/icons.tsx uses it.
```jsx
<IconSparkle />
<IconSparkle stroke="var(--color-bronze-deep)" />
```
Stroke-only, round caps, 1.33–1.5px stroke. Never recolour outside the ink/bronze/muted/white set.
