IconChevronRight — the chevronright glyph from the Figma export; use where src/lib/icons.tsx uses it.
```jsx
<IconChevronRight />
<IconChevronRight stroke="var(--color-bronze-deep)" />
```
Stroke-only, round caps, 1.33–1.5px stroke. Never recolour outside the ink/bronze/muted/white set.
