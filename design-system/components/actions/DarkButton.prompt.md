DarkButton — the single primary CTA: 48px, radius 16, dark vertical gradient, white 14px semibold + arrow. Lives in the Dock's cta slot, above the composer.
```jsx
<DarkButton label="Rebalance to his mandate" onClick={…} />
```
