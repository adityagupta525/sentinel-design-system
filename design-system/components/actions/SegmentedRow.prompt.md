SegmentedRow — one of a set as 32px filter pills; `locked` makes the whole row inert and states why beneath it, never hiding an option.
```jsx
<SegmentedRow label="Appearance" options={['Light','Dark']} value="Light" locked lockedNote="One theme so far. Dark arrives with its tokens." />
```
