RejectCallout — "That does not fit" callout with outline chips offering the two ways out. The user's text stays in the composer.
```jsx
<RejectCallout eyebrow="That does not fit" body="₹60,00,000 is above the ₹50 L ceiling on Meera's mandate. I have not applied it." chips={['Use ₹50 L','Raise the mandate first']} />
```
